# 💾 Application панель

> Storage, Cookies, Cache.

---

## 📦 Storage (Хранилище):

### 1. LOCAL STORAGE

**Определение:** Постоянное хранилище данных на клиенте.

**Характеристики:**
- Хранение данных без срока действия
- Доступно только на клиенте
- Максимум ~5-10 MB (зависит от браузера)
- Данные сохраняются между сессиями
- Доступно из JavaScript

**Просмотр:**
- **Application** → **Local Storage** → домен
- Key-Value пары
- Можно редактировать/удалять

**Пример данных:**
```
Key: token          Value: abc123xyz
Key: theme          Value: dark
Key: user_preferences Value: {"language":"ru","notifications":true}
```

**Что проверять:**
- ✓ Токены сохранены корректно
- ✓ Настройки пользователя сохраняются
- ✓ Нет чувствительных данных (пароли)
- ✓ Данные очищаются при logout
- ✓ Размер данных в пределах лимита

---

### 2. SESSION STORAGE

**Определение:** Временное хранилище данных на время сессии.

**Характеристики:**
- Хранение на время сессии
- Удаляется при закрытии вкладки
- Изолировано по вкладкам (каждая вкладка имеет свой sessionStorage)
- Максимум ~5-10 MB
- Доступно из JavaScript

**Просмотр:**
- **Application** → **Session Storage** → домен
- Key-Value пары
- Можно редактировать/удалять

**Пример данных:**
```
Key: form_data      Value: {"step":2,"fields":["name","email"]}
Key: temp_token     Value: temp123
```

**Что проверять:**
- ✓ Временные данные сохраняются
- ✓ Состояние формы сохраняется
- ✓ Данные удаляются при закрытии вкладки
- ✓ Изоляция между вкладками работает

---

### 3. COOKIES

**Определение:** Небольшие данные, отправляемые с каждым запросом.

**Характеристики:**
- Отправляются с каждым запросом
- Могут иметь срок действия (Expires)
- Могут быть HttpOnly (недоступны из JavaScript)
- Могут быть Secure (только HTTPS)
- Могут иметь SameSite атрибут

**Просмотр:**
- **Application** → **Cookies** → домен
- Таблица с атрибутами кук
- Можно редактировать/удалять

**Колонки:**
- **Name** — имя куки
- **Value** — значение
- **Domain** — для какого домена
- **Path** — для какого пути
- **Expires** — срок действия
- **Size** — размер
- **HttpOnly** — недоступна из JS
- **Secure** — только HTTPS
- **SameSite** — защита от CSRF

**Пример:**
```
Name: session_id
Value: abc123xyz
Domain: .example.com
Path: /
Expires: 2024-01-20T10:30:00Z
Size: 20 B
HttpOnly: ✓
Secure: ✓
SameSite: Strict
```

**Что проверять:**
- ✓ Session cookie установлена корректно
- ✓ HttpOnly для токенов (защита от XSS)
- ✓ Secure для production (только HTTPS)
- ✓ SameSite настроен (защита от CSRF)
- ✓ Expires корректен (не истек)
- ✓ Domain и Path правильные

---

### 4. INDEXEDDB

**Определение:** База данных в браузере для больших объемов данных.

**Характеристики:**
- База данных в браузере
- Для больших объемов данных (больше чем localStorage)
- Асинхронная работа
- Поддержка индексов
- Поддержка транзакций

**Просмотр:**
- **Application** → **IndexedDB** → база данных
- Структура базы данных
- Таблицы (Object Stores)
- Данные в таблицах

**Использование:**
- Хранение больших объемов данных
- Офлайн приложения
- Кеширование данных
- PWA приложения

**Что проверять:**
- ✓ Данные сохраняются корректно
- ✓ Структура базы данных правильная
- ✓ Индексы работают
- ✓ Данные доступны офлайн

---

### 5. CACHE STORAGE

**Определение:** Кеш для Service Worker.

**Характеристики:**
- Service Worker кеш
- Для PWA приложений
- Офлайн режим
- Кеширование ресурсов

**Просмотр:**
- **Application** → **Cache Storage** → кеш
- Список закешированных ресурсов
- Можно просмотреть содержимое

**Использование:**
- Кеширование статических ресурсов
- Офлайн доступ к ресурсам
- Улучшение производительности

**Что проверять:**
- ✓ Ресурсы кешируются корректно
- ✓ Офлайн режим работает
- ✓ Кеш обновляется при необходимости
- ✓ Нет устаревших ресурсов в кеше

---

## 🔄 Service Workers:

**Определение:** Фоновые скрипты для перехвата запросов.

**Характеристики:**
- Фоновые скрипты
- Перехват запросов
- Push уведомления
- Офлайн режим
- Кеширование ресурсов

**Просмотр:**
- **Application** → **Service Workers**
- Список зарегистрированных Service Workers
- Статус (activated, waiting, installing)
- Кнопки управления (Update, Unregister)

**Что показывает:**
- **Source** — URL Service Worker скрипта
- **Status** — статус (activated, waiting, installing)
- **Clients** — клиенты, использующие Service Worker

**Что проверять:**
- ✓ Service Worker зарегистрирован
- ✓ Статус activated (активен)
- ✓ Перехват запросов работает
- ✓ Офлайн режим работает
- ✓ Push уведомления работают (если используются)

---

## 📄 Manifest:

**Определение:** PWA манифест приложения.

**Характеристики:**
- PWA манифест
- Иконки, название
- Настройки приложения
- Офлайн режим
- Установка как приложение

**Просмотр:**
- **Application** → **Manifest**
- Информация о манифесте
- Иконки приложения
- Настройки отображения

**Что показывает:**
- **Name** — название приложения
- **Short name** — короткое название
- **Icons** — иконки приложения
- **Start URL** — стартовая страница
- **Display** — режим отображения (standalone, fullscreen)
- **Theme color** — цвет темы
- **Background color** — цвет фона

**Что проверять:**
- ✓ Манифест присутствует
- ✓ Иконки загружаются
- ✓ Название приложения правильное
- ✓ Настройки отображения корректны
- ✓ Приложение можно установить

---

## ✅ Что проверять:

### ✓ Токены в localStorage/cookies

**Проверка:**
- Открыть Application → Local Storage или Cookies
- Проверить наличие токенов
- Убедиться, что токены сохраняются корректно

**Что проверять:**
- ✅ Токен сохраняется после логина
- ✅ Токен имеет правильный формат
- ✅ Токен не истек (если есть срок действия)

---

### ✓ HttpOnly для чувствительных cookies

**Проверка:**
- Открыть Application → Cookies
- Найти cookies с токенами/сессиями
- Проверить, что HttpOnly установлен

**Что проверять:**
- ✅ Session cookies имеют HttpOnly
- ✅ Токены в cookies имеют HttpOnly
- ✅ Чувствительные данные защищены от XSS

---

### ✓ Secure в production

**Проверка:**
- Открыть Application → Cookies
- Проверить, что Secure установлен для production

**Что проверять:**
- ✅ Cookies имеют Secure флаг
- ✅ Только HTTPS используется
- ✅ Нет cookies без Secure в production

---

### ✓ Нет паролей в storage

**Проверка:**
- Открыть Application → Local Storage, Session Storage, Cookies
- Проверить отсутствие паролей в хранилище

**Что проверять:**
- ✅ Пароли не сохраняются в localStorage
- ✅ Пароли не сохраняются в sessionStorage
- ✅ Пароли не сохраняются в cookies (кроме session cookies)
- ✅ Нет открытых паролей в хранилище

---

### ✓ Данные очищаются при logout

**Проверка:**
1. Залогиниться в приложение
2. Проверить данные в Application панели
3. Выполнить logout
4. Проверить, что данные очищены

**Что проверять:**
- ✅ Токены удалены из localStorage/cookies
- ✅ Данные пользователя очищены
- ✅ Session Storage очищен (если используется)
- ✅ Нет остаточных данных после logout

---

## 💡 Ключевые моменты

> **📌 Запомните:**

- **Local Storage - постоянное хранение** — данные сохраняются между сессиями
- **Session Storage - на время сессии** — данные удаляются при закрытии вкладки
- **Cookies - HttpOnly, Secure, SameSite** — важные атрибуты безопасности
- **Не храни пароли в storage** — пароли не должны храниться в браузере

---

## 🎯 Практические примеры:

### Пример 1: Проверка токена

**Задача:** Проверить, что токен сохраняется после логина.

**Шаги:**
1. Открыть Application → Local Storage (или Cookies)
2. Выполнить логин
3. Проверить наличие токена
4. Проверить формат токена
5. Проверить атрибуты (если в cookies: HttpOnly, Secure)

---

### Пример 2: Тестирование logout

**Задача:** Проверить, что данные очищаются при logout.

**Шаги:**
1. Залогиниться в приложение
2. Открыть Application панель
3. Записать данные в Local Storage/Cookies
4. Выполнить logout
5. Проверить, что данные удалены

---

### Пример 3: Проверка безопасности cookies

**Задача:** Проверить безопасность cookies с токенами.

**Шаги:**
1. Открыть Application → Cookies
2. Найти cookies с токенами/сессиями
3. Проверить:
   - HttpOnly установлен
   - Secure установлен (для HTTPS)
   - SameSite установлен
   - Expires корректен

---

## 📋 Best Practices:

### ✅ Рекомендации:

1. **Всегда проверяйте Application панель** — при тестировании веб-приложений
2. **Проверяйте безопасность cookies** — HttpOnly, Secure, SameSite
3. **Не храните пароли** — пароли не должны быть в storage
4. **Проверяйте очистку данных** — данные должны очищаться при logout
5. **Используйте правильное хранилище** — Local Storage для постоянных данных, Session Storage для временных
6. **Проверяйте размер данных** — не превышайте лимиты хранилища
7. **Тестируйте офлайн режим** — для PWA приложений

---

## 🔄 Интеграция с другими панелями:

### Application + Network:

**Использование:**
- Проверить отправку cookies в запросах
- Проверить использование кеша
- Проверить работу Service Worker

---

### Application + Console:

**Использование:**
- Проверить доступность данных из JavaScript
- Тестировать работу с localStorage/sessionStorage
- Проверить работу с cookies (если не HttpOnly)

---

### Application + Elements:

**Использование:**
- Проверить влияние данных на отображение
- Проверить работу с формами
- Проверить сохранение состояния

---

## 🔒 Безопасность:

### ⚠️ Риски:

- **XSS атаки** — если cookies не имеют HttpOnly
- **CSRF атаки** — если cookies не имеют SameSite
- **Перехват данных** — если cookies не имеют Secure
- **Утечка паролей** — если пароли хранятся в storage

### ✅ Защита:

- **HttpOnly** — защита от XSS
- **Secure** — только HTTPS
- **SameSite** — защита от CSRF
- **Не хранить пароли** — пароли не должны быть в браузере
- **Очистка данных** — данные должны очищаться при logout

---

[← Вернуться к содержанию раздела](./README.md)
