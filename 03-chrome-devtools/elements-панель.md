# 📊 Elements панель

> Инспектирование HTML и CSS.

---

## 🔍 Инспектирование элементов:

### 1. Выбрать элемент:

**Способы:**
- `Ctrl + Shift + C` (Windows/Linux) или `Cmd + Option + C` (macOS) — открыть инспектор
- Навести курсор на элемент на странице → кликнуть
- Элемент подсветится в панели Elements

**Результат:**
- Элемент выделяется на странице
- HTML код элемента отображается в панели
- CSS стили элемента показываются справа

---

### 2. Просмотр HTML:

**Что можно увидеть:**
- Структура DOM дерева
- Атрибуты элементов (id, class, data-*, и т.д.)
- Вложенность элементов
- Текстовое содержимое

**Пример:**
```html
<div class="container" id="main">
  <h1 class="title">Заголовок</h1>
  <p class="text">Текст параграфа</p>
</div>
```

**Навигация:**
- Развернуть/свернуть элементы (стрелки слева)
- Клик по элементу для выделения
- Правой кнопкой → контекстное меню

---

### 3. Редактирование HTML:

**Способы редактирования:**

**Текст:**
- Двойной клик по тексту → редактировать текст
- Изменения применяются сразу

**HTML структура:**
- Правой кнопкой → **Edit as HTML**
- Редактировать HTML код напрямую
- Сохранить изменения

**Удаление:**
- Выбрать элемент → нажать `Delete`
- Элемент удаляется из DOM

**Важно:**
- ⚠️ Изменения временные (обновление страницы сбросит)
- ✅ Полезно для тестирования изменений
- ✅ Можно проверить поведение без изменения кода

---

## 🎨 Работа с CSS:

### Styles панель (справа):

**Что показывает:**
- Все стили элемента
- Стили из разных источников (inline, CSS файлы)
- Переопределенные стили (перечеркнутые)
- Активные стили (не перечеркнутые)

**Редактирование:**
- Клик по значению → редактировать
- Клик по свойству → добавить новое
- Галочка слева → включить/выключить свойство
- Изменения применяются в реальном времени

**Пример:**
```css
.title {
  color: #333;        /* Активный стиль */
  font-size: 24px;     /* Активный стиль */
  margin: 0;           /* Переопределен (перечеркнут) */
}
```

---

### Computed панель:

**Что показывает:**
- Финальные вычисленные стили элемента
- Box Model (margin, padding, border, width, height)
- Размеры элемента в пикселях
- Все примененные стили после всех вычислений

**Box Model визуализация:**
```
┌─────────────────────────┐
│      margin (внешний)    │
│  ┌───────────────────┐  │
│  │     border        │  │
│  │  ┌─────────────┐  │  │
│  │  │   padding   │  │  │
│  │  │  ┌───────┐  │  │  │
│  │  │  │content│  │  │  │
│  │  │  └───────┘  │  │  │
│  │  └─────────────┘  │  │
│  └───────────────────┘  │
└─────────────────────────┘
```

**Использование:**
- Проверка итоговых размеров элемента
- Анализ отступов и границ
- Отладка проблем с версткой

---

## 🛠️ Полезные функции:

### 1. :hov (псевдоклассы)

**Назначение:** Эмуляция псевдоклассов для тестирования стилей.

**Доступные псевдоклассы:**
- `:hover` — наведение мыши
- `:active` — активное состояние (клик)
- `:focus` — фокус на элементе
- `:focus-within` — фокус внутри элемента
- `:visited` — посещенная ссылка

**Как использовать:**
1. Выбрать элемент в Elements
2. В Styles панели найти кнопку `:hov`
3. Включить нужный псевдокласс
4. Проверить примененные стили

**Когда использовать:**
- Проверка стилей при наведении
- Тестирование интерактивных элементов
- Проверка состояний кнопок и ссылок

---

### 2. .cls (классы)

**Назначение:** Быстрое добавление/удаление CSS классов.

**Как использовать:**
1. Выбрать элемент в Elements
2. В Styles панели найти поле `.cls`
3. Ввести название класса → Enter
4. Класс добавится к элементу

**Возможности:**
- Добавить класс → ввести название
- Удалить класс → убрать галочку
- Быстрое тестирование разных стилей
- Проверка влияния классов на элемент

**Пример:**
```
.cls: [active] [highlighted] [disabled]
```

---

### 3. Box Model

**Назначение:** Визуализация margin, padding, border и размеров элемента.

**Что показывает:**
- **Margin** — внешние отступы (желтый)
- **Border** — границы (коричневый)
- **Padding** — внутренние отступы (зеленый)
- **Content** — содержимое (синий)
- Размеры в пикселях

**Как использовать:**
1. Выбрать элемент
2. В Computed панели найти Box Model
3. Навести на значения → увидеть визуализацию
4. Клик по значению → редактировать

**Когда использовать:**
- Отладка проблем с отступами
- Проверка размеров элементов
- Анализ верстки

---

### 4. Accessibility (доступность)

**Назначение:** Проверка доступности элементов для пользователей с ограниченными возможностями.

**Что проверяет:**
- **ARIA атрибуты** — aria-label, aria-describedby, aria-hidden
- **Контрастность цветов** — соответствие WCAG стандартам
- **Роли элементов** — правильное использование ролей
- **Доступность с клавиатуры** — возможность навигации
- **Альтернативный текст** — для изображений

**Как использовать:**
1. Выбрать элемент в Elements
2. В правой панели найти вкладку **Accessibility**
3. Просмотреть информацию о доступности

**Что показывает:**
- Роль элемента (button, link, heading и т.д.)
- Имя элемента (для screen readers)
- Контрастность текста
- Проблемы доступности (если есть)

---

## ✅ Что проверять:

### ✓ Элемент присутствует в DOM

**Проверка:**
- Найти элемент в Elements панели
- Убедиться, что элемент отображается
- Проверить, что элемент не скрыт (display: none, visibility: hidden)

**Как проверить:**
1. Использовать поиск в Elements (`Ctrl + F`)
2. Ввести селектор или текст элемента
3. Убедиться, что элемент найден

---

### ✓ Правильные классы и атрибуты

**Проверка:**
- Классы применены корректно
- Атрибуты присутствуют и имеют правильные значения
- ID уникальны (если используются)
- Data-атрибуты заполнены правильно

**Примеры проверки:**
```html
<!-- Проверить: -->
<button class="btn btn-primary" id="submit-btn" data-action="submit">
  Отправить
</button>
```

**Что проверить:**
- ✅ Класс `btn` и `btn-primary` присутствуют
- ✅ ID `submit-btn` уникален
- ✅ Data-атрибут `data-action` имеет значение `submit`

---

### ✓ Стили применены корректно

**Проверка:**
- Стили из CSS файлов применяются
- Нет конфликтов стилей
- Переопределения работают правильно
- Inline стили (если есть) корректны

**Как проверить:**
1. Выбрать элемент в Elements
2. Просмотреть Styles панель
3. Убедиться, что нужные стили активны
4. Проверить, что перечеркнутые стили действительно переопределены

---

### ✓ Адаптивность (разные размеры экрана)

**Проверка:**
- Элементы корректно отображаются на разных размерах
- Медиа-запросы работают правильно
- Верстка не ломается при изменении размера

**Как проверить:**
1. Открыть Device Toolbar (`Ctrl + Shift + M`)
2. Выбрать разные устройства (мобильный, планшет, десктоп)
3. Проверить отображение элементов
4. Использовать Elements для проверки примененных стилей

**Размеры для проверки:**
- Мобильный: 375px, 414px
- Планшет: 768px, 1024px
- Десктоп: 1280px, 1920px

---

### ✓ Доступность (aria-labels, roles)

**Проверка:**
- ARIA атрибуты присутствуют где нужно
- Роли элементов правильные
- Элементы доступны с клавиатуры
- Контрастность соответствует стандартам

**Что проверить:**
- ✅ aria-label для иконок без текста
- ✅ aria-describedby для описаний
- ✅ role="button" для элементов, которые ведут себя как кнопки
- ✅ Контрастность текста (минимум 4.5:1 для обычного текста)

**Пример:**
```html
<!-- Хорошо: -->
<button aria-label="Закрыть окно">
  <span aria-hidden="true">×</span>
</button>

<!-- Плохо: -->
<div onclick="close()">×</div>
```

---

## 💡 Ключевые моменты

> **📌 Запомните:**

- **Инспектор - выбор элемента на странице** — `Ctrl+Shift+C` для быстрого доступа
- **Редактирование HTML/CSS в реальном времени** — изменения временные, но полезны для тестирования
- **:hov - эмуляция псевдоклассов** — проверка стилей при наведении и взаимодействии
- **Box Model - визуализация отступов** — понимание структуры элемента и его размеров
- **Accessibility - проверка доступности** — важная часть тестирования для всех пользователей

---

## 🎯 Практические примеры:

### Пример 1: Проверка кнопки

**Задача:** Проверить, что кнопка имеет правильные стили и доступна.

**Шаги:**
1. Открыть Elements панель
2. Выбрать кнопку (`Ctrl+Shift+C` → клик по кнопке)
3. Проверить классы в HTML
4. Проверить стили в Styles панели
5. Использовать `:hov` для проверки hover состояния
6. Проверить Accessibility панель

---

### Пример 2: Отладка верстки

**Задача:** Найти, почему элемент имеет неправильные отступы.

**Шаги:**
1. Выбрать проблемный элемент
2. Открыть Computed панель
3. Просмотреть Box Model
4. Проверить margin, padding, border
5. Найти источник стилей в Styles панели
6. Отредактировать стили для проверки

---

### Пример 3: Тестирование адаптивности

**Задача:** Проверить, как элемент выглядит на мобильном устройстве.

**Шаги:**
1. Открыть Device Toolbar (`Ctrl+Shift+M`)
2. Выбрать мобильное устройство (например, iPhone 12)
3. Выбрать элемент в Elements
4. Проверить примененные стили
5. Проверить медиа-запросы в Styles панели

---

## 📋 Best Practices:

### ✅ Рекомендации:

1. **Используйте поиск** — `Ctrl+F` в Elements для быстрого поиска элементов
2. **Проверяйте Computed стили** — для понимания финального результата
3. **Тестируйте псевдоклассы** — используйте `:hov` для проверки интерактивности
4. **Проверяйте доступность** — всегда проверяйте Accessibility панель
5. **Используйте Box Model** — для понимания структуры элемента
6. **Тестируйте адаптивность** — проверяйте на разных размерах экрана
7. **Сохраняйте изменения** — если нужно, копируйте CSS в файлы проекта

---

[← Вернуться к содержанию раздела](./README.md)
