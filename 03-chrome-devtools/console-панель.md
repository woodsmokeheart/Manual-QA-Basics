# 💻 Console панель

> Логи, ошибки, выполнение JavaScript.

---

## 📋 Типы сообщений:

### console.log()

**Назначение:** Обычное информационное сообщение.

**Пример:**
```javascript
console.log("Информация")  // Обычное сообщение
console.log("Пользователь залогинен:", user)
console.log("Данные:", {id: 1, name: "John"})
```

**Особенности:**
- Белый/серый цвет текста
- Для обычной информации
- Не критично для работы приложения

---

### console.info()

**Назначение:** Информационное сообщение.

**Пример:**
```javascript
console.info("Инфо")  // Информационное
console.info("Загрузка данных завершена")
```

**Особенности:**
- Синий цвет текста (обычно)
- Для важной информации
- Похоже на console.log, но с другим значением

---

### console.warn()

**Назначение:** Предупреждение о потенциальных проблемах.

**Пример:**
```javascript
console.warn("Предупреждение")  // Желтое предупреждение
console.warn("Устаревший API будет удален в следующей версии")
```

**Особенности:**
- Желтый цвет текста
- Предупреждает о возможных проблемах
- Не блокирует работу приложения

---

### console.error()

**Назначение:** Ошибка в приложении.

**Пример:**
```javascript
console.error("Ошибка")  // Красная ошибка
console.error("Не удалось загрузить данные:", error)
```

**Особенности:**
- Красный цвет текста
- Указывает на ошибки
- Может блокировать работу приложения
- ⚠️ Важно проверять при тестировании

---

### console.debug()

**Назначение:** Отладочное сообщение.

**Пример:**
```javascript
console.debug("Отладка")  // Отладочное
console.debug("Текущее состояние:", state)
```

**Особенности:**
- Серый цвет текста
- Для отладки во время разработки
- Обычно скрыто в production

---

## 🔍 Фильтрация:

**Доступные фильтры:**

- **All (все)** — показать все сообщения
- **Errors (только ошибки)** — только красные ошибки
- **Warnings (предупреждения)** — только желтые предупреждения
- **Info (информация)** — только информационные сообщения
- **Verbose (подробные)** — все сообщения, включая debug

**Как использовать:**
1. Открыть Console панель
2. Найти кнопки фильтров вверху панели
3. Кликнуть по нужному фильтру
4. Сообщения отфильтруются

**Когда использовать:**
- Поиск только ошибок при тестировании
- Фильтрация предупреждений
- Фокус на определенном типе сообщений

---

## ⚡ Выполнение JavaScript:

### Работа с DOM:

#### Получить элемент:

```javascript
// Получить элемент
document.querySelector('.button')        // Первый элемент с классом .button
document.querySelector('#submit-btn')   // Элемент с ID submit-btn
document.querySelector('input[type="email"]')  // Input с типом email
```

**Возвращает:** Первый найденный элемент или `null`.

---

#### Получить все элементы:

```javascript
// Получить все элементы
document.querySelectorAll('.item')      // Все элементы с классом .item
document.querySelectorAll('button')     // Все кнопки
document.querySelectorAll('input')      // Все input элементы
```

**Возвращает:** NodeList со всеми найденными элементами.

---

#### Кликнуть на элемент:

```javascript
// Кликнуть на элемент
document.querySelector('.button').click()
document.querySelector('#submit-btn').click()
```

**Использование:**
- Автоматизация кликов для тестирования
- Проверка обработчиков событий
- Тестирование интерактивности

---

#### Изменить текст:

```javascript
// Изменить текст
document.querySelector('h1').textContent = 'Новый текст'
document.querySelector('.title').innerHTML = '<strong>Заголовок</strong>'
```

**Разница:**
- `textContent` — только текст (безопаснее)
- `innerHTML` — HTML разметка (может быть небезопасно)

---

#### Получить значение input:

```javascript
// Получить значение input
document.querySelector('input').value
document.querySelector('#email').value
document.querySelector('input[type="password"]').value
```

**Пример:**
```javascript
// Получить email из формы
const email = document.querySelector('#email').value
console.log("Email:", email)
```

---

#### Установить значение input:

```javascript
// Установить значение input
document.querySelector('input').value = 'test@example.com'
document.querySelector('#email').value = 'user@example.com'
document.querySelector('input[type="password"]').value = 'password123'
```

**Использование:**
- Заполнение форм для тестирования
- Проверка валидации
- Тестирование автозаполнения

---

### Работа с localStorage:

#### Получить данные:

```javascript
// Получить данные из localStorage
localStorage.getItem('token')
localStorage.getItem('user')
localStorage.getItem('theme')
```

**Пример:**
```javascript
const token = localStorage.getItem('token')
console.log("Token:", token)
```

---

#### Сохранить данные:

```javascript
// Сохранить данные в localStorage
localStorage.setItem('token', 'abc123')
localStorage.setItem('user', JSON.stringify({id: 1, name: 'John'}))
localStorage.setItem('theme', 'dark')
```

**Особенности:**
- Данные сохраняются как строки
- Для объектов используйте `JSON.stringify()`
- Данные сохраняются между сессиями

---

#### Очистить localStorage:

```javascript
// Очистить все данные
localStorage.clear()

// Удалить конкретный ключ
localStorage.removeItem('token')
```

**Использование:**
- Очистка данных для тестирования
- Проверка поведения без данных
- Тестирование логина/логаута

---

## 🛠️ Полезные команды:

### clear()

**Назначение:** Очистить консоль.

**Пример:**
```javascript
clear()  // Очистить консоль
```

**Альтернатива:** `Ctrl + L` (Windows/Linux) или `Cmd + K` (macOS)

---

### copy()

**Назначение:** Копировать данные в буфер обмена.

**Пример:**
```javascript
// Копировать текст элемента
copy(document.querySelector('.data').textContent)

// Копировать JSON
copy(JSON.stringify(data))

// Копировать значение переменной
copy(token)
```

**Использование:**
- Копирование данных для тестирования
- Сохранение токенов и ID
- Экспорт данных

---

### console.table()

**Назначение:** Отобразить данные в виде таблицы.

**Пример:**
```javascript
// Таблица объектов
console.table([
  {name: 'John', age: 30},
  {name: 'Jane', age: 25}
])

// Таблица массивов
console.table(['apple', 'banana', 'orange'])
```

**Особенности:**
- Удобно для просмотра массивов объектов
- Автоматическое форматирование
- Сортировка по клику на заголовок

---

### console.group()

**Назначение:** Группировать связанные сообщения.

**Пример:**
```javascript
console.group('User Data')
console.log('Name: John')
console.log('Age: 30')
console.log('Email: john@example.com')
console.groupEnd()
```

**Результат:**
```
▼ User Data
  Name: John
  Age: 30
  Email: john@example.com
```

**Использование:**
- Организация логов
- Группировка связанных данных
- Улучшение читаемости

---

### console.time() / console.timeEnd()

**Назначение:** Измерить время выполнения кода.

**Пример:**
```javascript
console.time('fetch')
// Код, время выполнения которого нужно измерить
fetch('/api/users')
  .then(response => response.json())
  .then(data => {
    console.timeEnd('fetch')  // Выведет: fetch: 123.456ms
  })
```

**Использование:**
- Измерение производительности
- Поиск медленных операций
- Оптимизация кода

---

## ✅ Что проверять:

### ✓ Нет ошибок в консоли (красные)

**Проверка:**
- Открыть Console панель
- Проверить наличие красных сообщений (console.error)
- Убедиться, что нет критических ошибок

**Что делать:**
- Если есть ошибки → зафиксировать в баг-репорте
- Проверить, влияют ли ошибки на функциональность
- Проверить, воспроизводятся ли ошибки стабильно

---

### ✓ Нет критичных предупреждений

**Проверка:**
- Проверить желтые предупреждения (console.warn)
- Оценить критичность предупреждений
- Проверить, влияют ли предупреждения на работу

**Что делать:**
- Критичные предупреждения → зафиксировать
- Некритичные → можно игнорировать (если не влияют на UX)

---

### ✓ API запросы успешны

**Проверка:**
- Открыть Network панель
- Проверить статус коды запросов
- Убедиться, что нет ошибок (4xx, 5xx)

**Что проверять:**
- Статус коды 200, 201 для успешных запросов
- Отсутствие 400, 401, 403, 404, 500 ошибок
- Корректность ответов от API

---

### ✓ Нет 404 ошибок (файлы не найдены)

**Проверка:**
- Открыть Network панель
- Фильтр: Status Code → 404
- Проверить отсутствие 404 ошибок

**Что проверять:**
- Загрузка CSS файлов
- Загрузка JavaScript файлов
- Загрузка изображений
- Загрузка шрифтов
- API запросы не возвращают 404

**Что делать:**
- Если есть 404 → проверить пути к файлам
- Зафиксировать в баг-репорте
- Проверить, влияет ли на функциональность

---

## 💡 Ключевые моменты

> **📌 Запомните:**

- **console.log/error/warn - разные типы логов** — используйте правильный тип для разных ситуаций
- **Выполнение JavaScript команд** — можно выполнять код прямо в консоли для тестирования
- **Фильтрация по типу сообщений** — удобно для поиска ошибок и предупреждений
- **Проверяй отсутствие ошибок** — красные ошибки в консоли указывают на проблемы

---

## 🎯 Практические примеры:

### Пример 1: Проверка формы

**Задача:** Проверить, что форма отправляет правильные данные.

**Шаги:**
1. Открыть Console панель
2. Заполнить форму вручную
3. Перед отправкой выполнить:
   ```javascript
   const formData = {
     email: document.querySelector('#email').value,
     password: document.querySelector('#password').value
   }
   console.log("Данные формы:", formData)
   ```
4. Отправить форму
5. Проверить Network панель для проверки отправленных данных

---

### Пример 2: Тестирование localStorage

**Задача:** Проверить сохранение данных в localStorage.

**Шаги:**
1. Выполнить действие, которое сохраняет данные
2. В Console выполнить:
   ```javascript
   console.log("LocalStorage:", localStorage)
   ```
3. Проверить сохраненные данные
4. Очистить localStorage:
   ```javascript
   localStorage.clear()
   ```
5. Проверить поведение приложения без данных

---

### Пример 3: Поиск ошибок

**Задача:** Найти все ошибки в консоли.

**Шаги:**
1. Открыть Console панель
2. Выбрать фильтр "Errors"
3. Просмотреть все красные ошибки
4. Для каждой ошибки:
   - Проверить описание
   - Проверить стек вызовов
   - Зафиксировать в баг-репорте

---

## 📋 Best Practices:

### ✅ Рекомендации:

1. **Всегда проверяйте консоль** — при каждом тестировании страницы
2. **Используйте фильтры** — для быстрого поиска ошибок
3. **Фиксируйте ошибки** — все красные ошибки должны быть зафиксированы
4. **Проверяйте Network** — вместе с Console для полной картины
5. **Используйте console.table** — для удобного просмотра данных
6. **Очищайте консоль** — перед началом нового теста
7. **Группируйте логи** — используйте console.group для организации

---

## 🔄 Интеграция с другими панелями:

### Console + Network:

**Использование:**
- Проверить ошибки в Console
- Проверить соответствующие запросы в Network
- Связать ошибки с конкретными API запросами

---

### Console + Elements:

**Использование:**
- Найти элемент в Elements
- Выполнить команды в Console для этого элемента
- Проверить изменения в Elements

---

### Console + Sources:

**Использование:**
- Найти ошибку в Console
- Перейти к источнику ошибки в Sources
- Отладить проблему с помощью breakpoints

---

[← Вернуться к содержанию раздела](./README.md)
