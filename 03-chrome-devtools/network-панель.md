# 🌐 Network панель

> Мониторинг HTTP запросов.

---

## 📊 Основные колонки:

### Name

**Описание:** Имя файла или endpoint.

**Что показывает:**
- URL запроса
- Имя файла (для статических ресурсов)
- Endpoint API (для запросов)

**Примеры:**
- `index.html` — HTML документ
- `/api/users` — API endpoint
- `styles.css` — CSS файл
- `script.js` — JavaScript файл

---

### Status

**Описание:** HTTP статус код.

**Примеры:**
- `200` — успешный запрос
- `201` — ресурс создан
- `404` — не найдено
- `500` — ошибка сервера

**Цветовая индикация:**
- 🟢 Зеленый — успешные запросы (2xx)
- 🟡 Желтый — редиректы (3xx)
- 🔴 Красный — ошибки (4xx, 5xx)

---

### Type

**Описание:** Тип ресурса.

**Типы:**
- `document` — HTML документ
- `xhr` — XMLHttpRequest (AJAX запросы)
- `fetch` — Fetch API запросы
- `js` — JavaScript файлы
- `css` — CSS файлы
- `img` — изображения
- `font` — шрифты
- `media` — видео/аудио
- `websocket` — WebSocket соединения

---

### Initiator

**Описание:** Кто инициировал запрос.

**Примеры:**
- `index.html` — запрос из HTML
- `script.js:45` — запрос из JavaScript файла, строка 45
- `(index)` — запрос из HTML документа
- `Other` — другой источник

**Использование:**
- Поиск источника запросов
- Отладка цепочки запросов
- Понимание зависимостей

---

### Size

**Описание:** Размер ответа.

**Формат:**
- `1.2 KB` — размер ответа
- `(from cache)` — загружено из кеша
- `0 B` — пустой ответ

**Использование:**
- Проверка размера ответов
- Оптимизация производительности
- Поиск больших файлов

---

### Time

**Описание:** Время выполнения запроса.

**Формат:**
- `123 ms` — время в миллисекундах
- `1.2 s` — время в секундах

**Использование:**
- Проверка производительности
- Поиск медленных запросов
- Анализ времени загрузки

---

### Waterfall

**Описание:** Визуализация времени выполнения запроса.

**Что показывает:**
- Время в очереди (Queueing)
- DNS lookup
- Установка соединения
- SSL handshake
- Отправка запроса
- Ожидание ответа (TTFB)
- Загрузка контента

**Использование:**
- Визуальный анализ времени
- Поиск узких мест
- Понимание этапов загрузки

---

## 🔍 Фильтры:

### All

**Описание:** Показать все запросы.

**Использование:**
- Общий обзор всех запросов
- По умолчанию

---

### Fetch/XHR

**Описание:** Только API запросы.

**Что показывает:**
- XMLHttpRequest запросы
- Fetch API запросы
- AJAX запросы

**Использование:**
- Тестирование API
- Проверка запросов к серверу
- Отладка API взаимодействий

---

### JS

**Описание:** JavaScript файлы.

**Использование:**
- Проверка загрузки скриптов
- Поиск проблем с JavaScript
- Проверка размера JS файлов

---

### CSS

**Описание:** CSS стили.

**Использование:**
- Проверка загрузки стилей
- Поиск проблем с CSS
- Проверка размера CSS файлов

---

### Img

**Описание:** Изображения.

**Использование:**
- Проверка загрузки изображений
- Поиск проблем с изображениями
- Проверка размера изображений
- Проверка оптимизации изображений

---

### Media

**Описание:** Видео и аудио файлы.

**Использование:**
- Проверка загрузки медиа файлов
- Поиск проблем с воспроизведением
- Проверка размера медиа файлов

---

### Font

**Описание:** Шрифты.

**Использование:**
- Проверка загрузки шрифтов
- Поиск проблем с отображением текста
- Проверка размера шрифтов

---

### Doc

**Описание:** HTML документы.

**Использование:**
- Проверка загрузки HTML
- Поиск проблем с навигацией
- Проверка редиректов

---

### WS

**Описание:** WebSocket соединения.

**Использование:**
- Проверка WebSocket соединений
- Мониторинг real-time коммуникаций
- Отладка WebSocket проблем

---

## 📋 Детали запроса:

### Клик на запрос → вкладки:

#### 1. Headers (Заголовки)

**Что показывает:**

**Request URL:**
```
https://api.example.com/users/1
```

**Request Method:**
```
GET, POST, PUT, PATCH, DELETE
```

**Status Code:**
```
200 OK
404 Not Found
500 Internal Server Error
```

**Request Headers:**
```
Authorization: Bearer token123
Content-Type: application/json
Accept: application/json
Cookie: session_id=abc123
```

**Response Headers:**
```
Set-Cookie: session_id=abc123; HttpOnly; Secure
Content-Type: application/json
Cache-Control: no-cache
```

**Использование:**
- Проверка заголовков запроса
- Проверка заголовков ответа
- Отладка проблем с авторизацией
- Проверка Content-Type

---

#### 2. Preview (Предпросмотр)

**Что показывает:**
- Форматированный ответ
- JSON дерево (для JSON ответов)
- Изображения (для изображений)
- HTML (для HTML документов)

**Пример для JSON:**
```json
{
  "id": 1,
  "name": "John",
  "email": "john@example.com"
}
```

**Использование:**
- Быстрый просмотр ответа
- Проверка структуры данных
- Валидация JSON
- Просмотр изображений

---

#### 3. Response (Ответ)

**Что показывает:**
- Сырой ответ (raw response)
- Можно скопировать
- Полный текст ответа

**Использование:**
- Копирование ответа для анализа
- Проверка точного содержимого
- Сохранение ответов для тестирования

---

#### 4. Timing (Время)

**Что показывает:**

**Queueing:**
- Время ожидания в очереди
- Обычно 0ms, если нет блокировки

**DNS Lookup:**
- Время поиска DNS
- Обычно 0ms, если DNS закеширован

**Initial connection:**
- Время установки соединения
- TCP handshake

**SSL:**
- Время SSL handshake
- Только для HTTPS

**Request sent:**
- Время отправки запроса
- Обычно очень быстро (<1ms)

**Waiting (TTFB - Time To First Byte):**
- Время ожидания первого байта
- Время ответа сервера
- ⚠️ Важный показатель производительности

**Content Download:**
- Время загрузки контента
- Зависит от размера ответа

**Использование:**
- Анализ производительности
- Поиск узких мест
- Оптимизация запросов

---

## 🛠️ Полезные функции:

### 1. Preserve log

**Назначение:** Сохранять логи при навигации.

**Как использовать:**
1. Открыть Network панель
2. Включить "Preserve log" (галочка вверху)
3. Логи не будут очищаться при переходе на другую страницу

**Когда использовать:**
- Тестирование многостраничных приложений
- Отслеживание запросов при навигации
- Анализ цепочки запросов

---

### 2. Disable cache

**Назначение:** Отключить кеш браузера.

**Как использовать:**
1. Открыть Network панель
2. Включить "Disable cache" (галочка вверху)
3. Всегда загружаются свежие данные

**Когда использовать:**
- Тестирование изменений на сервере
- Проверка актуальности данных
- Отладка проблем с кешем

---

### 3. Throttling (троттлинг)

**Назначение:** Эмуляция медленного интернета.

**Доступные опции:**
- **Online** — обычная скорость
- **Fast 3G** — быстрый 3G (~1.5 Mbps)
- **Slow 3G** — медленный 3G (~400 Kbps)
- **Offline** — офлайн режим

**Как использовать:**
1. Открыть Network панель
2. Выбрать скорость из выпадающего списка
3. Проверить поведение приложения

**Когда использовать:**
- Тестирование на медленном соединении
- Проверка загрузки на мобильных устройствах
- Тестирование offline функциональности
- Проверка обработки ошибок сети

---

### 4. Copy as cURL

**Назначение:** Копировать запрос как cURL команду.

**Как использовать:**
1. Правой кнопкой на запрос
2. **Copy** → **Copy as cURL**
3. Вставить в терминал для повторения запроса

**Пример:**
```bash
curl 'https://api.example.com/users' \
  -H 'Authorization: Bearer token123' \
  -H 'Content-Type: application/json'
```

**Использование:**
- Повторение запросов в терминале
- Тестирование API вне браузера
- Документирование запросов
- Отладка проблем с запросами

---

### 5. Copy as fetch

**Назначение:** Копировать запрос как JavaScript fetch код.

**Как использовать:**
1. Правой кнопкой на запрос
2. **Copy** → **Copy as fetch**
3. Вставить в Console для выполнения

**Пример:**
```javascript
fetch('https://api.example.com/users', {
  headers: {
    'Authorization': 'Bearer token123',
    'Content-Type': 'application/json'
  }
})
```

**Использование:**
- Повторение запросов в консоли
- Тестирование API в браузере
- Отладка проблем с запросами
- Автоматизация тестирования

---

## ✅ Что проверять:

### ✓ Статус коды корректны (200, 201, 404)

**Проверка:**
- Открыть Network панель
- Проверить колонку Status
- Убедиться, что запросы возвращают правильные статус коды

**Что проверять:**
- ✅ Успешные запросы → 200, 201
- ✅ Редиректы → 301, 302 (если ожидаются)
- ✅ Ошибки клиента → 400, 401, 403, 404 (если ожидаются)
- ✅ Ошибки сервера → 500, 502, 503 (не должны быть)

**Что делать:**
- Красные статус коды → зафиксировать в баг-репорте
- Неожиданные статус коды → проверить логику приложения

---

### ✓ Нет лишних запросов

**Проверка:**
- Просмотреть все запросы в Network панели
- Убедиться, что нет дублирующихся запросов
- Проверить, что все запросы необходимы

**Что проверять:**
- ✅ Нет дублирующихся API запросов
- ✅ Нет запросов к несуществующим ресурсам
- ✅ Нет лишних запросов при загрузке страницы

**Что делать:**
- Лишние запросы → зафиксировать как проблему производительности
- Дублирующиеся запросы → проверить логику приложения

---

### ✓ Время ответа приемлемо (<1s для API)

**Проверка:**
- Просмотреть колонку Time
- Проверить время ответа для API запросов
- Убедиться, что время приемлемо

**Рекомендации:**
- ✅ API запросы → < 1 секунды
- ✅ Статические ресурсы → < 500ms
- ✅ Критичные запросы → < 300ms

**Что делать:**
- Медленные запросы → зафиксировать как проблему производительности
- Проверить Timing вкладку для анализа узких мест

---

### ✓ Правильные заголовки (Authorization, Content-Type)

**Проверка:**
- Открыть запрос → вкладка Headers
- Проверить Request Headers
- Проверить Response Headers

**Что проверять:**
- ✅ Authorization заголовок присутствует (если нужен)
- ✅ Content-Type правильный (application/json, text/html и т.д.)
- ✅ Accept заголовок правильный
- ✅ Set-Cookie заголовки присутствуют (если нужны)

**Что делать:**
- Отсутствующие заголовки → зафиксировать в баг-репорте
- Неправильные заголовки → проверить логику приложения

---

### ✓ Размер ответа разумный

**Проверка:**
- Просмотреть колонку Size
- Проверить размер ответов
- Убедиться, что размеры разумные

**Рекомендации:**
- ✅ API ответы → < 100 KB (обычно)
- ✅ Изображения → оптимизированы
- ✅ JavaScript/CSS → минифицированы

**Что делать:**
- Большие ответы → зафиксировать как проблему производительности
- Проверить возможность оптимизации

---

### ✓ Нет 404 ошибок (файлы не найдены)

**Проверка:**
- Фильтр: Status Code → 404
- Проверить отсутствие 404 ошибок
- Убедиться, что все ресурсы загружаются

**Что проверять:**
- ✅ Нет 404 для CSS файлов
- ✅ Нет 404 для JavaScript файлов
- ✅ Нет 404 для изображений
- ✅ Нет 404 для API запросов (если не ожидается)

**Что делать:**
- 404 ошибки → зафиксировать в баг-репорте
- Проверить пути к файлам
- Проверить конфигурацию сервера

---

### ✓ HTTPS используется (не HTTP)

**Проверка:**
- Просмотреть все запросы
- Убедиться, что все запросы используют HTTPS
- Проверить отсутствие HTTP запросов

**Что проверять:**
- ✅ Все запросы начинаются с `https://`
- ✅ Нет запросов к `http://`
- ✅ Нет Mixed Content (HTTPS страница с HTTP ресурсами)

**Что делать:**
- HTTP запросы → зафиксировать как проблему безопасности
- Mixed Content → зафиксировать как проблему безопасности

---

## 💡 Ключевые моменты

> **📌 Запомните:**

- **Network - все HTTP запросы** — панель показывает все сетевые запросы страницы
- **Фильтры: XHR/Fetch для API** — используйте фильтры для поиска нужных запросов
- **Headers - заголовки запроса/ответа** — детальная информация о каждом запросе
- **Copy as cURL - повторить запрос** — удобно для тестирования API вне браузера

---

## 🎯 Практические примеры:

### Пример 1: Тестирование API запроса

**Задача:** Проверить, что API запрос работает корректно.

**Шаги:**
1. Открыть Network панель
2. Фильтр: Fetch/XHR
3. Выполнить действие, которое вызывает API запрос
4. Найти запрос в списке
5. Проверить:
   - Status Code (должен быть 200 или 201)
   - Request Headers (Authorization, Content-Type)
   - Response (Preview или Response вкладка)
   - Timing (время ответа)

---

### Пример 2: Поиск медленных запросов

**Задача:** Найти запросы, которые загружаются медленно.

**Шаги:**
1. Открыть Network панель
2. Отсортировать по колонке Time (клик по заголовку)
3. Просмотреть самые медленные запросы
4. Открыть Timing вкладку для анализа
5. Определить узкое место (TTFB, Content Download и т.д.)

---

### Пример 3: Копирование запроса для тестирования

**Задача:** Повторить запрос в терминале или консоли.

**Шаги:**
1. Найти нужный запрос в Network панели
2. Правой кнопкой → Copy → Copy as cURL (или Copy as fetch)
3. Вставить в терминал/консоль
4. Выполнить запрос
5. Проверить результат

---

## 📋 Best Practices:

### ✅ Рекомендации:

1. **Всегда проверяйте Network** — при каждом тестировании страницы
2. **Используйте фильтры** — для быстрого поиска нужных запросов
3. **Проверяйте статус коды** — все красные статусы должны быть зафиксированы
4. **Анализируйте Timing** — для понимания производительности
5. **Используйте Preserve log** — для многостраничных приложений
6. **Тестируйте на медленном соединении** — используйте Throttling
7. **Копируйте запросы** — для повторения и документирования

---

## 🔄 Интеграция с другими панелями:

### Network + Console:

**Использование:**
- Ошибки в Console могут указывать на проблемы с запросами
- Проверить соответствующие запросы в Network
- Связать ошибки с конкретными запросами

---

### Network + Elements:

**Использование:**
- Проверить загрузку ресурсов (CSS, JS, изображения)
- Найти элементы, которые не загрузились
- Проверить влияние на отображение страницы

---

### Network + Performance:

**Использование:**
- Анализ влияния запросов на производительность
- Поиск запросов, которые замедляют загрузку
- Оптимизация критического пути загрузки

---

[← Вернуться к содержанию раздела](./README.md)
