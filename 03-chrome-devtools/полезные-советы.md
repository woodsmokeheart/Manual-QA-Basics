# 💡 Полезные советы (Chrome DevTools)

> Набор быстрых практик для ручного тестирования в DevTools.

---

## 🧭 Базовые хоткеи, которые экономят время

- **Открыть DevTools**: `F12` / `Ctrl + Shift + I` (Windows/Linux), `Cmd + Option + I` (macOS)
- **Инспектор элемента**: `Ctrl + Shift + C` / `Cmd + Option + C`
- **Device Mode**: `Ctrl + Shift + M` / `Cmd + Shift + M`
- **Очистить Console**: `Ctrl + L` (Windows/Linux) / `Cmd + K` (macOS)

---

## 🌐 Network: быстрый чек-лист перед баг-репортом

- **Preserve log**: включай, если баг воспроизводится при переходах/редиректах
- **Disable cache**: включай, если проверяешь фиксы/обновления (иначе ловушка кэша)
- **Fetch/XHR фильтр**: почти всегда начинай с него для API
- **Проверь**:
  - статус код (2xx/3xx/4xx/5xx)
  - Request/Response Headers (особенно `Authorization`, `Content-Type`, `Set-Cookie`)
  - тело запроса (Payload) и ответ (Response/Preview)
  - Timing: TTFB/Download (для медленных запросов)
- **Copy as cURL / Copy as fetch**: прикладывай в баг — это ускоряет расследование

---

## 🧱 Elements: быстрое тестирование UI без правок кода

- **:hov**: принудительно включай `:hover/:active/:focus` — удобно тестировать состояния
- **.cls**: добавляй/снимай классы — проверяй UI-режимы (active/disabled/error)
- **Computed → Box model**: ищи причину «съехало/обрезалось» (margin/padding/width)
- **Edit as HTML**: быстро проверь гипотезу (например, aria-атрибут, текст, порядок блоков)

---

## 💻 Console: как ловить ошибки стабильно

- Перед воспроизведением: **очисти консоль** и обнови страницу
- Фильтры: **Errors** и **Warnings** — базово для smoke/regression
- Ошибка в консоли = не всегда баг, но **всегда сигнал** проверить влияние на сценарий

---

## 📦 Application: хранение данных и безопасность

- Проверь, **где лежит токен** (cookies vs localStorage)
- Для cookies сессии/токенов проверь флаги:
  - **HttpOnly** (защита от XSS)
  - **Secure** (только HTTPS)
  - **SameSite** (защита от CSRF)
- После logout: **данные должны очищаться** (cookies/storage/sessionStorage)
- Никогда не должно быть **паролей/PII в открытом виде** в storage

---

## 📱 Device Mode: типовые ошибки адаптива

- Горизонтальный скролл (если не задуман)
- Мелкий текст, элементы «слипаются»
- Кликабельные зоны меньше **~44×44px**
- Меню/модалки/дропдауны не открываются на touch
- «Прыгает» верстка при смене ориентации

---

## 📝 Что прикладывать к баг-репорту из DevTools

- **Network**: Request URL, метод, статус, request/response headers, payload/response, timing
- **Console**: ошибка + stack trace (если есть)
- **Screenshots/Video**: особенно для UI/адаптива
- **HAR** (если нужно): Export HAR (при сложных сетевых проблемах)

---

[← Вернуться к содержанию раздела](./README.md)

