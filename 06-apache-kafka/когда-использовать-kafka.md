# 🎯 Когда использовать Kafka

> Применение Kafka.

---

## 📋 Когда использовать Kafka

**Основные сценарии:**

1. **Event Sourcing** — хранение событий системы
2. **Микросервисы** — асинхронная коммуникация
3. **Потоковая обработка** — обработка данных в реальном времени
4. **Логи и метрики** — централизованный сбор
5. **CDC (Change Data Capture)** — отслеживание изменений в БД

---

## 1. 📝 Event Sourcing

**Что это:** Хранение всех событий системы как источника истины

**Применение:**
- Хранение всех событий системы
- Восстановление состояния из событий
- Аудит и история изменений

**Преимущества:**
- Полная история изменений
- Возможность восстановления состояния
- Аудит всех операций

**Пример:**
```
События: OrderCreated → OrderPaid → OrderShipped
Состояние: Восстанавливается из событий
```

---

## 2. 🔗 Микросервисы

**Что это:** Асинхронная коммуникация между сервисами

**Применение:**
- Асинхронная коммуникация
- Отказоустойчивость
- Масштабирование

**Преимущества:**
- Слабая связанность сервисов
- Высокая отказоустойчивость
- Независимое масштабирование

**Пример:**
```
Order Service → Kafka → Notification Service
                ↓
            Analytics Service
```

**Паттерны:**
- Event-driven architecture
- Pub/Sub
- Saga pattern

---

## 3. ⚡ Потоковая обработка (Stream Processing)

**Что это:** Обработка данных в реальном времени

**Применение:**
- Обработка данных в реальном времени
- Агрегация, фильтрация
- Kafka Streams, Apache Flink

**Преимущества:**
- Низкая задержка обработки
- Высокая пропускная способность
- Обработка больших объемов данных

**Примеры:**
- Аналитика в реальном времени
- Обнаружение аномалий
- Агрегация метрик

**Инструменты:**
- **Kafka Streams** — библиотека для потоковой обработки
- **Apache Flink** — фреймворк для потоковой обработки
- **Apache Spark Streaming** — потоковая обработка

---

## 4. 📊 Логи и метрики

**Что это:** Централизованный сбор логов и метрик

**Применение:**
- Сбор логов со всех сервисов
- Централизованное хранение
- Анализ в реальном времени

**Преимущества:**
- Единая точка сбора
- Масштабируемость
- Анализ в реальном времени

**Пример:**
```
Service 1 → Kafka → ELK Stack
Service 2 → Kafka → Grafana
Service 3 → Kafka → Monitoring
```

**Инструменты:**
- **ELK Stack** (Elasticsearch, Logstash, Kibana)
- **Grafana** — визуализация метрик
- **Prometheus** — сбор метрик

---

## 5. 🔄 CDC (Change Data Capture)

**Что это:** Отслеживание изменений в базе данных

**Применение:**
- Отслеживание изменений в БД
- Репликация данных
- Синхронизация систем

**Преимущества:**
- Автоматическое отслеживание изменений
- Репликация в реальном времени
- Синхронизация между системами

**Пример:**
```
PostgreSQL → Debezium → Kafka → Elasticsearch
```

**Инструменты:**
- **Debezium** — CDC для различных БД
- **Maxwell** — CDC для MySQL
- **Canal** — CDC для MySQL

---

## 📋 Примеры использования

### 🛒 E-commerce

**Сценарий:** Обработка заказов

**Поток:**
```
Заказ создан → Kafka → Уведомление клиенту
                ↓
            Обновление склада
                ↓
            Аналитика продаж
```

**Преимущества:**
- Асинхронная обработка
- Независимость сервисов
- Масштабируемость

**События:**
- `order.created`
- `order.paid`
- `order.shipped`
- `order.delivered`

---

### 💰 Финансы

**Сценарий:** Обработка транзакций

**Поток:**
```
Транзакция → Kafka → Fraud Detection
              ↓
          Аналитика
              ↓
          Отчетность
```

**Требования:**
- Exactly-once гарантии
- Высокая надежность
- Аудит всех операций

**Преимущества:**
- Надежность доставки
- Аудит транзакций
- Обработка в реальном времени

---

### 📱 IoT

**Сценарий:** Обработка данных с устройств

**Поток:**
```
Датчики → Kafka → Обработка данных
            ↓
        Хранение
            ↓
        Аналитика
```

**Требования:**
- Высокая пропускная способность
- Обработка больших объемов
- Масштабируемость

**Преимущества:**
- Обработка миллионов сообщений
- Низкая задержка
- Надежное хранение

**Примеры:**
- Данные с датчиков
- Телеметрия устройств
- Логи устройств

---

## ❌ Когда НЕ использовать Kafka

### 1. Простые задачи

**Проблема:** Kafka сложен для простых случаев

**Когда не использовать:**
- Простая отправка сообщений
- Нет требований к масштабированию
- Низкая нагрузка

**Альтернативы:**
- **REST API** — для синхронных запросов
- **Очереди** (RabbitMQ, SQS) — для простых задач

**Пример:**
```
❌ Отправка одного email
✅ Используй REST API или простую очередь
```

---

### 2. Низкая задержка критична

**Проблема:** Kafka не для real-time (<1ms)

**Когда не использовать:**
- Требуется задержка <1ms
- Real-time коммуникация
- Интерактивные приложения

**Альтернативы:**
- **Redis Pub/Sub** — низкая задержка
- **WebSockets** — для real-time коммуникации
- **gRPC** — для быстрых запросов

**Пример:**
```
❌ Игра в реальном времени
✅ Используй Redis Pub/Sub или WebSockets
```

---

### 3. Маленькие объемы данных

**Проблема:** Overhead не оправдан

**Когда не использовать:**
- Небольшое количество сообщений
- Нет требований к масштабированию
- Простые задачи

**Альтернативы:**
- **RabbitMQ** — для небольших объемов
- **SQS** — для простых задач
- **REST API** — для синхронных запросов

**Пример:**
```
❌ 100 сообщений в день
✅ Используй RabbitMQ или SQS
```

---

### 4. Request-Response паттерн

**Проблема:** Kafka для событий, не для запросов

**Когда не использовать:**
- Нужен ответ на запрос
- Синхронная коммуникация
- Request-response паттерн

**Альтернативы:**
- **REST API** — для HTTP запросов
- **gRPC** — для RPC запросов
- **GraphQL** — для гибких запросов

**Пример:**
```
❌ Получить данные пользователя (нужен ответ)
✅ Используй REST API или gRPC
```

---

## 💡 Ключевые моменты

> **📌 Запомните:**

- **Kafka** — для событий и потоков
- **Отлично для микросервисов** — асинхронная коммуникация
- **Не для request-response** — используй REST или gRPC
- **Не для ultra-low latency** — используй Redis Pub/Sub

---

## 📊 Сравнение с альтернативами

### Kafka vs RabbitMQ

| Критерий | Kafka | RabbitMQ |
|----------|-------|----------|
| Пропускная способность | Очень высокая | Средняя |
| Задержка | Средняя | Низкая |
| Хранение | Да | Нет |
| Масштабирование | Отличное | Хорошее |
| Сложность | Высокая | Средняя |

**Когда использовать:**
- **Kafka** — большие объемы, потоковая обработка
- **RabbitMQ** — простые задачи, низкая задержка

---

### Kafka vs Redis Pub/Sub

| Критерий | Kafka | Redis Pub/Sub |
|----------|-------|---------------|
| Задержка | Средняя | Очень низкая |
| Хранение | Да | Нет |
| Пропускная способность | Очень высокая | Высокая |
| Сложность | Высокая | Низкая |

**Когда использовать:**
- **Kafka** — события, потоковая обработка
- **Redis Pub/Sub** — real-time коммуникация

---

## 🎯 Резюме

### ✅ Используй Kafka когда:

1. **Большие объемы данных** — миллионы сообщений в секунду
2. **Event-driven архитектура** — события и потоки данных
3. **Микросервисы** — асинхронная коммуникация
4. **Потоковая обработка** — обработка в реальном времени
5. **Нужно хранение** — история событий
6. **Масштабирование** — горизонтальное масштабирование

### ❌ НЕ используй Kafka когда:

1. **Простые задачи** — используй REST или очереди
2. **Низкая задержка критична** — используй Redis Pub/Sub
3. **Маленькие объемы** — используй RabbitMQ или SQS
4. **Request-response** — используй REST или gRPC

---

## 📚 Дополнительные ресурсы

### Когда выбрать Kafka:
- Event-driven архитектура
- Потоковая обработка данных
- Микросервисы
- Большие объемы данных

### Альтернативы:
- **REST API** — синхронные запросы
- **RabbitMQ** — простые очереди
- **Redis Pub/Sub** — real-time коммуникация
- **gRPC** — RPC запросы

---

[← Вернуться к содержанию раздела](./README.md)
