# 🧪 Тестирование Kafka

> Как тестировать интеграцию с Kafka.

---

## 📤 1. PRODUCER (отправка сообщений)

**Что проверять:**

### ✓ Сообщение отправлено в правильный топик

**Проверка:**
- Сообщение попало в нужный топик
- Топик существует и доступен
- Нет ошибок при отправке

**Как проверить:**
- Использовать инструменты для просмотра сообщений (Kafka Tool, kafkacat)
- Проверить логи Producer
- Проверить метрики Kafka

---

### ✓ Ключ сообщения корректный (для партиционирования)

**Проверка:**
- Ключ сообщения присутствует (если нужен)
- Ключ определяет правильную партицию
- Сообщения с одинаковым ключом попадают в одну партицию

**Как проверить:**
- Отправить несколько сообщений с одинаковым ключом
- Проверить, что они в одной партиции
- Проверить распределение по партициям

---

### ✓ Формат сообщения (JSON, Avro, Protobuf)

**Проверка:**
- Формат сообщения соответствует ожидаемому
- Структура данных корректна
- Кодировка правильная

**Форматы:**
- **JSON** — текстовый формат
- **Avro** — бинарный формат с схемой
- **Protobuf** — бинарный формат Google

**Как проверить:**
- Просмотреть сообщение в топике
- Проверить парсинг сообщения
- Валидировать структуру данных

---

### ✓ Обязательные поля присутствуют

**Проверка:**
- Все обязательные поля заполнены
- Типы данных корректны
- Значения валидны

**Пример:**
```json
{
  "orderId": 123,        // обязательное
  "userId": 456,        // обязательное
  "amount": 100.00,     // обязательное
  "timestamp": "2024-01-15T10:00:00Z"  // обязательное
}
```

**Как проверить:**
- Валидация схемы сообщения
- Проверка обязательных полей
- Тестирование с отсутствующими полями

---

### ✓ Обработка ошибок отправки

**Проверка:**
- Ошибки обрабатываются корректно
- Есть retry механизм
- Логирование ошибок
- Dead letter queue (если используется)

**Типы ошибок:**
- Топик не существует
- Брокер недоступен
- Ошибка сериализации
- Превышен лимит размера сообщения

---

### 📋 Пример проверки Producer:

**Сценарий:** Создать заказ

**Шаги:**
1. Создать заказ через API
2. Проверить, что в топик `orders.created` пришло сообщение
3. Проверить структуру: `{orderId, userId, amount, timestamp}`

**Проверки:**
- ✅ Сообщение в правильном топике
- ✅ Все поля присутствуют
- ✅ Формат JSON корректен
- ✅ Ключ сообщения = `orderId`
- ✅ Timestamp присутствует

---

## 📥 2. CONSUMER (чтение сообщений)

**Что проверять:**

### ✓ Сообщение прочитано из топика

**Проверка:**
- Consumer читает сообщения из топика
- Сообщения обрабатываются
- Нет ошибок при чтении

**Как проверить:**
- Проверить логи Consumer
- Проверить метрики обработки
- Проверить offset консьюмера

---

### ✓ Обработка корректна

**Проверка:**
- Сообщение обработано правильно
- Бизнес-логика выполняется корректно
- Данные сохранены в БД (если нужно)

**Как проверить:**
- Проверить результат обработки
- Проверить изменения в БД
- Проверить вызовы внешних сервисов

---

### ✓ Offset сохранен после обработки

**Проверка:**
- Offset коммитится после успешной обработки
- Offset не коммитится при ошибке (если at-least-once)
- Offset отслеживается корректно

**Как проверить:**
- Проверить offset консьюмера
- Проверить логи коммита offset
- Проверить поведение при ошибке

---

### ✓ Обработка ошибок (retry, dead letter queue)

**Проверка:**
- Ошибки обрабатываются корректно
- Есть retry механизм
- Dead letter queue работает (если используется)
- Логирование ошибок

**Типы ошибок:**
- Ошибка десериализации
- Ошибка обработки сообщения
- Ошибка сохранения в БД
- Внешний сервис недоступен

---

### ✓ Идемпотентность (повторная обработка безопасна)

**Проверка:**
- Повторная обработка сообщения безопасна
- Дубликаты не создаются
- Результат одинаковый при повторе

**Как проверить:**
- Отправить одно сообщение дважды
- Проверить, что дубликат не создан
- Проверить, что результат одинаковый

---

### 📋 Пример проверки Consumer:

**Сценарий:** Обработка создания заказа

**Шаги:**
1. Отправить сообщение в топик `orders.created`
2. Проверить, что консьюмер обработал
3. Проверить, что заказ создан в БД
4. Отправить то же сообщение повторно
5. Проверить, что дубликат не создан (идемпотентность)

**Проверки:**
- ✅ Сообщение прочитано из топика
- ✅ Заказ создан в БД
- ✅ Offset сохранен после обработки
- ✅ Повторная обработка не создает дубликат
- ✅ Ошибки обрабатываются корректно

---

## 🔀 3. ПАРТИЦИИ И ПОРЯДОК

**Что проверять:**

### ✓ Сообщения с одним ключом в одной партиции

**Проверка:**
- Сообщения с одинаковым ключом попадают в одну партицию
- Распределение по партициям корректно
- Ключ определяет партицию правильно

**Пример:**
```
Отправить 10 сообщений с key="user_123"
Проверить, что все в одной партиции
```

**Как проверить:**
- Отправить несколько сообщений с одинаковым ключом
- Проверить партицию для каждого сообщения
- Убедиться, что все в одной партиции

---

### ✓ Порядок сохранен в партиции

**Проверка:**
- Сообщения в партиции упорядочены
- Порядок соответствует порядку отправки
- Между партициями порядок не гарантирован

**Пример:**
```
Отправить сообщения: msg1, msg2, msg3 с key="user_123"
Проверить, что порядок сохранен в партиции
```

**Как проверить:**
- Отправить последовательность сообщений с одним ключом
- Проверить порядок чтения консьюмером
- Убедиться, что порядок соответствует отправке

---

### ✓ Балансировка по партициям

**Проверка:**
- Сообщения распределяются по партициям равномерно
- Нет перегрузки одной партиции
- Балансировка работает корректно

**Как проверить:**
- Отправить много сообщений без ключа
- Проверить распределение по партициям
- Убедиться, что распределение равномерное

---

## 🛡️ 4. ОТКАЗОУСТОЙЧИВОСТЬ

**Что проверять:**

### ✓ Консьюмер упал → другой подхватил

**Проверка:**
- При падении консьюмера его партиции перераспределяются
- Другие консьюмеры продолжают работать
- Rebalancing происходит автоматически

**Как проверить:**
- Запустить несколько консьюмеров в группе
- Остановить одного консьюмера
- Проверить, что партиции перераспределены
- Проверить, что обработка продолжается

---

### ✓ Брокер упал → реплика стала лидером

**Проверка:**
- При падении брокера реплика становится лидером
- Обработка продолжается без потери данных
- Автоматическое восстановление работает

**Как проверить:**
- Остановить брокер с leader партицией
- Проверить, что реплика стала лидером
- Проверить, что обработка продолжается
- Проверить, что данные не потеряны

---

### ✓ Сообщения не потеряны

**Проверка:**
- При сбоях сообщения не теряются
- Репликация работает корректно
- Offset отслеживается правильно

**Как проверить:**
- Отправить сообщения
- Имитировать сбой
- Проверить, что все сообщения обработаны
- Проверить отсутствие потерь

---

## ⚡ 5. ПРОИЗВОДИТЕЛЬНОСТЬ

**Что проверять:**

### ✓ Пропускная способность (messages/sec)

**Проверка:**
- Количество сообщений в секунду соответствует требованиям
- Производительность достаточна для нагрузки
- Нет узких мест

**Метрики:**
- Сообщений в секунду (throughput)
- Задержка обработки (latency)
- Использование ресурсов

**Как проверить:**
- Нагрузочное тестирование
- Мониторинг метрик Kafka
- Анализ производительности

---

### ✓ Задержка (latency)

**Проверка:**
- Время от отправки до обработки приемлемо
- Задержка соответствует требованиям
- Нет длительных задержек

**Метрики:**
- Producer latency
- Consumer latency
- End-to-end latency

**Как проверить:**
- Измерение времени обработки
- Мониторинг метрик задержки
- Анализ узких мест

---

### ✓ Размер сообщений

**Проверка:**
- Размер сообщений в пределах лимитов
- Большие сообщения обрабатываются корректно
- Нет проблем с сериализацией

**Лимиты:**
- Максимальный размер сообщения (обычно 1MB)
- Рекомендуемый размер (меньше 100KB)

**Как проверить:**
- Отправка сообщений разного размера
- Проверка обработки больших сообщений
- Проверка ошибок при превышении лимита

---

### ✓ Lag консьюмера (отставание)

**Проверка:**
- Consumer lag в пределах нормы
- Нет длительного отставания
- Обработка успевает за поступлением сообщений

**Метрики:**
- Consumer lag (отставание offset)
- Time to process (время обработки)
- Throughput (пропускная способность)

**Как проверить:**
- Мониторинг consumer lag
- Анализ причин отставания
- Оптимизация обработки

---

## 💡 Ключевые моменты

> **📌 Запомните:**

- **Проверяй отправку в правильный топик** — сообщения должны попадать в нужный топик
- **Проверяй обработку и сохранение offset** — обработка должна быть корректной, offset сохраняться
- **Тестируй идемпотентность** — повторная обработка должна быть безопасной
- **Проверяй порядок в партиции** — порядок сообщений должен сохраняться в рамках партиции

---

## 📋 Чек-лист тестирования Kafka:

### Producer:
- [ ] Сообщение отправлено в правильный топик
- [ ] Ключ сообщения корректный
- [ ] Формат сообщения правильный
- [ ] Обязательные поля присутствуют
- [ ] Ошибки обрабатываются корректно

### Consumer:
- [ ] Сообщение прочитано из топика
- [ ] Обработка корректна
- [ ] Offset сохранен после обработки
- [ ] Ошибки обрабатываются (retry, DLQ)
- [ ] Идемпотентность работает

### Партиции и порядок:
- [ ] Сообщения с одним ключом в одной партиции
- [ ] Порядок сохранен в партиции
- [ ] Балансировка по партициям работает

### Отказоустойчивость:
- [ ] Консьюмер упал → другой подхватил
- [ ] Брокер упал → реплика стала лидером
- [ ] Сообщения не потеряны

### Производительность:
- [ ] Пропускная способность достаточна
- [ ] Задержка приемлема
- [ ] Размер сообщений в пределах лимитов
- [ ] Consumer lag в норме

---

## 🎯 Best Practices:

### ✅ Рекомендации:

1. **Тестируйте end-to-end** — от Producer до Consumer
2. **Проверяйте идемпотентность** — критично для at-least-once
3. **Тестируйте отказоустойчивость** — имитация сбоев
4. **Мониторьте метрики** — lag, throughput, latency
5. **Используйте тестовые данные** — изолированные от production
6. **Проверяйте порядок сообщений** — в рамках партиции
7. **Тестируйте обработку ошибок** — retry, DLQ, логирование

---

## 🔍 Инструменты для тестирования:

### Просмотр сообщений:
- **Kafka Tool** — GUI для просмотра топиков
- **kafkacat** — CLI инструмент
- **Kafka Console Consumer** — встроенный инструмент

### Мониторинг:
- **Kafka Manager** — управление и мониторинг
- **Prometheus + Grafana** — метрики и визуализация
- **JMX метрики** — встроенные метрики Kafka

### Тестирование:
- **kafka-producer-perf-test** — нагрузочное тестирование Producer
- **kafka-consumer-perf-test** — нагрузочное тестирование Consumer
- **Custom тесты** — интеграционные тесты

---

[← Вернуться к содержанию раздела](./README.md)
