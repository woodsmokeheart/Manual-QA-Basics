# ✅ Гарантии доставки

> At-most-once, At-least-once, Exactly-once.

---

## 📊 Типы гарантий доставки:

Kafka поддерживает три типа гарантий доставки сообщений:

1. **At-most-once** — максимум один раз
2. **At-least-once** — минимум один раз
3. **Exactly-once** — ровно один раз

---

## 1. ⚡ At-most-once (максимум один раз)

**Характеристики:**
- Сообщение может быть потеряно
- Не будет дубликатов
- Самый быстрый режим

**Настройки Producer:**
```
acks=0  // не ждет подтверждения
```

**Настройки Consumer:**
```
auto-commit offset до обработки
```

---

### Пример сценария:

**Процесс:**
1. Consumer читает сообщение
2. Сохраняет offset (auto-commit)
3. Обрабатывает сообщение
4. Если упал на шаге 3 → сообщение потеряно

**Результат:**
- Offset уже сохранен
- Сообщение не будет прочитано повторно
- Данные потеряны

---

### Когда использовать:

**Подходит для:**
- ✅ Метрики
- ✅ Логи
- ✅ Данные, потеря которых не критична
- ✅ Высокая производительность важнее надежности

**Не подходит для:**
- ❌ Финансовые операции
- ❌ Критичные данные
- ❌ Когда потеря данных недопустима

---

## 2. ✅ At-least-once (минимум один раз)

**Характеристики:**
- Сообщение гарантированно доставлено
- Могут быть дубликаты
- Стандартный режим

**Настройки Producer:**
```
acks=all  // ждет подтверждения от всех реплик
```

**Настройки Consumer:**
```
commit offset после обработки
```

---

### Пример сценария:

**Процесс:**
1. Consumer читает сообщение
2. Обрабатывает сообщение
3. Сохраняет offset (commit)
4. Если упал на шаге 3 → сообщение обработается повторно

**Результат:**
- Сообщение гарантированно обработано
- Возможны дубликаты при сбоях
- Нужна идемпотентная обработка

---

### Когда использовать:

**Подходит для:**
- ✅ Большинство случаев
- ✅ Обработка идемпотентна
- ✅ Дубликаты допустимы или обрабатываются
- ✅ Баланс между надежностью и производительностью

**Требования:**
- Обработка должна быть идемпотентной
- Защита от дубликатов на уровне приложения

---

## 3. 🎯 Exactly-once (ровно один раз)

**Характеристики:**
- Сообщение доставлено ровно один раз
- Нет потерь и дубликатов
- Самый медленный режим

**Настройки Producer:**
```
enable.idempotence=true
```

**Настройки Consumer:**
```
Транзакции
```

---

### Пример сценария:

**Процесс:**
1. Producer отправляет сообщение с idempotence
2. Kafka гарантирует уникальность
3. Consumer обрабатывает в транзакции
4. Commit offset и обработка атомарно

**Результат:**
- Сообщение обработано ровно один раз
- Нет потерь и дубликатов
- Высокая надежность

---

### Когда использовать:

**Подходит для:**
- ✅ Финансовые операции
- ✅ Критичные данные
- ✅ Обработка не идемпотентна
- ✅ Когда дубликаты недопустимы

**Ограничения:**
- Медленнее других режимов
- Требует транзакций
- Более сложная настройка

---

## 🔄 Идемпотентность

**Определение:** Свойство операции, при котором повторное выполнение дает тот же результат.

---

### Идемпотентная операция:

**Характеристики:**
- Повторное выполнение = тот же результат
- Безопасно выполнять несколько раз
- Не изменяет состояние при повторе

**Пример:**
```sql
SET balance = 100  -- всегда результат 100
UPDATE users SET status = 'active' WHERE id = 123  -- идемпотентно
```

**Преимущества:**
- Безопасно при дубликатах
- Можно использовать at-least-once
- Проще реализация

---

### Не идемпотентная операция:

**Характеристики:**
- Повторное выполнение = другой результат
- Изменяет состояние при каждом выполнении
- Опасно при дубликатах

**Пример:**
```sql
balance = balance + 100  -- каждый раз добавляет 100
INSERT INTO orders ...  -- создает новую запись
```

**Проблемы:**
- Дубликаты приводят к ошибкам
- Нужна защита от повторной обработки
- Требуется exactly-once или идемпотентность на уровне приложения

---

## 💡 Рекомендация:

**Стандартный подход:**
1. Используй **at-least-once**
2. Делай обработку **идемпотентной**
3. **Exactly-once** только если критично

**Почему:**
- At-least-once проще и быстрее
- Идемпотентность решает проблему дубликатов
- Exactly-once сложнее и медленнее

---

## 📊 Сравнение гарантий:

| Характеристика | At-most-once | At-least-once | Exactly-once |
|----------------|--------------|---------------|--------------|
| **Потеря данных** | Возможна | Нет | Нет |
| **Дубликаты** | Нет | Возможны | Нет |
| **Производительность** | Высокая | Средняя | Низкая |
| **Сложность** | Низкая | Средняя | Высокая |
| **Использование** | Метрики, логи | Большинство случаев | Финансы, критика |

---

## 🎯 Примеры идемпотентной обработки:

### Пример 1: Обновление статуса

**Не идемпотентно:**
```javascript
// Плохо: каждый раз увеличивает счетчик
order.count += 1;
```

**Идемпотентно:**
```javascript
// Хорошо: всегда устанавливает конкретное значение
order.status = 'processed';
order.processedAt = timestamp;
```

---

### Пример 2: Создание записи

**Не идемпотентно:**
```javascript
// Плохо: создает дубликаты
db.insert({ orderId: 123, status: 'created' });
```

**Идемпотентно:**
```javascript
// Хорошо: проверяет существование или использует уникальный ключ
db.upsert({ 
  orderId: 123,  // уникальный ключ
  status: 'created' 
});
```

---

### Пример 3: Отправка уведомления

**Не идемпотентно:**
```javascript
// Плохо: отправляет несколько раз
sendEmail(userId, message);
```

**Идемпотентно:**
```javascript
// Хорошо: проверяет, было ли уже отправлено
if (!isNotificationSent(orderId, 'email')) {
  sendEmail(userId, message);
  markNotificationSent(orderId, 'email');
}
```

---

## 💡 Ключевые моменты

> **📌 Запомните:**

- **At-most-once - может потеряться** — самый быстрый, но данные могут быть потеряны
- **At-least-once - могут быть дубликаты** — стандартный режим, нужна идемпотентность
- **Exactly-once - ровно один раз (медленно)** — самый надежный, но медленный
- **Делай обработку идемпотентной** — это решает проблему дубликатов

---

## 📋 Чек-лист выбора гарантии:

### At-most-once:
- [ ] Потеря данных допустима
- [ ] Высокая производительность важнее надежности
- [ ] Метрики или логи
- [ ] Дубликаты недопустимы

### At-least-once:
- [ ] Потеря данных недопустима
- [ ] Дубликаты допустимы или обрабатываются
- [ ] Обработка идемпотентна
- [ ] Баланс между надежностью и производительностью

### Exactly-once:
- [ ] Потеря данных недопустима
- [ ] Дубликаты недопустимы
- [ ] Финансовые операции
- [ ] Обработка не идемпотентна

---

## 🎯 Best Practices:

### ✅ Рекомендации:

1. **Используйте at-least-once по умолчанию** — баланс надежности и производительности
2. **Делайте обработку идемпотентной** — защита от дубликатов
3. **Exactly-once только когда критично** — сложнее и медленнее
4. **Проверяйте идемпотентность операций** — перед выбором гарантии
5. **Документируйте выбор гарантии** — для понимания командой
6. **Тестируйте обработку дубликатов** — при at-least-once
7. **Мониторьте потери и дубликаты** — для оптимизации

---

## 🔍 Что проверять при тестировании:

- ✅ Настройки Producer корректны (acks)
- ✅ Настройки Consumer корректны (commit offset)
- ✅ Обработка идемпотентна (если at-least-once)
- ✅ Нет потери данных (если не at-most-once)
- ✅ Дубликаты обрабатываются корректно
- ✅ Транзакции работают (если exactly-once)

---

[← Вернуться к содержанию раздела](./README.md)
