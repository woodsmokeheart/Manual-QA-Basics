# 📖 Что такое Kafka

> Основы Apache Kafka.

---

## 🚀 Apache Kafka

**Определение:** Распределенная платформа потоковой передачи данных.

**История:**
- Разработана LinkedIn
- Открыта в 2011 году
- Стала стандартом для event-driven архитектур

**Назначение:** Обработка потоков данных в реальном времени.

---

## ⭐ Ключевые особенности:

### 1. Высокая пропускная способность

**Характеристики:**
- Миллионы сообщений в секунду
- Низкая задержка (latency)
- Эффективная обработка больших объемов данных

**Пример:**
- Обработка логов приложений
- Стриминг событий пользователей
- Обработка метрик и мониторинга

---

### 2. Горизонтальное масштабирование

**Характеристики:**
- Добавляй серверы для увеличения производительности
- Масштабируется линейно
- Легко расширяется при росте нагрузки

**Пример:**
- Начало: 3 брокера
- Рост нагрузки: добавить еще 3 брокера
- Результат: удвоенная пропускная способность

---

### 3. Хранение сообщений

**Характеристики:**
- Сообщения не удаляются сразу после чтения
- Хранятся определенное время (retention period)
- Можно перечитать сообщения

**Преимущества:**
- Восстановление после сбоев
- Повторная обработка данных
- Анализ исторических данных

---

### 4. Гарантии доставки

**Типы гарантий:**
- **At-least-once** — сообщение доставлено минимум один раз
- **Exactly-once** — сообщение доставлено ровно один раз
- **At-most-once** — сообщение доставлено максимум один раз

**Использование:**
- Настройка в зависимости от требований
- Баланс между производительностью и надежностью

---

### 5. Распределенная система

**Характеристики:**
- Отказоустойчивость
- Репликация данных
- Автоматическое восстановление

**Преимущества:**
- Высокая доступность
- Защита от потери данных
- Работа при отказе части серверов

---

## 🔑 Основные концепции:

### Producer (Продюсер)

**Определение:** Отправляет сообщения в Kafka.

**Функции:**
- Создание сообщений
- Отправка в топики
- Выбор партиции (если нужно)

**Пример:**
```javascript
// Сервис заказов отправляет событие "заказ создан"
producer.send({
  topic: 'orders',
  messages: [{
    key: 'order-123',
    value: JSON.stringify({
      orderId: 123,
      userId: 456,
      amount: 100.00,
      status: 'created'
    })
  }]
});
```

**Использование:**
- Сервисы приложений
- Микросервисы
- Системы сбора данных

---

### Topic (Топик)

**Определение:** Категория/канал для сообщений.

**Характеристики:**
- Логическая категория сообщений
- Может иметь несколько партиций
- Несколько продюсеров могут писать в один топик
- Несколько консьюмеров могут читать из одного топика

**Примеры:**
- `orders` — события заказов
- `payments` — события платежей
- `notifications` — события уведомлений
- `user-events` — события пользователей

**Использование:**
- Организация сообщений по типам
- Разделение данных по доменам
- Управление потоками событий

---

### Partition (Партиция)

**Определение:** Топик разделен на партиции для параллелизма.

**Характеристики:**
- Сообщения в партиции упорядочены
- Параллельная обработка
- Масштабирование производительности

**Особенности:**
- Каждая партиция хранится на разных брокерах
- Порядок сообщений гарантирован только в рамках партиции
- Ключ сообщения определяет партицию (если указан)

**Пример:**
```
Topic: orders
├── Partition 0
├── Partition 1
└── Partition 2
```

**Преимущества:**
- Параллельная обработка
- Масштабирование
- Распределение нагрузки

---

### Consumer (Консьюмер)

**Определение:** Читает сообщения из Kafka.

**Функции:**
- Подписка на топики
- Чтение сообщений
- Обработка данных

**Пример:**
```javascript
// Сервис уведомлений читает события
consumer.subscribe({ topics: ['orders'] });

consumer.run({
  eachMessage: async ({ topic, partition, message }) => {
    const order = JSON.parse(message.value.toString());
    // Отправить уведомление пользователю
    sendNotification(order.userId, `Order ${order.orderId} created`);
  }
});
```

**Использование:**
- Обработка событий
- Агрегация данных
- Интеграция с другими системами

---

### Consumer Group (Группа консьюмеров)

**Определение:** Несколько консьюмеров работают вместе.

**Характеристики:**
- Каждая партиция читается только одним консьюмером из группы
- Распределение нагрузки между консьюмерами
- Масштабирование обработки

**Пример:**
```
Consumer Group: notification-service
├── Consumer 1 → читает Partition 0
├── Consumer 2 → читает Partition 1
└── Consumer 3 → читает Partition 2
```

**Преимущества:**
- Параллельная обработка
- Масштабирование
- Отказоустойчивость

---

### Broker (Брокер)

**Определение:** Сервер Kafka.

**Функции:**
- Хранит данные
- Обслуживает запросы продюсеров и консьюмеров
- Управляет партициями и репликацией

**Характеристики:**
- Обычно несколько брокеров в кластере
- Репликация данных между брокерами
- Автоматическое восстановление при сбоях

**Пример:**
```
Kafka Cluster:
├── Broker 1
├── Broker 2
└── Broker 3
```

---

## 🔄 Как работает:

**Схема работы:**
```
Producer → Topic (Partition 0, 1, 2) → Consumer Group
                                         ├── Consumer 1 (Partition 0)
                                         ├── Consumer 2 (Partition 1)
                                         └── Consumer 3 (Partition 2)
```

**Процесс:**
1. **Producer** отправляет сообщения в **Topic**
2. Сообщения распределяются по **Partitions**
3. **Consumer Group** читает сообщения из партиций
4. Каждый **Consumer** в группе читает свою партицию

**Особенности:**
- Сообщения упорядочены в рамках партиции
- Параллельная обработка через несколько партиций
- Масштабирование через добавление консьюмеров

---

## 💡 Ключевые моменты

> **📌 Запомните:**

- **Kafka - платформа потоковой передачи** — для обработки потоков данных в реальном времени
- **Producer отправляет, Consumer читает** — базовая модель работы
- **Topic разделен на Partitions** — для параллелизма и масштабирования
- **Сообщения хранятся, не удаляются сразу** — можно перечитать и восстановить данные

---

## 🎯 Основные преимущества:

### Производительность:
- ✅ Высокая пропускная способность
- ✅ Низкая задержка
- ✅ Эффективная обработка больших объемов

### Масштабируемость:
- ✅ Горизонтальное масштабирование
- ✅ Легко добавлять брокеры и консьюмеры
- ✅ Линейный рост производительности

### Надежность:
- ✅ Отказоустойчивость
- ✅ Репликация данных
- ✅ Гарантии доставки

### Гибкость:
- ✅ Хранение сообщений
- ✅ Возможность перечитать данные
- ✅ Поддержка различных паттернов использования

---

## 📊 Сравнение с другими системами:

| Характеристика | Kafka | RabbitMQ | Redis Pub/Sub |
|----------------|-------|----------|---------------|
| **Тип** | Потоковая платформа | Message Broker | Pub/Sub система |
| **Хранение** | Да (настраиваемое) | Нет (после чтения удаляется) | Нет |
| **Пропускная способность** | Очень высокая | Средняя | Высокая |
| **Гарантии доставки** | Да | Да | Нет |
| **Масштабирование** | Горизонтальное | Вертикальное | Горизонтальное |

---

## 🔍 Когда использовать Kafka:

**Идеально для:**
- ✅ Event-driven архитектур
- ✅ Потоковой обработки данных
- ✅ Сбора логов и метрик
- ✅ Интеграции микросервисов
- ✅ Real-time аналитики

**Не подходит для:**
- ❌ Простых очередей задач
- ❌ Синхронных запросов-ответов
- ❌ Систем с низкой нагрузкой

---

[← Вернуться к содержанию раздела](./README.md)
