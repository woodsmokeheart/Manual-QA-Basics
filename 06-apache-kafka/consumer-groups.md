# 👥 Consumer Groups

> Группы консьюмеров и балансировка.

---

## 📋 Consumer Group – группа консьюмеров

**Определение:** Группа консьюмеров, работающих вместе для обработки сообщений из топика.

**Принцип:** Каждая партиция читается только одним консьюмером из группы.

---

## 🎯 Зачем нужны:

### 1. Параллельная обработка

**Механизм:**
- Разные консьюмеры читают разные партиции
- Увеличение пропускной способности
- Параллельная обработка сообщений

**Преимущества:**
- Быстрая обработка больших объемов
- Распределение нагрузки
- Масштабирование производительности

---

### 2. Отказоустойчивость

**Механизм:**
- При падении консьюмера его партиции перераспределяются
- Остальные консьюмеры продолжают работать
- Автоматическое восстановление

**Преимущества:**
- Высокая доступность
- Автоматическое восстановление
- Защита от потери данных

---

### 3. Масштабирование

**Механизм:**
- Можно добавлять консьюмеров для увеличения производительности
- Партиции автоматически перераспределяются
- Линейный рост производительности

**Преимущества:**
- Горизонтальное масштабирование
- Гибкость в настройке
- Оптимизация под нагрузку

---

## 📊 Пример 1: Один консьюмер

**Сценарий:**
```
Topic: orders (3 партиции)
Consumer Group: order-processors
├ Consumer 1 → Partition 0, 1, 2
```

**Результат:**
- Один консьюмер читает все партиции
- Последовательная обработка
- Нет параллелизма

**Использование:**
- Низкая нагрузка
- Простые сценарии
- Тестирование

---

## 📊 Пример 2: Три консьюмера

**Сценарий:**
```
Topic: orders (3 партиции)
Consumer Group: order-processors
├ Consumer 1 → Partition 0
├ Consumer 2 → Partition 1
└ Consumer 3 → Partition 2
```

**Результат:**
- Каждый консьюмер читает свою партицию
- Максимальный параллелизм
- Оптимальное распределение нагрузки

**Использование:**
- Высокая нагрузка
- Требуется параллелизм
- Production окружение

---

## 📊 Пример 3: Больше консьюмеров чем партиций

**Сценарий:**
```
Topic: orders (3 партиции)
Consumer Group: order-processors
├ Consumer 1 → Partition 0
├ Consumer 2 → Partition 1
├ Consumer 3 → Partition 2
└ Consumer 4 → (idle, ничего не читает)
```

**Результат:**
- Consumer 4 простаивает
- Нет смысла иметь больше консьюмеров чем партиций
- Лишние консьюмеры не используются

**Рекомендация:**
- Количество консьюмеров ≤ количество партиций
- Оптимально: количество консьюмеров = количество партиций

---

## 🔄 Rebalancing (Ребалансировка)

**Определение:** Процесс перераспределения партиций между консьюмерами в группе.

**Когда происходит:**
- Добавляется новый консьюмер
- Консьюмер падает
- Добавляются партиции
- Удаляются партиции

---

### Пример ребалансировки:

**До:**
```
Consumer 1 → Partition 0, 1, 2
```

**Действие:** Добавили Consumer 2

**После:**
```
Consumer 1 → Partition 0, 1
Consumer 2 → Partition 2
```

**Процесс:**
1. Kafka обнаруживает изменение в группе
2. Запускается rebalancing
3. Партиции перераспределяются
4. Консьюмеры получают новые назначения

---

### Особенности rebalancing:

**Временная остановка:**
- Во время rebalancing консьюмеры временно останавливаются
- Сообщения не обрабатываются
- После завершения обработка возобновляется

**Стратегии:**
- **Eager rebalancing** — все консьюмеры останавливаются
- **Incremental rebalancing** — только затронутые консьюмеры останавливаются

**Рекомендации:**
- Минимизируйте количество rebalancing
- Используйте стабильные консьюмеры
- Настройте session timeout правильно

---

## 🔀 Разные Consumer Groups

**Принцип:** Разные группы читают независимо.

**Пример:**
```
Topic: orders

Consumer Group: order-processors
├ Consumer 1 → читает все партиции

Consumer Group: analytics
├ Consumer 2 → читает все партиции
```

**Особенности:**
- Разные группы читают независимо
- Каждая группа имеет свой offset
- Одно сообщение может быть прочитано разными группами

**Использование:**
- Разные сервисы обрабатывают одни и те же события
- Аналитика и бизнес-логика разделены
- Множественная обработка сообщений

---

## 💡 Ключевые моменты

> **📌 Запомните:**

- **Consumer Group - группа для параллелизма** — несколько консьюмеров работают вместе
- **Одна партиция = один консьюмер в группе** — каждая партиция читается только одним консьюмером из группы
- **Rebalancing при добавлении/удалении** — автоматическое перераспределение партиций
- **Разные группы читают независимо** — каждая группа имеет свой offset и читает независимо

---

## 📊 Правила распределения партиций:

### Правило 1: Одна партиция = один консьюмер

**В рамках одной группы:**
- Каждая партиция читается только одним консьюмером
- Несколько консьюмеров не могут читать одну партицию одновременно

**Пример:**
```
Consumer Group: processors
├ Consumer 1 → Partition 0 ✅
├ Consumer 2 → Partition 0 ❌ (не может)
```

---

### Правило 2: Количество консьюмеров ≤ партиций

**Оптимально:**
- Количество консьюмеров = количество партиций
- Максимальный параллелизм
- Нет простаивающих консьюмеров

**Пример:**
```
3 партиции → 3 консьюмера ✅ (оптимально)
3 партиции → 2 консьюмера ✅ (работает)
3 партиции → 4 консьюмера ⚠️ (1 простаивает)
```

---

### Правило 3: Разные группы независимы

**Принцип:**
- Разные Consumer Groups читают независимо
- Каждая группа имеет свой offset
- Одно сообщение может быть прочитано разными группами

**Пример:**
```
Topic: orders

Group A: order-processors → читает все сообщения
Group B: analytics → читает те же сообщения независимо
```

---

## 🎯 Примеры использования:

### Пример 1: Масштабирование обработки

**Сценарий:**
- Начало: 1 консьюмер обрабатывает 3 партиции
- Рост нагрузки: добавить еще 2 консьюмера

**Результат:**
- 3 консьюмера обрабатывают по 1 партиции
- Утроенная производительность
- Автоматическое распределение

---

### Пример 2: Отказоустойчивость

**Сценарий:**
- 3 консьюмера обрабатывают по 1 партиции
- Consumer 2 падает

**Результат:**
- Rebalancing запускается автоматически
- Партиция 1 перераспределяется между Consumer 1 и 3
- Обработка продолжается без потери данных

---

### Пример 3: Разные сервисы

**Сценарий:**
```
Topic: user-events

Group: notification-service → отправляет уведомления
Group: analytics-service → собирает аналитику
Group: audit-service → ведет аудит
```

**Результат:**
- Каждый сервис читает все события независимо
- Разные offset для каждой группы
- Независимая обработка

---

## 📋 Best Practices:

### ✅ Рекомендации:

1. **Количество консьюмеров = количество партиций** — оптимальное распределение
2. **Используйте стабильные консьюмеры** — минимизация rebalancing
3. **Настройте session timeout** — правильное определение падения консьюмера
4. **Мониторьте lag** — отслеживание отставания консьюмеров
5. **Разделяйте группы по назначению** — разные сервисы в разных группах
6. **Тестируйте rebalancing** — проверка поведения при изменениях
7. **Используйте разные группы для разных целей** — независимая обработка

---

## 🔍 Что проверять при тестировании:

- ✅ Партиции распределены между консьюмерами корректно
- ✅ Каждая партиция читается только одним консьюмером
- ✅ Rebalancing работает при добавлении/удалении консьюмеров
- ✅ Разные группы читают независимо
- ✅ Offset отслеживается для каждой группы отдельно
- ✅ Нет простаивающих консьюмеров (если партиций достаточно)
- ✅ Обработка продолжается при падении консьюмера

---

## 📊 Сравнение сценариев:

| Сценарий | Консьюмеры | Партиции | Результат |
|----------|------------|----------|-----------|
| **1 консьюмер** | 1 | 3 | Все партиции читает один консьюмер |
| **Равное количество** | 3 | 3 | Каждый консьюмер читает свою партицию |
| **Больше консьюмеров** | 4 | 3 | 1 консьюмер простаивает |
| **Меньше консьюмеров** | 2 | 3 | 1 консьюмер читает 2 партиции |

---

[← Вернуться к содержанию раздела](./README.md)
