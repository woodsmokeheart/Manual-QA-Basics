# 🛠️ Инструменты для работы

> CLI и GUI инструменты.

---

## 📋 Инструменты для Kafka

**Категории:**
1. **Kafka CLI** — консольные команды
2. **Kafka UI** — графические интерфейсы
3. **Инструменты для тестирования** — Testcontainers, EmbeddedKafka

---

## 1. 💻 KAFKA CLI (консольные команды)

**Расположение:** Встроенные инструменты в дистрибутиве Kafka

**Путь:** `bin/kafka-*.sh` (или `bin/kafka-*.bat` для Windows)

---

### Создать топик

**Команда:**
```bash
kafka-topics.sh --create \
  --topic orders \
  --bootstrap-server localhost:9092 \
  --partitions 3 \
  --replication-factor 2
```

**Параметры:**
- `--topic` — название топика
- `--bootstrap-server` — адрес Kafka брокера
- `--partitions` — количество партиций
- `--replication-factor` — фактор репликации

**Результат:** Топик создан с указанными параметрами

---

### Список топиков

**Команда:**
```bash
kafka-topics.sh --list \
  --bootstrap-server localhost:9092
```

**Результат:** Список всех топиков в кластере

**Пример вывода:**
```
orders
payments
notifications
```

---

### Описание топика

**Команда:**
```bash
kafka-topics.sh --describe \
  --topic orders \
  --bootstrap-server localhost:9092
```

**Результат:** Детальная информация о топике

**Пример вывода:**
```
Topic: orders	PartitionCount: 3	ReplicationFactor: 2
	Partition: 0	Leader: 1	Replicas: 1,2	Isr: 1,2
	Partition: 1	Leader: 2	Replicas: 2,3	Isr: 2,3
	Partition: 2	Leader: 3	Replicas: 3,1	Isr: 3,1
```

**Информация:**
- Количество партиций
- Фактор репликации
- Leader для каждой партиции
- Replicas и ISR (In-Sync Replicas)

---

### Отправить сообщение

**Команда:**
```bash
kafka-console-producer.sh \
  --topic orders \
  --bootstrap-server localhost:9092
```

**Использование:**
- После запуска команды вводите сообщения
- Каждая строка = одно сообщение
- `Ctrl+C` для выхода

**Пример:**
```bash
> {"orderId": 1, "amount": 100}
> {"orderId": 2, "amount": 200}
```

**С ключом:**
```bash
kafka-console-producer.sh \
  --topic orders \
  --bootstrap-server localhost:9092 \
  --property "parse.key=true" \
  --property "key.separator=:"
```

**Пример:**
```bash
> user_123:{"orderId": 1, "amount": 100}
```

---

### Читать сообщения

**Команда:**
```bash
kafka-console-consumer.sh \
  --topic orders \
  --bootstrap-server localhost:9092 \
  --from-beginning
```

**Параметры:**
- `--from-beginning` — читать с начала топика
- Без параметра — читать только новые сообщения

**С Consumer Group:**
```bash
kafka-console-consumer.sh \
  --topic orders \
  --bootstrap-server localhost:9092 \
  --group order-processors \
  --from-beginning
```

**С ключом:**
```bash
kafka-console-consumer.sh \
  --topic orders \
  --bootstrap-server localhost:9092 \
  --property "print.key=true" \
  --property "key.separator=:" \
  --from-beginning
```

---

### Список consumer groups

**Команда:**
```bash
kafka-consumer-groups.sh --list \
  --bootstrap-server localhost:9092
```

**Результат:** Список всех consumer groups

**Пример вывода:**
```
order-processors
analytics-service
notification-service
```

---

### Lag консьюмера

**Команда:**
```bash
kafka-consumer-groups.sh --describe \
  --group order-processors \
  --bootstrap-server localhost:9092
```

**Результат:** Детальная информация о consumer group

**Пример вывода:**
```
GROUP           TOPIC           PARTITION  CURRENT-OFFSET  LAG
order-processors orders          0          100             5
order-processors orders          1          95              10
order-processors orders          2          98              2
```

**Информация:**
- `CURRENT-OFFSET` — текущий offset консьюмера
- `LAG` — отставание (разница между последним сообщением и текущим offset)

**Важно:** LAG показывает, сколько сообщений не обработано

---

## 2. 🖥️ KAFKA UI (графические интерфейсы)

### Kafka UI (провиденциальный)

**Описание:** Веб-интерфейс для работы с Kafka

**Возможности:**
- Просмотр топиков и сообщений
- Мониторинг консьюмеров
- Управление топиками

**Особенности:**
- Простой и удобный интерфейс
- Просмотр сообщений в реальном времени
- Мониторинг метрик

**Установка:**
```bash
docker run -p 8080:8080 \
  -e KAFKA_CLUSTERS_0_NAME=local \
  -e KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS=kafka:9092 \
  provectuslabs/kafka-ui
```

---

### Conduktor

**Описание:** Профессиональный инструмент для работы с Kafka

**Возможности:**
- Тестирование и отладка
- Мониторинг
- Управление топиками и консьюмерами
- Визуализация потоков данных

**Особенности:**
- Расширенный функционал
- Профессиональные инструменты
- Поддержка различных форматов

**Использование:**
- Для production окружений
- Комплексное управление Kafka
- Отладка и мониторинг

---

### Kafdrop

**Описание:** Простой веб-интерфейс для Kafka

**Возможности:**
- Просмотр топиков
- Мониторинг
- Чтение сообщений

**Особенности:**
- Легкий и быстрый
- Простой интерфейс
- Хорошо подходит для разработки

**Установка:**
```bash
docker run -d -p 9000:9000 \
  -e KAFKA_BROKERCONNECT=localhost:9092 \
  obsidiandynamics/kafdrop
```

---

## 3. 🧪 ТЕСТИРОВАНИЕ

### Testcontainers (для интеграционных тестов)

**Описание:** Запуск Kafka в Docker для тестирования

**Возможности:**
- Запуск Kafka в Docker
- Изолированное тестирование
- Автоматическая очистка после тестов

**Пример использования (Java):**
```java
@Container
static KafkaContainer kafka = new KafkaContainer(
    DockerImageName.parse("confluentinc/cp-kafka:latest")
);

@Test
void testKafkaIntegration() {
    String bootstrapServers = kafka.getBootstrapServers();
    // Тестирование с реальным Kafka
}
```

**Преимущества:**
- Реальный Kafka для тестов
- Изоляция тестов
- Легкая настройка

---

### EmbeddedKafka (для unit тестов)

**Описание:** Встроенный Kafka для быстрых тестов

**Возможности:**
- Встроенный Kafka в тестах
- Быстрые тесты
- Не требует Docker

**Пример использования (Java):**
```java
@EmbeddedKafka(
    topics = {"orders"},
    partitions = 3
)
class OrderServiceTest {
    // Unit тесты с встроенным Kafka
}
```

**Преимущества:**
- Быстрое выполнение
- Не требует внешних зависимостей
- Легкая настройка

**Ограничения:**
- Упрощенная версия Kafka
- Не все функции доступны
- Только для unit тестов

---

## 💡 Ключевые моменты

> **📌 Запомните:**

- **Kafka CLI - консольные команды** — встроенные инструменты для работы с Kafka
- **Kafka UI - графический интерфейс** — удобные инструменты для просмотра и управления
- **Testcontainers - для тестов** — реальный Kafka в Docker для интеграционных тестов
- **Проверяй lag консьюмера** — важная метрика для мониторинга

---

## 📋 Полезные команды:

### Удалить топик

**Команда:**
```bash
kafka-topics.sh --delete \
  --topic orders \
  --bootstrap-server localhost:9092
```

**Осторожно:** Удаляет все данные в топике!

---

### Изменить количество партиций

**Команда:**
```bash
kafka-topics.sh --alter \
  --topic orders \
  --partitions 6 \
  --bootstrap-server localhost:9092
```

**Ограничение:** Можно только увеличивать, нельзя уменьшать

---

### Сбросить offset consumer group

**Команда:**
```bash
kafka-consumer-groups.sh --reset-offsets \
  --group order-processors \
  --topic orders:0,1,2 \
  --to-earliest \
  --bootstrap-server localhost:9092 \
  --execute
```

**Варианты:**
- `--to-earliest` — с начала
- `--to-latest` — с последнего сообщения
- `--to-offset` — с конкретного offset

---

### Просмотр сообщений с фильтром

**Команда:**
```bash
kafka-console-consumer.sh \
  --topic orders \
  --bootstrap-server localhost:9092 \
  --from-beginning \
  --property print.key=true \
  --property print.value=true \
  --property print.partition=true \
  --property print.offset=true
```

**Результат:** Подробная информация о каждом сообщении

---

## 🎯 Best Practices:

### ✅ Рекомендации:

1. **Используйте CLI для автоматизации** — скрипты и CI/CD
2. **Используйте UI для отладки** — визуальный просмотр и анализ
3. **Testcontainers для интеграционных тестов** — реальный Kafka
4. **EmbeddedKafka для unit тестов** — быстрые тесты
5. **Мониторьте lag консьюмеров** — важная метрика производительности
6. **Используйте правильный инструмент** — CLI для автоматизации, UI для отладки

---

## 🔍 Что проверять при работе с инструментами:

- ✅ Команды выполняются корректно
- ✅ Топики создаются с правильными параметрами
- ✅ Сообщения отправляются и читаются правильно
- ✅ Consumer groups работают корректно
- ✅ Lag отслеживается и в пределах нормы
- ✅ UI инструменты показывают актуальные данные

---

[← Вернуться к содержанию раздела](./README.md)
