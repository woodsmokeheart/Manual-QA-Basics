# 📝 Уровни логирования

> Стандартные уровни логов и их применение.

---

## 📊 Уровни логирования (от самого детального к критичному)

---

## 1. 🔍 TRACE (Трассировка)

**Описание:**
- Самый детальный уровень
- Пошаговое выполнение кода
- Используется редко, только для глубокого дебага

**Пример:**
```
TRACE: Entering method calculateTotal()
TRACE: Parameter: items = [item1, item2]
TRACE: Loop iteration 1: processing item1
TRACE: Loop iteration 2: processing item2
TRACE: Exiting method calculateTotal(), result = 150.00
```

**Когда использовать:**
- Детальная отладка сложной логики
- Трассировка выполнения алгоритмов
- Временно, не в production!

---

## 2. 🐛 DEBUG (Отладка)

**Описание:**
- Детальная информация для разработчиков
- Помогает понять, что происходит в приложении

**Пример:**
```
DEBUG: User authentication started for email: user@example.com
DEBUG: Fetching user from database, query: SELECT * FROM users WHERE email = ?
DEBUG: User found: id=123, role=admin
DEBUG: Validating password hash
DEBUG: Password validation successful
DEBUG: Generating JWT token with payload: {userId: 123, role: admin}
DEBUG: Authentication completed successfully
```

**Когда использовать:**
- Отладка в dev/staging
- Диагностика проблем
- Понимание flow приложения
- Обычно выключен в production

---

## 3. ℹ️ INFO (Информация)

**Описание:**
- Важные события в приложении
- Нормальная работа системы

**Пример:**
```
INFO: Application started on port 8080
INFO: User logged in: user@example.com
INFO: Order created: orderId=456, amount=150.00
INFO: Email sent to: user@example.com
INFO: Payment processed successfully: transactionId=789
INFO: Database connection pool initialized: size=10
```

**Когда использовать:**
- Ключевые бизнес-события
- Старт/стоп сервисов
- Успешные операции
- Включен в production

---

## 4. ⚠️ WARN (Предупреждение)

**Описание:**
- Потенциальные проблемы, но приложение работает
- Что-то пошло не так, но есть fallback

**Пример:**
```
WARN: API response time exceeded threshold: 2500ms (expected < 1000ms)
WARN: Cache miss for key: user_123, fetching from database
WARN: Retry attempt 2/3 for external API call
WARN: Deprecated method used: oldCalculatePrice(), use calculatePriceV2()
WARN: Disk space low: 85% used on /var/log
WARN: Rate limit approaching: 95/100 requests used
```

**Когда использовать:**
- Медленные операции
- Использование deprecated функций
- Ретраи запросов
- Приближение к лимитам
- Неоптимальное поведение

---

## 5. ❌ ERROR (Ошибка)

**Описание:**
- Ошибка в приложении, но оно продолжает работать
- Конкретная операция провалилась

**Пример:**
```
ERROR: Failed to process payment: transactionId=789
ERROR: Database connection failed: Connection timeout after 30s
ERROR: Failed to send email to: user@example.com, reason: SMTP server unavailable
ERROR: File not found: /uploads/document.pdf
ERROR: Invalid JSON in request body: Unexpected token at position 45
ERROR: External API returned 500: https://api.example.com/users
```

**Когда использовать:**
- Ошибки обработки запросов
- Проблемы с внешними сервисами
- Ошибки валидации
- Исключения в коде

---

## 6. 🔴 FATAL / CRITICAL (Критическая ошибка)

**Описание:**
- Критическая ошибка, приложение не может работать
- Требуется немедленное вмешательство

**Пример:**
```
FATAL: Database connection pool exhausted, cannot serve requests
FATAL: Out of memory: Cannot allocate 512MB
FATAL: Configuration file missing: /etc/app/config.yml
FATAL: License expired, application shutting down
FATAL: Critical security breach detected, stopping all services
FATAL: Disk full: Cannot write logs or data
```

**Когда использовать:**
- Приложение не может запуститься
- Критические ресурсы недоступны
- Безопасность скомпрометирована
- Требуется перезапуск/вмешательство

---

## 🔄 Иерархия уровней:

**Порядок от самого детального к критичному:**
```
TRACE < DEBUG < INFO < WARN < ERROR < FATAL
```

**Принцип работы:**
- Если установлен уровень, логируются этот уровень и все более критичные
- Менее критичные уровни игнорируются

---

## ⚙️ Настройка уровня:

### Если установлен INFO:

**Пишутся:**
- ✅ INFO
- ✅ WARN
- ✅ ERROR
- ✅ FATAL

**Игнорируются:**
- ❌ TRACE
- ❌ DEBUG

---

### Если установлен ERROR:

**Пишутся:**
- ✅ ERROR
- ✅ FATAL

**Игнорируются:**
- ❌ TRACE
- ❌ DEBUG
- ❌ INFO
- ❌ WARN

---

## 🌍 Настройка уровней по окружениям:

### Development (разработка):

```
LOG_LEVEL=DEBUG
```

**Особенности:**
- Видим все детали
- Быстрая отладка
- Много логов

---

### Staging (тестирование):

```
LOG_LEVEL=INFO
```

**Особенности:**
- Ключевые события
- Ошибки и предупреждения
- Баланс детальности и объема

---

### Production (продакшн):

```
LOG_LEVEL=WARN
```

**Особенности:**
- Только проблемы
- Минимум логов
- Производительность

---

## 📋 Пример конфигурации (log4j2.xml):

```xml
<Configuration>
  <Loggers>
    <!-- Root logger -->
    <Root level="INFO">
      <AppenderRef ref="Console"/>
    </Root>

    <!-- Детальные логи для своего кода -->
    <Logger name="com.myapp" level="DEBUG"/>

    <!-- Меньше логов от библиотек -->
    <Logger name="org.springframework" level="WARN"/>
    <Logger name="org.hibernate" level="ERROR"/>
  </Loggers>
</Configuration>
```

**Объяснение:**
- Root logger: `INFO` — для всего приложения
- `com.myapp`: `DEBUG` — детальные логи для своего кода
- Библиотеки: `WARN`/`ERROR` — меньше логов от сторонних библиотек

---

## ✅ Best Practices:

### 1. Правильный уровень

**Правильно:**
```javascript
INFO: "User logged in"
ERROR: "Payment failed"
```

**Неправильно:**
```javascript
ERROR: "User logged in"  // не ошибка!
INFO: "Payment failed"   // это ошибка!
```

**Принцип:**
- Используйте правильный уровень для события
- Не завышайте и не занижайте уровень

---

### 2. Контекст

**Правильно:**
```javascript
ERROR: "Payment failed: orderId=123, userId=456, amount=100.00, reason=Insufficient funds"
```

**Неправильно:**
```javascript
ERROR: "Payment failed"  // недостаточно информации
```

**Принцип:**
- Включайте контекст в логи
- ID запросов, пользователей, транзакций
- Параметры, которые помогут воспроизвести проблему

---

### 3. Не логировать чувствительные данные

**Неправильно:**
```javascript
DEBUG: "Password: qwerty123"
INFO: "Credit card: 4111-1111-1111-1111"
```

**Правильно:**
```javascript
INFO: "Payment processed: card ending ****1111"
```

**Что не логировать:**
- ❌ Пароли
- ❌ Полные номера кредитных карт
- ❌ Токены авторизации
- ❌ Персональные данные (полные)

**Что можно логировать:**
- ✅ Маскированные данные (последние 4 цифры)
- ✅ Хеши паролей (если нужно)
- ✅ ID пользователей (не чувствительно)

---

### 4. Структурированные логи (JSON)

**Преимущества:**
- Легко парсить
- Легко фильтровать
- Легко анализировать

**Пример:**
```json
{
  "timestamp": "2024-01-15T10:30:00Z",
  "level": "ERROR",
  "message": "Payment failed",
  "context": {
    "orderId": 123,
    "userId": 456,
    "amount": 100.00,
    "error": "Insufficient funds"
  }
}
```

**Использование:**
- Для production систем
- Для анализа логов инструментами (ELK, Splunk и т.д.)
- Для автоматической обработки

---

### 5. Не логировать в циклах

**Неправильно:**
```javascript
for (item in items) {
  DEBUG: "Processing item"  // будет 1000+ логов!
}
```

**Правильно:**
```javascript
DEBUG: "Processing 100 items"
// ... обработка ...
DEBUG: "Processed 100 items in 250ms"
```

**Принцип:**
- Логируйте начало и конец операции
- Не логируйте каждую итерацию цикла
- Логируйте итоговую статистику

---

## 💡 Ключевые моменты

> **📌 Запомните:**

- **TRACE/DEBUG - для разработки** — детальная информация, обычно выключены в production
- **INFO - важные события** — ключевые бизнес-события, включены в production
- **WARN - потенциальные проблемы** — что-то неоптимально, но работает
- **ERROR/FATAL - ошибки и критика** — проблемы, требующие внимания
- **Не логируй пароли и sensitive данные** — безопасность важна

---

## 🎯 Чек-лист использования уровней:

### TRACE:
- [ ] Используется только для глубокой отладки
- [ ] Выключен в production
- [ ] Пошаговое выполнение кода

### DEBUG:
- [ ] Используется в dev/staging
- [ ] Помогает понять flow приложения
- [ ] Обычно выключен в production

### INFO:
- [ ] Ключевые бизнес-события
- [ ] Успешные операции
- [ ] Включен в production

### WARN:
- [ ] Потенциальные проблемы
- [ ] Медленные операции
- [ ] Приближение к лимитам

### ERROR:
- [ ] Ошибки обработки запросов
- [ ] Проблемы с внешними сервисами
- [ ] Исключения в коде

### FATAL:
- [ ] Критические ошибки
- [ ] Приложение не может работать
- [ ] Требуется немедленное вмешательство

---

## 📊 Рекомендации по окружениям:

| Окружение | Уровень | Причина |
|-----------|---------|---------|
| **Development** | DEBUG | Максимальная детализация для отладки |
| **Staging** | INFO | Баланс между детализацией и объемом |
| **Production** | WARN | Только проблемы, минимум логов |

---

[← Вернуться к содержанию раздела](./README.md)
