# 🔺 Пирамида тестирования

> Оптимальное распределение типов тестов.

---

## 📊 Пирамида тестирования

**Концепция:** Оптимальное распределение тестов по уровням.

---

## 🏗️ Структура пирамиды:

```
        / UI \        ← Мало (5-10%)
       / API \        ← Средне (20-30%)
      / Integration \ ← Больше (30-40%)
     / Unit \         ← Много (40-50%)
```

**Уровни (снизу вверх):**
1. **Unit** — основание пирамиды (40-50%)
2. **Integration** — второй уровень (30-40%)
3. **API** — третий уровень (20-30%)
4. **UI** — вершина пирамиды (5-10%)

---

## 📐 Принцип:

**Основные правила:**
- Чем ниже уровень — тем больше тестов
- Чем выше уровень — тем дороже и медленнее

**Почему пирамида?**
- **Unit тесты:** быстрые, дешевые, стабильные
- **UI тесты:** медленные, дорогие, хрупкие

**Цель:** максимум покрытия при минимуме затрат

---

## 1. 🧪 UNIT ТЕСТЫ (основание пирамиды)

**Что:** Тестирование отдельных функций/методов

**Кто:** Разработчики

**Инструменты:** JUnit, pytest, Jest

**Характеристики:**
- ✅ Очень быстрые (миллисекунды)
- ✅ Изолированные (моки зависимостей)
- ✅ Стабильные (нет внешних зависимостей)
- ✅ Дешевые в поддержке
- ✅ Легко найти причину падения

**Пример:**
```java
// Тест функции расчета скидки
@Test
public void testCalculateDiscount() {
    // Given
    double price = 100.0;
    double discountPercent = 10.0;
    
    // When
    double result = calculator.calculateDiscount(price, discountPercent);
    
    // Then
    assertEquals(90.0, result);
}
```

**Покрытие:** 40-50% всех тестов

**Время выполнения:** секунды для тысяч тестов

---

## 2. 🔗 INTEGRATION ТЕСТЫ (интеграционные)

**Что:** Взаимодействие между компонентами

**Кто:** Разработчики + QA

**Инструменты:** TestContainers, Spring Test

**Характеристики:**
- ✅ Быстрые (секунды)
- ⚠️ Требуют реальных зависимостей (БД, очереди)
- ✅ Проверяют реальное взаимодействие
- ⚠️ Сложнее в настройке

**Пример:**
```java
// Тест сохранения пользователя в БД
@Test
public void testSaveUser() {
    // Given
    User user = new User("John", "john@example.com");
    
    // When
    userRepository.save(user);
    
    // Then
    User saved = userRepository.findByEmail("john@example.com");
    assertNotNull(saved);
    assertEquals("John", saved.getName());
}
```

**Покрытие:** 30-40% всех тестов

**Время выполнения:** минуты

---

## 3. 🌐 API ТЕСТЫ (сервисные)

**Что:** Тестирование API endpoints

**Кто:** QA

**Инструменты:** REST Assured, Postman, Playwright

**Характеристики:**
- ⚠️ Средняя скорость (секунды на тест)
- ✅ Не зависят от UI
- ✅ Стабильнее UI тестов
- ✅ Проверяют бизнес-логику

**Пример:**
```java
// Тест создания пользователя через API
@Test
public void testCreateUser() {
    given()
        .contentType("application/json")
        .body("{\"name\": \"John\", \"email\": \"john@example.com\"}")
    .when()
        .post("/api/users")
    .then()
        .statusCode(201)
        .body("name", equalTo("John"))
        .body("email", equalTo("john@example.com"));
}
```

**Покрытие:** 20-30% всех тестов

**Время выполнения:** минуты-десятки минут

---

## 4. 🖥️ UI ТЕСТЫ (вершина пирамиды)

**Что:** Тестирование через интерфейс

**Кто:** QA

**Инструменты:** Selenium, Playwright, Cypress

**Характеристики:**
- ❌ Медленные (секунды-минуты на тест)
- ❌ Хрупкие (ломаются от изменений UI)
- ❌ Дорогие в поддержке
- ❌ Flaky (нестабильные)
- ✅ Проверяют реальный UX

**Пример:**
```java
// Тест регистрации через UI
@Test
public void testUserRegistration() {
    driver.get("https://example.com/register");
    driver.findElement(By.id("email")).sendKeys("john@example.com");
    driver.findElement(By.id("password")).sendKeys("Test123!");
    driver.findElement(By.id("submit")).click();
    
    String message = driver.findElement(By.className("success")).getText();
    assertEquals("Registration successful", message);
}
```

**Покрытие:** 5-10% всех тестов

**Время выполнения:** часы

---

## 🎯 Что тестировать на UI:

**Рекомендуется:**
- ✅ Критические пути (регистрация, оплата)
- ✅ Smoke тесты
- ✅ Основные пользовательские сценарии

**Не рекомендуется:**
- ❌ Все возможные сценарии (делай на API/Unit)
- ❌ Граничные случаи (делай на Unit)
- ❌ Валидация полей (делай на Unit)

---

## ⚠️ АНТИПАТТЕРН: Перевернутая пирамида (Ice Cream Cone)

**Структура антипаттерна:**
```
\ Unit /         ← Мало
\ Integration /  ← Мало
\ API /          ← Средне
\ UI /           ← МНОГО [-]
```

**Проблемы:**
- ❌ Медленные тесты (часы выполнения)
- ❌ Нестабильные (flaky tests)
- ❌ Дорогие в поддержке
- ❌ Сложно найти причину падения
- ❌ Блокируют релизы

**Причины:**
- Нет unit тестов от разработчиков
- QA автоматизирует только UI
- "Тестируем как пользователь"

**Решение:**
1. Разработчики пишут unit тесты
2. QA автоматизирует API
3. UI только для критических путей

---

## 💡 Практические рекомендации:

### 1. Начни с основания

**Порядок создания тестов:**
1. Сначала unit тесты
2. Потом integration
3. Затем API
4. В конце UI

---

### 2. Правило: чем ниже, тем лучше

**Выбор уровня тестирования:**
- Можно проверить на unit? → Делай unit
- Нужна БД? → Integration
- Нужен API? → API тест
- Только UI? → UI тест

---

### 3. Пример: Валидация email

**Неправильно (UI тест):**
```java
// Открыть форму, ввести невалидный email, проверить ошибку
[-] UI тест: открыть форму, ввести невалидный email, проверить ошибку
```

**Правильно (Unit тест):**
```java
// Прямая проверка функции валидации
[+] Unit тест: validateEmail("invalid") → false
```

**Почему:**
- Unit тест быстрее (миллисекунды vs секунды)
- Unit тест стабильнее (не зависит от UI)
- Unit тест проще поддерживать

---

### 4. Пример: Создание заказа

**Комплексное тестирование:**

**Unit:**
```java
[+] Unit: расчет суммы
[+] Unit: применение скидки
[+] Unit: валидация данных
```

**Integration:**
```java
[+] Integration: сохранение в БД
[+] Integration: отправка в очередь
```

**API:**
```java
[+] API: POST /orders
[+] API: проверка статус кода
[+] API: проверка ответа
```

**UI:**
```java
[+] UI: smoke тест оформления заказа
```

**Результат:** Полное покрытие на разных уровнях

---

### 5. Метрики здоровой пирамиды:

**Распределение тестов:**
- **Unit:** 40-50%
- **Integration:** 30-40%
- **API:** 20-30%
- **UI:** 5-10%

**Время прогона:** < 10 минут для всех тестов

**Признаки здоровой пирамиды:**
- ✅ Большинство тестов — unit
- ✅ Быстрое выполнение всего набора
- ✅ Стабильные тесты
- ✅ Легко найти причину падения

---

## 💡 Ключевые моменты

> **📌 Запомните:**

- **Больше unit, меньше UI** — основание пирамиды должно быть широким
- **UI тесты медленные и хрупкие** — используйте их минимально
- **Тестируй на самом низком уровне** — unit > integration > API > UI
- **Избегай перевернутой пирамиды** — это антипаттерн

---

## 📊 Сравнение уровней:

| Характеристика | Unit | Integration | API | UI |
|----------------|------|-------------|-----|-----|
| **Скорость** | Очень быстро | Быстро | Средне | Медленно |
| **Стабильность** | Высокая | Средняя | Средняя | Низкая |
| **Стоимость** | Низкая | Средняя | Средняя | Высокая |
| **Поддержка** | Легко | Средне | Средне | Сложно |
| **Покрытие** | 40-50% | 30-40% | 20-30% | 5-10% |

---

## 🎯 Best Practices:

### ✅ Рекомендации:

1. **Начните с unit тестов** — они основа пирамиды
2. **Используйте правильный уровень** — тестируйте на самом низком возможном уровне
3. **Избегайте перевернутой пирамиды** — это антипаттерн
4. **UI тесты только для критических путей** — не покрывайте все через UI
5. **Автоматизируйте API тесты** — они стабильнее UI
6. **Мониторьте метрики** — следите за распределением тестов
7. **Оптимизируйте время выполнения** — цель < 10 минут для всех тестов

---

## 📋 Чек-лист здоровой пирамиды:

- [ ] Unit тесты составляют 40-50% всех тестов
- [ ] Integration тесты составляют 30-40% всех тестов
- [ ] API тесты составляют 20-30% всех тестов
- [ ] UI тесты составляют 5-10% всех тестов
- [ ] Время выполнения всех тестов < 10 минут
- [ ] Большинство тестов стабильны
- [ ] Легко найти причину падения теста
- [ ] Нет перевернутой пирамиды

---

[← Вернуться к содержанию раздела](./README.md)
