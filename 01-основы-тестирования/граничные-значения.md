# 🎯 Граничные значения

> Boundary Value Analysis - тестирование границ.

---

## 🔍 BOUNDARY VALUE ANALYSIS (Граничные значения)

**Идея:** Баги чаще всего на границах диапазонов.

**Принцип:** Тестируем значения **НА границе** и **ОКОЛО границы**.

---

## 📐 Правило для диапазона [min, max]:

Для диапазона значений нужно протестировать:

| Значение | Тип | Описание |
|----------|-----|----------|
| `min - 1` | ❌ Невалидное | Меньше минимального |
| `min` | ✅ Валидное (граница) | Минимальная граница |
| `min + 1` | ✅ Валидное | Сразу после минимума |
| `max - 1` | ✅ Валидное | Сразу перед максимумом |
| `max` | ✅ Валидное (граница) | Максимальная граница |
| `max + 1` | ❌ Невалидное | Больше максимального |

**Итого:** 6 тестов для одного диапазона

---

## 📝 Пример: Возраст (18-65)

### Тест-кейсы для граничных значений:

| № | Значение | Ожидаемый результат | Тип |
|---|----------|---------------------|-----|
| 1 | `17` | ❌ Ошибка (меньше минимума) | Невалидное |
| 2 | `18` | ✅ Успех (минимальная граница) | Граница |
| 3 | `19` | ✅ Успех (после границы) | Валидное |
| 4 | `64` | ✅ Успех (перед границей) | Валидное |
| 5 | `65` | ✅ Успех (максимальная граница) | Граница |
| 6 | `66` | ❌ Ошибка (больше максимума) | Невалидное |

---

## ⚠️ Почему важно:

**Примеры проблем на границах:**

- **[+]** `18` работает, но `17` нет — проверка условия работает правильно
- **[+]** `65` работает, но `66` нет — проверка условия работает правильно
- **[-]** Часто ошибки в условиях: `>=` vs `>`, `<=` vs `<`

**Типичные ошибки разработчиков:**
- Использование `>` вместо `>=` — граничное значение не обрабатывается
- Использование `<` вместо `<=` — граничное значение не обрабатывается
- Пропуск проверки граничных значений

---

## 💡 Ключевые моменты

> **📌 Запомните:**

- **Баги часто на границах** — большинство ошибок происходит именно при обработке граничных значений
- **Тестируй min, max и около них** — обязательно проверяем значения на границах и рядом
- **Проверяй условия >= vs >** — обращайте внимание на строгие и нестрогие неравенства
- **Комбинируй с классами эквивалентности** — BVA дополняет технику классов эквивалентности

---

## 🔄 Связь с классами эквивалентности

**Классы эквивалентности:**
- Определяют группы данных

**Граничные значения:**
- Уточняют тестирование на границах этих групп

**Комбинация обеих техник:**
- Максимальное покрытие при минимальном количестве тестов

---

## 📊 Когда использовать

Граничные значения эффективны для:
- ✅ Числовых диапазонов
- ✅ Ограничений по длине (текст, пароли, массивы)
- ✅ Временных интервалов
- ✅ Любых диапазонов с четкими границами

---

[← Вернуться к содержанию раздела](./README.md)
