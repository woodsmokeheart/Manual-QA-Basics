# 📮 Postman для тестирования

> Основы работы с Postman.

---

## 📝 Создание запроса:

### Шаг 1: Выбрать метод

**Доступные методы:**
- `GET` — получение данных
- `POST` — создание ресурса
- `PUT` — полная замена ресурса
- `PATCH` — частичное обновление
- `DELETE` — удаление ресурса

**Как выбрать:**
- Выпадающий список вверху окна запроса
- Или ввести метод вручную

---

### Шаг 2: Ввести URL

**Пример:**
```
https://api.example.com/users
```

**Особенности:**
- Можно использовать переменные: `{{base_url}}/users`
- Поддержка query параметров: `?page=1&limit=10`
- Автодополнение для сохраненных URL

---

### Шаг 3: Добавить заголовки (Headers)

**Обязательные заголовки:**
```
Content-Type: application/json
Authorization: Bearer {{token}}
```

**Другие полезные заголовки:**
```
Accept: application/json
X-Request-ID: {{request_id}}
Cookie: session_id={{session_id}}
```

**Как добавить:**
- Вкладка **Headers**
- Key: название заголовка
- Value: значение (можно использовать переменные)

---

### Шаг 4: Добавить body (для POST/PUT)

**Для POST/PUT запросов:**

**JSON формат:**
```json
{
  "name": "John",
  "email": "john@example.com"
}
```

**Другие форматы:**
- **form-data** — для загрузки файлов
- **x-www-form-urlencoded** — для форм
- **raw** — для произвольных данных
- **binary** — для бинарных данных

**Как добавить:**
- Вкладка **Body**
- Выбрать формат (raw → JSON)
- Ввести данные

---

### Шаг 5: Нажать Send

**Результат:**
- Отправка запроса на сервер
- Получение ответа
- Отображение статус кода, заголовков, тела ответа
- Выполнение тестов (если настроены)

---

## 🔄 Переменные:

### Environment Variables (Переменные окружения)

**Определение переменных:**

```
{{base_url}} = https://api.example.com
{{token}} = eyJhbGc...
{{user_id}} = 123
```

**Как создать:**
1. Открыть **Environments** (слева)
2. Создать новое окружение или выбрать существующее
3. Добавить переменные: **Key** и **Value**

---

### Использование переменных:

**В URL:**
```
GET {{base_url}}/users
```

**В заголовках:**
```
Authorization: Bearer {{token}}
```

**В body:**
```json
{
  "user_id": {{user_id}},
  "email": "{{email}}"
}
```

**Особенности:**
- Переменные оборачиваются в двойные фигурные скобки: `{{variable}}`
- Подстановка происходит автоматически при отправке запроса
- Можно использовать в любом месте запроса

---

### Установка переменной в тесте:

**Извлечение значения из ответа:**

```javascript
// Получить ID из ответа и сохранить в переменную
pm.environment.set("user_id", response.id);

// Получить токен из ответа
pm.environment.set("token", response.json().token);

// Установить глобальную переменную
pm.globals.set("global_var", "value");
```

**Использование:**
- Вкладка **Tests** запроса
- После получения ответа
- Для передачи данных между запросами

---

## 📚 Коллекции и Runner:

### Структура коллекции:

**Пример организации:**

```
User API/
├── Auth/
│   ├── Login
│   └── Logout
├── Users/
│   ├── Create User
│   ├── Get User
│   ├── Update User
│   └── Delete User
└── Orders/
    ├── Create Order
    └── Get Orders
```

**Преимущества:**
- Организация запросов по функциональности
- Группировка связанных запросов
- Удобная навигация
- Возможность запуска всей коллекции

---

### Collection Runner:

**Как использовать:**

1. **Выбрать коллекцию** — выбрать нужную коллекцию
2. **Выбрать environment** — выбрать окружение с переменными
3. **Настроить iterations** — количество повторений (повторы)
4. **Настроить delay** — задержка между запросами (в миллисекундах)
5. **Run** — запустить выполнение

**Результат:**
- Сколько тестов прошло/провалилось
- Время выполнения
- Детали по каждому запросу
- Логи выполнения

**Использование:**
- Автоматизация тестирования
- Запуск набора запросов
- Проверка всего API

---

## 🔧 Pre-request Script:

**Назначение:** Выполняется перед запросом.

**Использование:**
- Генерация данных
- Получение токенов
- Подготовка переменных
- Динамические значения

---

### Примеры:

#### Генерация случайных данных:

```javascript
// Генерация случайных данных
const randomEmail = `user${Date.now()}@example.com`;
pm.environment.set("random_email", randomEmail);

// Генерация случайного имени
const randomName = `User_${Math.floor(Math.random() * 1000)}`;
pm.environment.set("random_name", randomName);
```

---

#### Получение токена:

```javascript
// Получение токена
pm.sendRequest({
    url: pm.environment.get("base_url") + "/auth/token",
    method: "POST",
    header: {
        "Content-Type": "application/json"
    },
    body: {
        mode: "raw",
        raw: JSON.stringify({
            username: pm.environment.get("username"),
            password: pm.environment.get("password")
        })
    }
}, function(err, response) {
    if (response.code === 200) {
        pm.environment.set("token", response.json().token);
    }
});
```

---

#### Установка текущей даты:

```javascript
// Установка текущей даты
const now = new Date();
pm.environment.set("current_date", now.toISOString());
```

---

## ✅ Тесты (Tests):

**Назначение:** Проверка ответа после выполнения запроса.

**Как использовать:**
- Вкладка **Tests** в запросе
- JavaScript код для проверок
- Автоматическое выполнение после получения ответа

---

### Примеры тестов:

#### Проверка статус кода:

```javascript
// Проверка статус кода
pm.test("Status code is 200", function () {
    pm.response.to.have.status(200);
});

pm.test("Status code is 201", function () {
    pm.response.to.have.status(201);
});
```

---

#### Проверка тела ответа:

```javascript
// Проверка структуры ответа
pm.test("Response has user data", function () {
    const responseJson = pm.response.json();
    pm.expect(responseJson).to.have.property("id");
    pm.expect(responseJson).to.have.property("name");
    pm.expect(responseJson).to.have.property("email");
});

// Проверка значений
pm.test("User ID is correct", function () {
    const responseJson = pm.response.json();
    pm.expect(responseJson.id).to.be.a("number");
});
```

---

#### Проверка заголовков:

```javascript
// Проверка заголовков
pm.test("Content-Type is JSON", function () {
    pm.response.to.have.header("Content-Type", "application/json");
});

pm.test("Location header exists", function () {
    pm.expect(pm.response.headers.get("Location")).to.exist;
});
```

---

#### Проверка времени ответа:

```javascript
// Проверка времени ответа
pm.test("Response time is less than 500ms", function () {
    pm.expect(pm.response.responseTime).to.be.below(500);
});
```

---

## 💡 Ключевые моменты

> **📌 Запомните:**

- **Используй переменные {{variable}}** — для переиспользования значений
- **Пиши тесты во вкладке Tests** — для автоматической проверки ответов
- **Collection Runner для автоматизации** — для запуска набора запросов
- **Pre-request для подготовки данных** — для генерации данных и получения токенов

---

## 🎯 Best Practices:

### ✅ Рекомендации:

1. **Используйте переменные** — для base_url, токенов, ID
2. **Организуйте коллекции** — группируйте связанные запросы
3. **Пишите тесты** — автоматизируйте проверки
4. **Используйте Pre-request** — для подготовки данных
5. **Используйте Collection Runner** — для автоматизации
6. **Сохраняйте примеры** — для документации API
7. **Используйте окружения** — для разных сред (dev, staging, prod)

---

## 📋 Примеры использования:

### Пример 1: Создание пользователя с тестами

**Запрос:**
```
POST {{base_url}}/users
```

**Headers:**
```
Content-Type: application/json
Authorization: Bearer {{token}}
```

**Body:**
```json
{
  "name": "{{random_name}}",
  "email": "{{random_email}}"
}
```

**Pre-request Script:**
```javascript
const randomEmail = `user${Date.now()}@example.com`;
pm.environment.set("random_email", randomEmail);
```

**Tests:**
```javascript
pm.test("Status code is 201", function () {
    pm.response.to.have.status(201);
});

pm.test("User created successfully", function () {
    const responseJson = pm.response.json();
    pm.expect(responseJson).to.have.property("id");
    pm.environment.set("user_id", responseJson.id);
});
```

---

### Пример 2: Запуск коллекции

**Шаги:**
1. Открыть **Collection Runner**
2. Выбрать коллекцию **User API**
3. Выбрать environment **Development**
4. Установить **Iterations: 1**
5. Установить **Delay: 1000ms**
6. Нажать **Run**

**Результат:**
- Все запросы выполнятся последовательно
- Тесты проверятся автоматически
- Результаты отобразятся в отчете

---

[← Вернуться к содержанию раздела](./README.md)
