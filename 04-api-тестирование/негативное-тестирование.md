# 🧨 Негативное тестирование API

> Проверка обработки ошибок.

---

## 🎯 Цель негативного тестирования

**Определение:** Тестирование API с невалидными данными и некорректными запросами для проверки обработки ошибок.

**Задачи:**
- Проверить, что API корректно обрабатывает ошибки
- Убедиться, что система не падает при невалидных данных
- Проверить информативность сообщений об ошибках
- Проверить безопасность API

---

## 1. ❌ Невалидные данные

### Тест: Неверный тип данных

**Запрос:**
```http
POST /api/users
Content-Type: application/json

{
  "age": "abc",        ← строка вместо числа
  "email": 12345,      ← число вместо строки
  "active": "yes"      ← строка вместо boolean
}
```

**Ожидаем:**
- ✓ `422 Unprocessable Entity`
- ✓ Понятные сообщения об ошибках

**Пример ответа:**
```json
{
  "error": "Validation failed",
  "details": [
    {
      "field": "age",
      "message": "age must be a number"
    },
    {
      "field": "email",
      "message": "email must be a string"
    },
    {
      "field": "active",
      "message": "active must be a boolean"
    }
  ]
}
```

**Что проверять:**
- ✅ Статус код `422` (или `400`)
- ✅ Сообщения об ошибках указывают на конкретные поля
- ✅ Типы данных валидируются корректно

---

### Тест: Отсутствующие обязательные поля

**Запрос:**
```http
POST /api/users
Content-Type: application/json

{
  "name": "John"
  // email отсутствует
}
```

**Ожидаем:**
- ✓ `422 Unprocessable Entity`
- ✓ `"email is required"`

**Пример ответа:**
```json
{
  "error": "Validation failed",
  "details": [
    {
      "field": "email",
      "message": "email is required"
    }
  ]
}
```

**Что проверять:**
- ✅ Статус код `422`
- ✅ Указано, какие поля обязательны
- ✅ Сообщение об ошибке понятное

---

### Тест: Дополнительные поля

**Запрос:**
```http
POST /api/users
Content-Type: application/json

{
  "name": "John",
  "email": "john@example.com",
  "hacker_field": "malicious"  ← лишнее поле
}
```

**Ожидаем:**
- ✓ Поле игнорируется или ошибка
- ✓ Не сохраняется в БД

**Варианты поведения:**
1. **Поле игнорируется** (рекомендуется):
   ```json
   {
     "id": 1,
     "name": "John",
     "email": "john@example.com"
     // hacker_field не сохранился
   }
   ```

2. **Ошибка валидации**:
   ```json
   {
     "error": "Unknown field: hacker_field"
   }
   ```

**Что проверять:**
- ✅ Лишние поля не сохраняются в БД
- ✅ Лишние поля не влияют на работу API
- ✅ Безопасность данных (нет утечки через лишние поля)

---

## 2. 🔲 Граничные случаи

### Тест: Пустые значения

**Запрос:**
```http
POST /api/users
Content-Type: application/json

{
  "name": "",
  "email": "",
  "bio": ""
}
```

**Ожидаем:**
- ✓ Валидация пустых строк
- ✓ Или разрешены, если допустимо

**Пример ответа (если недопустимо):**
```json
{
  "error": "Validation failed",
  "details": [
    {
      "field": "name",
      "message": "name cannot be empty"
    },
    {
      "field": "email",
      "message": "email cannot be empty"
    }
  ]
}
```

**Что проверять:**
- ✅ Пустые строки валидируются
- ✅ Сообщения об ошибках понятные
- ✅ Или пустые строки разрешены (если это допустимо по бизнес-логике)

---

### Тест: Очень длинные строки

**Запрос:**
```http
POST /api/users
Content-Type: application/json

{
  "name": "A".repeat(10000),   ← 10000 символов
  "bio": "B".repeat(100000)     ← 100000 символов
}
```

**Ожидаем:**
- ✓ `422` или обрезка до лимита
- ✓ Не падает сервер

**Пример ответа:**
```json
{
  "error": "Validation failed",
  "details": [
    {
      "field": "name",
      "message": "name must be less than 255 characters"
    },
    {
      "field": "bio",
      "message": "bio must be less than 10000 characters"
    }
  ]
}
```

**Что проверять:**
- ✅ Статус код `422` (или `400`)
- ✅ Сервер не падает при очень длинных строках
- ✅ Есть ограничения на длину полей
- ✅ Сообщения об ошибках информативны

---

### Тест: Специальные символы

**Запрос:**
```http
POST /api/users
Content-Type: application/json

{
  "name": "John<script>alert('xss')</script>",  // XSS попытка
  "bio": "'; DROP TABLE users; --"               // SQL injection попытка
}
```

**Ожидаем:**
- ✓ Экранирование или отклонение
- ✓ Безопасное сохранение

**Пример ответа (безопасное сохранение):**
```json
{
  "id": 1,
  "name": "John&lt;script&gt;alert('xss')&lt;/script&gt;",
  "bio": "'; DROP TABLE users; --"
}
```

**Или отклонение:**
```json
{
  "error": "Validation failed",
  "details": [
    {
      "field": "name",
      "message": "name contains invalid characters"
    }
  ]
}
```

**Что проверять:**
- ✅ XSS атаки блокируются или экранируются
- ✅ SQL injection атаки блокируются
- ✅ Данные сохраняются безопасно
- ✅ Специальные символы обрабатываются корректно

---

## 3. 🔍 Несуществующие ресурсы

### Тест: Несуществующий ID

**Запрос:**
```http
GET /api/users/999999
```

**Ожидаем:**
- ✓ `404 Not Found`
- ✓ Понятное сообщение: `{ "error": "User not found" }`

**Пример ответа:**
```json
{
  "error": "User not found",
  "message": "User with id 999999 does not exist"
}
```

**Что проверять:**
- ✅ Статус код `404`
- ✅ Сообщение об ошибке понятное
- ✅ Не раскрывает внутреннюю структуру системы

---

### Тест: Несуществующий endpoint

**Запрос:**
```http
GET /api/nonexistent
```

**Ожидаем:**
- ✓ `404 Not Found`
- ✓ Не раскрывает структуру API

**Пример ответа:**
```json
{
  "error": "Not Found",
  "message": "Endpoint not found"
}
```

**Что проверять:**
- ✅ Статус код `404`
- ✅ Сообщение не раскрывает структуру API
- ✅ Безопасность (не дает информацию о доступных эндпоинтах)

---

### Тест: Неверный метод

**Запрос:**
```http
POST /api/users/1
← должен быть PUT
```

**Ожидаем:**
- ✓ `405 Method Not Allowed`
- ✓ `Allow: GET, PUT, DELETE`

**Пример ответа:**
```http
HTTP/1.1 405 Method Not Allowed
Allow: GET, PUT, DELETE

{
  "error": "Method Not Allowed",
  "message": "POST method is not allowed for this endpoint"
}
```

**Что проверять:**
- ✅ Статус код `405`
- ✅ Заголовок `Allow` указывает разрешенные методы
- ✅ Сообщение об ошибке понятное

---

## 4. 📋 Невалидные заголовки

### Тест: Неверный Content-Type

**Запрос:**
```http
POST /api/users
Content-Type: text/plain

{"name": "John"}
```

**Ожидаем:**
- ✓ `415 Unsupported Media Type`

**Пример ответа:**
```json
{
  "error": "Unsupported Media Type",
  "message": "Content-Type must be application/json"
}
```

**Что проверять:**
- ✅ Статус код `415`
- ✅ Сообщение указывает на требуемый Content-Type
- ✅ API не обрабатывает неверный Content-Type

---

### Тест: Отсутствующий токен

**Запрос:**
```http
GET /api/users/1
(без Authorization)
```

**Ожидаем:**
- ✓ `401 Unauthorized`

**Пример ответа:**
```json
{
  "error": "Unauthorized",
  "message": "Token is required"
}
```

**Что проверять:**
- ✅ Статус код `401`
- ✅ Сообщение об ошибке понятное
- ✅ Защищенные эндпоинты требуют авторизацию

---

## 5. ⚡ Конкурентные запросы и Race Conditions

### Тест: Одновременное создание дубликатов

**Сценарий:**
- Отправить два одинаковых запроса одновременно
- Проверить обработку дубликатов

**Запрос 1 и 2 (одновременно):**
```http
POST /api/users
Content-Type: application/json

{
  "email": "duplicate@example.com"
}
```

**Ожидаем:**
- ✓ Один запрос успешен (`201 Created`)
- ✓ Второй запрос возвращает ошибку (`409 Conflict` или `422`)

**Пример ответа (второй запрос):**
```json
{
  "error": "Conflict",
  "message": "User with email duplicate@example.com already exists"
}
```

**Что проверять:**
- ✅ Дубликаты обрабатываются корректно
- ✅ Нет создания двух одинаковых записей
- ✅ Статус код `409` для конфликтов

---

### Тест: Одновременное обновление

**Сценарий:**
- Отправить два запроса на обновление одного ресурса одновременно
- Проверить целостность данных

**Запрос 1:**
```http
PUT /api/users/1
Content-Type: application/json

{
  "name": "John Updated 1"
}
```

**Запрос 2 (одновременно):**
```http
PUT /api/users/1
Content-Type: application/json

{
  "name": "John Updated 2"
}
```

**Ожидаем:**
- ✓ Оба запроса обрабатываются
- ✓ Финальное состояние корректно
- ✓ Нет потери данных

**Что проверять:**
- ✅ Конкурентные обновления обрабатываются
- ✅ Данные не теряются
- ✅ Используется правильная стратегия (last write wins или optimistic locking)

---

## 💡 Ключевые моменты

> **📌 Запомните:**

- **Тестируй неверные типы данных** — API должен валидировать типы
- **Проверяй граничные значения** — пустые строки, очень длинные строки, специальные символы
- **Несуществующие ресурсы → 404** — корректная обработка отсутствующих данных
- **Конкурентные запросы и race conditions** — проверка параллельных операций

---

## 📋 Чек-лист негативного тестирования:

### Невалидные данные:
- [ ] Неверные типы данных → `422`
- [ ] Отсутствующие обязательные поля → `422`
- [ ] Дополнительные поля → игнорируются или ошибка
- [ ] Null значения → валидация

### Граничные случаи:
- [ ] Пустые строки → валидация
- [ ] Очень длинные строки → `422` или обрезка
- [ ] Специальные символы → экранирование или отклонение
- [ ] Отрицательные числа (если недопустимо) → валидация
- [ ] Нулевые значения → валидация

### Несуществующие ресурсы:
- [ ] Несуществующий ID → `404`
- [ ] Несуществующий endpoint → `404`
- [ ] Неверный HTTP метод → `405`

### Невалидные заголовки:
- [ ] Неверный Content-Type → `415`
- [ ] Отсутствующий токен → `401`
- [ ] Невалидный токен → `401`

### Конкурентные запросы:
- [ ] Дубликаты → `409` или `422`
- [ ] Одновременные обновления → корректная обработка
- [ ] Race conditions → нет потери данных

---

## 🎯 Best Practices:

### ✅ Рекомендации:

1. **Всегда тестируйте негативные сценарии** — они не менее важны, чем позитивные
2. **Проверяйте сообщения об ошибках** — они должны быть понятными и информативными
3. **Тестируйте граничные значения** — часто ошибки возникают на границах
4. **Проверяйте безопасность** — XSS, SQL injection, специальные символы
5. **Тестируйте конкурентные запросы** — реальные системы работают параллельно
6. **Документируйте ожидаемое поведение** — что должно происходить при ошибках
7. **Используйте автоматизацию** — негативные тесты можно автоматизировать

---

[← Вернуться к содержанию раздела](./README.md)
