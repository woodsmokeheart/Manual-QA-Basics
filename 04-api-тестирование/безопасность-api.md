# 🔒 Безопасность API

> JWT авторизация в API.

---

## 🔐 JWT Авторизация

**Определение:** JWT (JSON Web Token) — токен для авторизации запросов.

**Структура JWT:**
```
header.payload.signature
```

**Три части токена:**
1. **Header** — метаданные (алгоритм, тип токена)
2. **Payload** — данные (user_id, role, exp и т.д.)
3. **Signature** — подпись для проверки подлинности

---

## 📋 Пример токена:

**Полный токен:**
```
eyJhbGci0iJIUzI1NiIsInR5cCI6IkpXVCJ9.
eyJ1c2VyX2lkIjoxLCJyb2xlIjoidXNlciJ9.
SflKxwRJSMeKKF2QT4fwpMeJf36P0k6yJV_adQssw5c
```

**Декодированный payload:**
```json
{
  "user_id": 1,
  "role": "user",
  "exp": 1640000000  // время истечения (expiration time)
}
```

**Особенности:**
- Токен состоит из трех частей, разделенных точками
- Payload содержит данные пользователя
- `exp` — время истечения токена (Unix timestamp)
- Токен подписан секретным ключом сервера

---

## 🚀 Как использовать:

### 1. Получить токен (логин):

**Запрос:**
```http
POST /api/auth/login
Content-Type: application/json

{
  "email": "user@example.com",
  "password": "password123"
}
```

**Ответ:**
```json
{
  "token": "eyJhbGci...",
  "expires_in": 3600  // время жизни токена в секундах
}
```

**Что делать:**
- Сохранить токен из ответа
- Использовать токен в последующих запросах
- Учитывать время истечения (`expires_in`)

---

### 2. Использовать токен в запросах:

**Запрос с токеном:**
```http
GET /api/users/profile
Authorization: Bearer eyJhbGci...
```

**Формат заголовка:**
```
Authorization: Bearer <token>
```

**Особенности:**
- Токен передается в заголовке `Authorization`
- Префикс `Bearer` обязателен
- Пробел между `Bearer` и токеном обязателен

---

## ✅ Что проверяем:

### ✓ Без токена → 401 Unauthorized

**Проверка:**
- Отправить запрос без заголовка `Authorization`
- Ожидаемый статус: `401 Unauthorized`

**Пример:**
```http
GET /api/users/1/orders
→ 401 Unauthorized
```

**Что проверять:**
- ✅ Статус код `401`
- ✅ Сообщение об ошибке (если есть)
- ✅ Защита эндпоинта работает

---

### ✓ С невалидным токеном → 401

**Проверка:**
- Отправить запрос с неправильным токеном
- Ожидаемый статус: `401 Unauthorized`

**Пример:**
```http
GET /api/users/1/orders
Authorization: Bearer invalid_token_12345
→ 401 Unauthorized
```

**Что проверять:**
- ✅ Статус код `401`
- ✅ Сообщение об ошибке (например, "Invalid token")
- ✅ Валидация токена работает

---

### ✓ С истекшим токеном → 401

**Проверка:**
- Отправить запрос с истекшим токеном
- Ожидаемый статус: `401 Unauthorized`

**Пример:**
```http
GET /api/users/1/orders
Authorization: Bearer <expired_token>
→ 401 Unauthorized

{
  "error": "Token expired"
}
```

**Что проверять:**
- ✅ Статус код `401`
- ✅ Сообщение об ошибке "Token expired"
- ✅ Проверка времени истечения работает

---

### ✓ С валидным токеном → 200 OK

**Проверка:**
- Отправить запрос с валидным токеном
- Ожидаемый статус: `200 OK`

**Пример:**
```http
GET /api/users/profile
Authorization: Bearer <valid_token>
→ 200 OK

{
  "id": 1,
  "name": "John",
  "email": "john@example.com"
}
```

**Что проверять:**
- ✅ Статус код `200`
- ✅ Данные возвращаются корректно
- ✅ Авторизация работает правильно

---

### ✓ Доступ к чужим данным → 403 Forbidden

**Проверка:**
- Попытаться получить доступ к данным другого пользователя
- Ожидаемый статус: `403 Forbidden`

**Пример:**
```http
GET /api/users/1/orders
Authorization: Bearer <token_user_2>
→ 403 Forbidden (чужие заказы)
```

**Что проверять:**
- ✅ Статус код `403`
- ✅ Доступ запрещен к чужим данным
- ✅ Проверка прав доступа работает

---

## 📊 Примеры:

### Пример 1: Запрос без токена

**Запрос:**
```http
GET /api/users/1/orders
```

**Ответ:**
```http
HTTP/1.1 401 Unauthorized
Content-Type: application/json

{
  "error": "Unauthorized",
  "message": "Token is required"
}
```

**Проверка:**
- ✅ Статус код `401`
- ✅ Сообщение об ошибке присутствует

---

### Пример 2: Запрос с токеном другого пользователя

**Запрос:**
```http
GET /api/users/1/orders
Authorization: Bearer <token_user_2>
```

**Ответ:**
```http
HTTP/1.1 403 Forbidden
Content-Type: application/json

{
  "error": "Forbidden",
  "message": "Access denied"
}
```

**Проверка:**
- ✅ Статус код `403`
- ✅ Доступ к чужим данным запрещен

---

### Пример 3: Запрос с токеном владельца данных

**Запрос:**
```http
GET /api/users/1/orders
Authorization: Bearer <token_user_1>
```

**Ответ:**
```http
HTTP/1.1 200 OK
Content-Type: application/json

[
  {
    "id": 1,
    "user_id": 1,
    "total": 100.00
  }
]
```

**Проверка:**
- ✅ Статус код `200`
- ✅ Данные возвращаются корректно
- ✅ Доступ к своим данным разрешен

---

### Пример 4: Запрос с истекшим токеном

**Запрос:**
```http
GET /api/users/1/orders
Authorization: Bearer <expired_token>
```

**Ответ:**
```http
HTTP/1.1 401 Unauthorized
Content-Type: application/json

{
  "error": "Token expired",
  "message": "Please login again"
}
```

**Проверка:**
- ✅ Статус код `401`
- ✅ Сообщение "Token expired"
- ✅ Предложение повторного логина

---

## 💡 Ключевые моменты

> **📌 Запомните:**

- **JWT = header.payload.signature** — структура токена из трех частей
- **Передается в заголовке Authorization: Bearer <token>** — формат передачи токена
- **Без токена → 401, с чужим доступом → 403** — основные статус коды безопасности
- **Проверяй истечение токена (exp)** — важно проверять время жизни токена

---

## 🔍 Дополнительные проверки:

### Проверка ролей:

**Пример:**
```http
GET /api/admin/users
Authorization: Bearer <token_user>
→ 403 Forbidden (недостаточно прав)
```

**Что проверять:**
- ✅ Пользователь с ролью `user` не может получить доступ к админским эндпоинтам
- ✅ Только пользователи с ролью `admin` имеют доступ

---

### Проверка обновления токена:

**Refresh Token:**
```http
POST /api/auth/refresh
Authorization: Bearer <refresh_token>
→ 200 OK

{
  "token": "new_token...",
  "expires_in": 3600
}
```

**Что проверять:**
- ✅ Можно обновить токен с помощью refresh token
- ✅ Новый токен возвращается в ответе
- ✅ Старый токен больше не работает (если требуется)

---

## 📋 Best Practices:

### ✅ Рекомендации:

1. **Всегда проверяйте авторизацию** — для защищенных эндпоинтов
2. **Проверяйте истечение токена** — тестируйте с истекшими токенами
3. **Проверяйте права доступа** — доступ к чужим данным должен быть запрещен
4. **Проверяйте валидацию токена** — невалидные токены должны отклоняться
5. **Используйте разные токены** — для разных пользователей и ролей
6. **Проверяйте сообщения об ошибках** — они должны быть информативными
7. **Тестируйте граничные случаи** — отсутствие токена, пустой токен и т.д.

---

## 🎯 Чек-лист проверки безопасности:

- [ ] Запрос без токена возвращает `401`
- [ ] Запрос с невалидным токеном возвращает `401`
- [ ] Запрос с истекшим токеном возвращает `401`
- [ ] Запрос с валидным токеном возвращает `200`
- [ ] Доступ к чужим данным возвращает `403`
- [ ] Проверка ролей работает корректно
- [ ] Сообщения об ошибках информативны
- [ ] Токен передается в заголовке `Authorization: Bearer`

---

[← Вернуться к содержанию раздела](./README.md)
