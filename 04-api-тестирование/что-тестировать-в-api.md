# ✅ Что тестировать в API

> Основные аспекты проверки API.

---

## 🧪 Что проверяем в API

Проверяем, как отработал бэкенд и какой ответ пришёл.

---

## 1) 📩 Ответ (Response)

- **Статус код**: `200`, `201`, `400`, `404`, `500` (и др. по контракту)
- **Тело ответа (body)**: корректные данные, структура, типы, обязательные поля
- **Заголовки (headers)**: `Content-Type`, `Authorization` (если применимо), `Location`, `Set-Cookie` и др.

---

## 2) 🧩 Опционально: побочные эффекты (Side effects)

В зависимости от операции и архитектуры — важно проверять, что произошли ожидаемые действия:

- **Данные записались в БД**
- **Сообщение отправилось в Kafka/очереди** (если используется)
- **Файл сохранился на диск/в хранилище**
- **Email/SMS отправлен**

---

## 🧾 Пример (создание пользователя)

### Запрос

```http
POST /api/users
Content-Type: application/json

{
  "name": "John",
  "email": "john@example.com"
}
```

### Проверяем

- ✅ **Статус код**: `201 Created`
- ✅ **Тело ответа**, например:

```json
{
  "id": 1,
  "name": "John",
  "email": "john@example.com",
  "created_at": "2024-01-15T10:00:00Z"
}
```

- ✅ **Заголовки**, например:
  - `Content-Type: application/json`
  - `Location: /api/users/1`

### Опционально

- ✅ **В БД**:

```sql
SELECT * FROM users WHERE id = 1;
```

- ✅ **В Kafka/очереди** (пример):

```text
Topic: user.created
Message: {"user_id": 1, "event": "created"}
```

---

## 💡 Ключевые моменты

- **Проверяй статус код, тело и заголовки ответа**
- **Опционально**: данные в БД
- **Опционально**: сообщения в Kafka/очередях
- **Опционально**: другие побочные эффекты

[← Вернуться к содержанию раздела](./README.md)

