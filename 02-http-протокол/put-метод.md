# 🔄 PUT метод

> Полная замена ресурса.

---

## 🔍 PUT — заменить ресурс полностью

**Назначение:** Полная замена существующего ресурса.

**Характеристика:** Заменяет весь ресурс целиком.

---

## 📝 Пример запроса:

```http
PUT /api/users/1 HTTP/1.1
Host: api.example.com
Content-Type: application/json
Authorization: Bearer token123

{
  "name": "John Updated",
  "email": "new@example.com",
  "role": "admin"
}
```

**Ответ:**
```http
HTTP/1.1 200 OK
Content-Type: application/json

{
  "id": 1,
  "name": "John Updated",
  "email": "new@example.com",
  "role": "admin"
}
```

---

## ⚠️ Важно: PUT заменяет ВСЕ поля!

### Пример полной замены:

**Было:**
```json
{
  "id": 1,
  "name": "John",
  "email": "old@example.com",
  "role": "user"
}
```

**Запрос PUT:**
```http
PUT /api/users/1 HTTP/1.1
Content-Type: application/json

{
  "name": "John Updated"
}
```

**Стало:**
```json
{
  "id": 1,
  "name": "John Updated"
}
```

**⚠️ Важно:** `email` и `role` удалены, так как не были переданы в запросе!

---

## ⚙️ Особенности:

### ✅ Полная замена ресурса

- PUT запросы **заменяют** весь ресурс целиком
- Все поля, которые не переданы, будут **удалены** или установлены в значения по умолчанию
- Ресурс должен **существовать** (или создается, если поддерживается)

---

### 📋 Все поля обязательны

**Принцип:** Для полной замены нужно передать все поля ресурса.

**Правильный подход:**
1. Сначала получить текущее состояние ресурса (GET)
2. Изменить нужные поля
3. Отправить PUT со всеми полями

**Неправильный подход:**
- Отправка только измененных полей → остальные удалятся!

---

### 🔄 Идемпотентный

**Определение:** Повторный запрос дает тот же результат.

**Пример:**
```
PUT /api/users/1 → {id: 1, name: "John"}
PUT /api/users/1 → {id: 1, name: "John"}  // Тот же результат
PUT /api/users/1 → {id: 1, name: "John"}  // Тот же результат
```

**Преимущества:**
- Безопасно повторять запрос
- Можно использовать для синхронизации
- Предсказуемое поведение

---

### ✅ Возвращает 200 OK

**Статус код:** `200 OK` — стандартный ответ при успешном обновлении.

**Альтернативные статусы:**
- `201 Created` — если ресурс был создан (не существовал)
- `204 No Content` — если обновление успешно, но тело ответа пустое
- `400 Bad Request` — если данные невалидны
- `404 Not Found` — если ресурс не существует

---

## 💡 Ключевые моменты

> **📌 Запомните:**

- **Полная замена ресурса** — PUT заменяет весь ресурс, а не отдельные поля
- **Все поля обязательны** — нужно передавать все поля, иначе они удалятся
- **Идемпотентный** — повторный запрос дает тот же результат
- **Возвращает 200 OK** — стандартный статус код успешного обновления

---

## 🎯 Когда использовать PUT:

### ✅ Подходит для:

- Полная замена ресурса
- Обновление всех полей объекта
- Синхронизация состояния ресурса
- Когда нужно гарантировать полное состояние ресурса

### ❌ Не подходит для:

- Частичное обновление (используйте PATCH)
- Создание новых ресурсов (используйте POST)
- Обновление только одного поля

---

## 📊 Примеры использования:

### Пример 1: Полное обновление пользователя

**Шаг 1: Получить текущее состояние**
```http
GET /api/users/1 HTTP/1.1
```

**Ответ:**
```json
{
  "id": 1,
  "name": "John",
  "email": "john@example.com",
  "role": "user",
  "phone": "+1234567890"
}
```

**Шаг 2: Обновить с изменением email**
```http
PUT /api/users/1 HTTP/1.1
Content-Type: application/json

{
  "name": "John",
  "email": "john.new@example.com",
  "role": "user",
  "phone": "+1234567890"
}
```

**Важно:** Переданы все поля, включая неизмененные!

---

### Пример 2: Ошибка — неполная замена

**Неправильно:**
```http
PUT /api/users/1 HTTP/1.1
Content-Type: application/json

{
  "email": "new@example.com"
}
```

**Результат:** Все остальные поля (`name`, `role`, `phone`) будут удалены!

**Правильно:** Использовать PATCH для частичного обновления.

---

## 🔄 Сравнение PUT и PATCH:

| Характеристика | PUT | PATCH |
|----------------|-----|-------|
| **Назначение** | Полная замена | Частичное обновление |
| **Поля** | Все поля обязательны | Только измененные |
| **Идемпотентность** | ✅ Да | ⚠️ Зависит от реализации |
| **Безопасность** | Может удалить поля | Обновляет только указанные |

---

## 🧪 Тестирование PUT запросов:

### Что проверять:

- ✅ Успешное обновление ресурса (200 OK)
- ✅ Полная замена всех полей
- ✅ Удаление полей, не переданных в запросе
- ✅ Идемпотентность (повторный запрос дает тот же результат)
- ✅ Обработка несуществующего ресурса (404 или 201)
- ✅ Валидация данных (400 при невалидных данных)
- ✅ Проверка, что все обязательные поля переданы

---

## ⚠️ Частые ошибки:

### ❌ Ошибка 1: Частичное обновление через PUT

**Неправильно:**
```http
PUT /api/users/1 HTTP/1.1
Content-Type: application/json

{
  "name": "New Name"
  // Остальные поля не переданы → удалятся!
}
```

**Правильно:** Использовать PATCH для частичного обновления.

---

### ❌ Ошибка 2: Использование PUT для создания

**Неправильно:** PUT для создания новых ресурсов (если не поддерживается).

**Правильно:** Использовать POST для создания.

---

## 🔒 Безопасность:

### ⚠️ Важно помнить:

- **Валидация всех полей** — проверяйте все обязательные поля
- **Проверка существования** — убедитесь, что ресурс существует
- **Защита от потери данных** — предупреждайте о полной замене
- **Авторизация** — проверяйте права на изменение ресурса

---

## 📋 Best Practices:

### ✅ Рекомендации:

1. **Всегда передавайте все поля** при использовании PUT
2. **Используйте PATCH** для частичного обновления
3. **Проверяйте существование ресурса** перед PUT
4. **Документируйте поведение** — объясняйте, что PUT заменяет весь ресурс
5. **Используйте 204 No Content** если не нужно возвращать обновленный ресурс

---

[← Вернуться к содержанию раздела](./README.md)
