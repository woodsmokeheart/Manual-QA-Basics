# 📥 GET метод

> Получение данных с сервера.

---

## 🔍 GET - получить данные

**Назначение:** Получение данных с сервера без изменения состояния.

**Характеристика:** Только чтение данных.

---

## 📝 Пример запроса:

### Базовый запрос:

```http
GET /api/users/1 HTTP/1.1
Host: api.example.com
```

**Ответ:**
```http
HTTP/1.1 200 OK
Content-Type: application/json

{
  "id": 1,
  "name": "John",
  "email": "john@example.com"
}
```

---

### С параметрами (Query Parameters):

```http
GET /api/users?status=active&page=2 HTTP/1.1
Host: api.example.com
```

**Разбор URL:**
- **Путь:** `/api/users`
- **Параметры:** `?status=active&page=2`
  - `status=active` — фильтр по статусу
  - `page=2` — номер страницы

**Ответ:**
```http
HTTP/1.1 200 OK
Content-Type: application/json

{
  "data": [...],
  "page": 2,
  "total": 100
}
```

---

## ⚙️ Особенности:

### ✅ Только чтение данных

- GET запросы **не изменяют** состояние сервера
- Используются для **получения** информации
- Безопасны для повторного выполнения

---

### 📋 Параметры в URL

**Формат:** `?параметр1=значение1&параметр2=значение2`

**Примеры:**
- `GET /api/users?limit=10`
- `GET /api/products?category=electronics&sort=price`
- `GET /api/search?q=test&page=1`

**Ограничения:**
- Максимальная длина URL (обычно 2048 символов)
- Параметры видны в истории браузера
- Не подходят для чувствительных данных

---

### 🔄 Идемпотентный

**Определение:** Повторный запрос дает тот же результат.

**Пример:**
```
GET /api/users/1 → {id: 1, name: "John"}
GET /api/users/1 → {id: 1, name: "John"}  // Тот же результат
GET /api/users/1 → {id: 1, name: "John"}  // Тот же результат
```

**Преимущества:**
- Безопасно повторять запрос
- Можно использовать кеширование
- Не вызывает побочных эффектов

---

### 💾 Можно кешировать

**Кеширование:** Сохранение ответа для повторного использования.

**Преимущества:**
- Уменьшение нагрузки на сервер
- Более быстрые ответы
- Экономия трафика

**Заголовки для кеширования:**
- `Cache-Control` — управление кешем
- `ETag` — тег версии ресурса
- `Last-Modified` — дата последнего изменения

---

## 💡 Ключевые моменты

> **📌 Запомните:**

- **Только чтение данных** — GET не изменяет состояние сервера
- **Параметры в URL** — данные передаются через query string
- **Идемпотентный** — повторный запрос дает тот же результат
- **Можно кешировать** — ответы можно сохранять для повторного использования

---

## 🎯 Когда использовать GET:

### ✅ Подходит для:

- Получение списка ресурсов
- Получение конкретного ресурса по ID
- Поиск с параметрами фильтрации
- Пагинация (page, limit)
- Сортировка (sort, order)

### ❌ Не подходит для:

- Отправка чувствительных данных (пароли, токены)
- Создание новых ресурсов
- Изменение данных
- Удаление данных
- Большие объемы данных в параметрах

---

## 📊 Примеры использования:

### Пример 1: Получение пользователя

```http
GET /api/users/123 HTTP/1.1
Host: api.example.com
Authorization: Bearer token123
```

**Ответ:**
```json
{
  "id": 123,
  "name": "John Doe",
  "email": "john@example.com",
  "created_at": "2024-01-15"
}
```

---

### Пример 2: Список с фильтрацией

```http
GET /api/products?category=electronics&min_price=100&max_price=1000&sort=price&order=asc HTTP/1.1
Host: api.example.com
```

**Параметры:**
- `category=electronics` — фильтр по категории
- `min_price=100` — минимальная цена
- `max_price=1000` — максимальная цена
- `sort=price` — сортировка по цене
- `order=asc` — порядок сортировки (ascending)

---

### Пример 3: Пагинация

```http
GET /api/users?page=2&limit=20 HTTP/1.1
Host: api.example.com
```

**Ответ:**
```json
{
  "data": [...],
  "pagination": {
    "page": 2,
    "limit": 20,
    "total": 150,
    "pages": 8
  }
}
```

---

## 🔒 Безопасность:

### ⚠️ Важно помнить:

- **Параметры видны в URL** — не передавайте пароли, токены
- **История браузера** — GET запросы сохраняются в истории
- **Логирование** — URL может попасть в логи сервера
- **Длина URL** — ограничена (обычно 2048 символов)

### ✅ Рекомендации:

- Используйте GET только для безопасных данных
- Для чувствительных данных используйте POST
- Используйте HTTPS для защиты данных в URL

---

## 🧪 Тестирование GET запросов:

### Что проверять:

- ✅ Корректность получения данных
- ✅ Работа с параметрами (фильтры, сортировка)
- ✅ Обработка несуществующих ресурсов (404)
- ✅ Валидация параметров
- ✅ Кеширование ответов
- ✅ Производительность при больших объемах данных

---

[← Вернуться к содержанию раздела](./README.md)
