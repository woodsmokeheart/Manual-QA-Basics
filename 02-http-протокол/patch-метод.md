# 🔧 PATCH метод

> Частичное обновление ресурса.

---

## 🔍 PATCH — обновить частично

**Назначение:** Частичное обновление существующего ресурса.

**Характеристика:** Обновляет только указанные поля, остальные остаются без изменений.

---

## 📝 Пример запроса:

```http
PATCH /api/users/1 HTTP/1.1
Host: api.example.com
Content-Type: application/json
Authorization: Bearer token123

{
  "email": "new@example.com"
}
```

**Ответ:**
```http
HTTP/1.1 200 OK
Content-Type: application/json

{
  "id": 1,
  "name": "John",
  "email": "new@example.com",
  "role": "user"
}
```

**Результат:** Обновлен только `email`, остальные поля (`name`, `role`) остались без изменений.

---

## 🔄 PATCH vs PUT:

### Пример сравнения:

**Исходное состояние:**
```json
{
  "id": 1,
  "name": "John",
  "email": "old@example.com",
  "role": "user"
}
```

---

### PATCH — частичное обновление:

**Запрос:**
```http
PATCH /api/users/1 HTTP/1.1
Content-Type: application/json

{
  "email": "new@example.com"
}
```

**Результат:**
```json
{
  "id": 1,
  "name": "John",
  "email": "new@example.com",
  "role": "user"
}
```

**✅ Обновлено только email** — остальные поля остались без изменений.

---

### PUT — полная замена:

**Запрос:**
```http
PUT /api/users/1 HTTP/1.1
Content-Type: application/json

{
  "email": "new@example.com"
}
```

**Результат:**
```json
{
  "id": 1,
  "email": "new@example.com"
}
```

**❌ name и role удалены!** — PUT заменяет весь ресурс.

---

## ⚙️ Особенности:

### ✅ Частичное обновление

- PATCH запросы обновляют **только указанные** поля
- Остальные поля остаются **без изменений**
- Не нужно передавать все поля ресурса

---

### 📋 Только указанные поля

**Принцип:** Обновляются только поля, переданные в запросе.

**Примеры:**
- Обновить только email: `{"email": "new@example.com"}`
- Обновить несколько полей: `{"email": "new@example.com", "phone": "+1234567890"}`
- Остальные поля не затрагиваются

---

### ⚠️ Может быть идемпотентным

**Определение:** Зависит от реализации сервера.

**Идемпотентный PATCH:**
```
PATCH /api/users/1 {"email": "new@example.com"}
PATCH /api/users/1 {"email": "new@example.com"}  // Тот же результат
```

**Не идемпотентный PATCH:**
```
PATCH /api/users/1 {"balance": 100}  // Добавить 100
PATCH /api/users/1 {"balance": 100}  // Добавить еще 100 (разный результат!)
```

**Важно:** Идемпотентность зависит от логики обновления.

---

### ✅ Возвращает 200 OK

**Статус код:** `200 OK` — стандартный ответ при успешном обновлении.

**Альтернативные статусы:**
- `204 No Content` — если обновление успешно, но тело ответа пустое
- `400 Bad Request` — если данные невалидны
- `404 Not Found` — если ресурс не существует
- `422 Unprocessable Entity` — если данные валидны, но не могут быть обработаны

---

## 💡 Ключевые моменты

> **📌 Запомните:**

- **Частичное обновление** — PATCH обновляет только указанные поля
- **Только указанные поля** — остальные поля остаются без изменений
- **Может быть идемпотентным** — зависит от реализации сервера
- **Возвращает 200 OK** — стандартный статус код успешного обновления

---

## 🎯 Когда использовать PATCH:

### ✅ Подходит для:

- Частичное обновление ресурса
- Обновление одного или нескольких полей
- Когда не нужно передавать все поля
- Обновление настроек пользователя
- Изменение статуса заказа

### ❌ Не подходит для:

- Полная замена ресурса (используйте PUT)
- Создание новых ресурсов (используйте POST)
- Когда нужно гарантировать полное состояние ресурса

---

## 📊 Примеры использования:

### Пример 1: Обновление email пользователя

**Запрос:**
```http
PATCH /api/users/1 HTTP/1.1
Content-Type: application/json
Authorization: Bearer token123

{
  "email": "john.new@example.com"
}
```

**Ответ:**
```http
HTTP/1.1 200 OK
Content-Type: application/json

{
  "id": 1,
  "name": "John",
  "email": "john.new@example.com",
  "role": "user",
  "phone": "+1234567890"
}
```

**Результат:** Обновлен только email, остальные поля без изменений.

---

### Пример 2: Обновление нескольких полей

**Запрос:**
```http
PATCH /api/users/1 HTTP/1.1
Content-Type: application/json

{
  "email": "new@example.com",
  "phone": "+9876543210",
  "role": "admin"
}
```

**Результат:** Обновлены только указанные поля (`email`, `phone`, `role`).

---

### Пример 3: Изменение статуса заказа

**Запрос:**
```http
PATCH /api/orders/456 HTTP/1.1
Content-Type: application/json

{
  "status": "shipped",
  "tracking_number": "TRACK123456"
}
```

**Результат:** Обновлен статус заказа и добавлен номер отслеживания.

---

## 🔄 Сравнение методов обновления:

| Характеристика | PUT | PATCH |
|----------------|-----|-------|
| **Назначение** | Полная замена | Частичное обновление |
| **Поля** | Все поля обязательны | Только измененные |
| **Остальные поля** | Удаляются | Остаются без изменений |
| **Идемпотентность** | ✅ Всегда | ⚠️ Зависит от реализации |
| **Безопасность** | Может удалить поля | Безопаснее для частичных изменений |

---

## 🧪 Тестирование PATCH запросов:

### Что проверять:

- ✅ Успешное частичное обновление (200 OK)
- ✅ Обновляются только указанные поля
- ✅ Остальные поля остаются без изменений
- ✅ Обновление одного поля
- ✅ Обновление нескольких полей
- ✅ Обработка несуществующего ресурса (404)
- ✅ Валидация данных (400 при невалидных данных)
- ✅ Обновление несуществующего поля (игнорируется или ошибка)

---

## ⚠️ Частые ошибки:

### ❌ Ошибка 1: Использование PUT вместо PATCH

**Неправильно:**
```http
PUT /api/users/1 HTTP/1.1
Content-Type: application/json

{
  "email": "new@example.com"
}
```

**Проблема:** Остальные поля удалятся!

**Правильно:** Использовать PATCH для частичного обновления.

---

### ❌ Ошибка 2: Передача всех полей в PATCH

**Неэффективно:**
```http
PATCH /api/users/1 HTTP/1.1
Content-Type: application/json

{
  "name": "John",
  "email": "new@example.com",
  "role": "user",
  "phone": "+1234567890"
  // Все поля переданы, хотя нужно обновить только email
}
```

**Правильно:** Передавать только изменяемые поля.

---

## 🔒 Безопасность:

### ⚠️ Важно помнить:

- **Валидация полей** — проверяйте каждое поле отдельно
- **Проверка существования** — убедитесь, что ресурс существует
- **Авторизация** — проверяйте права на изменение каждого поля
- **Защита от перезаписи** — проверяйте конфликты при параллельных обновлениях

---

## 📋 Best Practices:

### ✅ Рекомендации:

1. **Используйте PATCH для частичных обновлений** — это стандартный подход
2. **Передавайте только изменяемые поля** — не нужно передавать все поля
3. **Документируйте поведение** — объясняйте, какие поля можно обновлять
4. **Возвращайте обновленный ресурс** — показывайте результат обновления
5. **Используйте 204 No Content** если не нужно возвращать ресурс

---

## 🔄 Идемпотентность PATCH:

### Варианты реализации:

**Вариант 1: Идемпотентный (рекомендуется)**
```http
PATCH /api/users/1 {"email": "new@example.com"}
→ email = "new@example.com"

PATCH /api/users/1 {"email": "new@example.com"}
→ email = "new@example.com"  // Тот же результат
```

**Вариант 2: Не идемпотентный (добавление)**
```http
PATCH /api/users/1 {"balance": 100}  // Добавить 100
→ balance += 100

PATCH /api/users/1 {"balance": 100}  // Добавить еще 100
→ balance += 100  // Разный результат!
```

**Рекомендация:** Реализуйте PATCH как идемпотентный для предсказуемости.

---

[← Вернуться к содержанию раздела](./README.md)
