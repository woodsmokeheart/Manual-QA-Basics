# 📤 POST метод

> Создание новых ресурсов.

---

## 🔍 POST — создать ресурс

**Назначение:** Создание новых ресурсов на сервере.

**Характеристика:** Изменяет состояние сервера (создает новый ресурс).

---

## 📝 Пример запроса:

```http
POST /api/users HTTP/1.1
Host: api.example.com
Content-Type: application/json
Authorization: Bearer token123

{
  "name": "John",
  "email": "john@example.com"
}
```

**Ответ:**
```http
HTTP/1.1 201 Created
Content-Type: application/json
Location: /api/users/123

{
  "id": 123,
  "name": "John",
  "email": "john@example.com",
  "created_at": "2024-01-15T10:30:00Z"
}
```

---

## ⚙️ Особенности:

### ✅ Создает новый ресурс

- POST запросы **создают** новые ресурсы на сервере
- Каждый запрос создает **новый** объект
- Сервер генерирует уникальный ID для нового ресурса

---

### 📋 Данные в body

**Формат:** Данные передаются в теле запроса (body), а не в URL.

**Типы данных:**
- JSON (`Content-Type: application/json`)
- XML (`Content-Type: application/xml`)
- Form Data (`Content-Type: application/x-www-form-urlencoded`)
- Multipart Form (`Content-Type: multipart/form-data`)

**Пример JSON:**
```json
{
  "name": "John",
  "email": "john@example.com",
  "password": "SecurePass123!"
}
```

---

### ❌ НЕ идемпотентный

**Определение:** Повторный запрос создает новый ресурс каждый раз.

**Пример:**
```
POST /api/users → {id: 123, name: "John"}
POST /api/users → {id: 124, name: "John"}  // Новый ID!
POST /api/users → {id: 125, name: "John"}  // Еще один новый ID!
```

**Важно:**
- Каждый запрос создает новый ресурс
- Нельзя безопасно повторять без проверки
- Может привести к дублированию данных

---

### ✅ Возвращает 201 Created

**Статус код:** `201 Created` — стандартный ответ при успешном создании.

**Заголовок Location:**
- Указывает на созданный ресурс
- Формат: `Location: /api/users/123`

**Альтернативные статусы:**
- `200 OK` — если ресурс создан и возвращается в ответе
- `202 Accepted` — если запрос принят, но обработка асинхронная
- `400 Bad Request` — если данные невалидны
- `409 Conflict` — если ресурс уже существует

---

## 💡 Ключевые моменты

> **📌 Запомните:**

- **Создает новый ресурс** — POST используется для создания новых объектов
- **Данные в body** — информация передается в теле запроса, а не в URL
- **НЕ идемпотентный** — каждый запрос создает новый ресурс с новым ID
- **Возвращает 201 Created** — стандартный статус код успешного создания

---

## 🎯 Когда использовать POST:

### ✅ Подходит для:

- Создание новых ресурсов (пользователи, заказы, статьи)
- Отправка данных формы
- Загрузка файлов
- Выполнение действий (отправка email, запуск процесса)
- Отправка чувствительных данных (пароли, токены)

### ❌ Не подходит для:

- Получение данных (используйте GET)
- Обновление существующих ресурсов (используйте PUT/PATCH)
- Удаление ресурсов (используйте DELETE)
- Идемпотентные операции

---

## 📊 Примеры использования:

### Пример 1: Создание пользователя

**Запрос:**
```http
POST /api/users HTTP/1.1
Host: api.example.com
Content-Type: application/json

{
  "name": "John Doe",
  "email": "john@example.com",
  "password": "SecurePass123!"
}
```

**Ответ:**
```http
HTTP/1.1 201 Created
Content-Type: application/json
Location: /api/users/123

{
  "id": 123,
  "name": "John Doe",
  "email": "john@example.com",
  "created_at": "2024-01-15T10:30:00Z"
}
```

---

### Пример 2: Создание заказа

**Запрос:**
```http
POST /api/orders HTTP/1.1
Host: api.example.com
Content-Type: application/json
Authorization: Bearer token123

{
  "user_id": 123,
  "items": [
    {"product_id": 1, "quantity": 2},
    {"product_id": 5, "quantity": 1}
  ],
  "shipping_address": "123 Main St"
}
```

**Ответ:**
```http
HTTP/1.1 201 Created
Content-Type: application/json
Location: /api/orders/456

{
  "id": 456,
  "user_id": 123,
  "status": "pending",
  "total": 299.99,
  "created_at": "2024-01-15T10:30:00Z"
}
```

---

### Пример 3: Отправка формы (Form Data)

**Запрос:**
```http
POST /api/contact HTTP/1.1
Host: api.example.com
Content-Type: application/x-www-form-urlencoded

name=John+Doe&email=john%40example.com&message=Hello
```

**Ответ:**
```http
HTTP/1.1 200 OK
Content-Type: application/json

{
  "success": true,
  "message": "Message sent successfully"
}
```

---

## 🔒 Безопасность:

### ⚠️ Важно помнить:

- **Валидация данных** — всегда проверяйте входные данные на сервере
- **Защита от CSRF** — используйте токены для защиты форм
- **Ограничение размера** — ограничивайте размер тела запроса
- **Rate limiting** — ограничивайте количество запросов

### ✅ Рекомендации:

- Используйте HTTPS для защиты данных
- Валидируйте все входные данные
- Не логируйте чувствительные данные (пароли, токены)
- Используйте Content-Type для указания формата данных

---

## 🧪 Тестирование POST запросов:

### Что проверять:

- ✅ Успешное создание ресурса (201 Created)
- ✅ Корректность данных в ответе
- ✅ Заголовок Location указывает на новый ресурс
- ✅ Валидация данных (невалидные данные → 400)
- ✅ Дублирование данных (если применимо)
- ✅ Обработка ошибок (500, 400, 409)
- ✅ Размер тела запроса (ограничения)

---

## 🔄 Сравнение с GET:

| Характеристика | GET | POST |
|----------------|-----|------|
| **Назначение** | Получение данных | Создание ресурсов |
| **Данные** | В URL (query params) | В body |
| **Идемпотентность** | ✅ Да | ❌ Нет |
| **Кеширование** | ✅ Да | ❌ Нет |
| **Безопасность** | Параметры видны | Данные скрыты |
| **Статус код** | 200 OK | 201 Created |

---

[← Вернуться к содержанию раздела](./README.md)
