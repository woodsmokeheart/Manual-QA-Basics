# 🎯 REST API принципы

> Основы RESTful архитектуры.

---

## 🔍 REST (Representational State Transfer)

**Определение:** Архитектурный стиль для проектирования веб-сервисов.

**Основная идея:** Использование стандартных HTTP методов для работы с ресурсами через URL.

---

## 📋 Принципы:

### 1. Ресурсы идентифицируются URL

**Принцип:** Каждый ресурс имеет уникальный URL.

**Примеры:**
```
GET /api/users          → список пользователей
GET /api/users/1        → пользователь с ID 1
GET /api/users/1/posts   → посты пользователя 1
GET /api/posts/5         → пост с ID 5
```

**Важно:**
- ✅ URL описывает ресурс, а не действие
- ✅ Иерархическая структура ресурсов
- ✅ Понятные и предсказуемые URL

---

### 2. Стандартные HTTP методы

**Принцип:** Использование стандартных HTTP методов для операций.

**Соответствие:**
- **GET** — получение данных
- **POST** — создание ресурса
- **PUT** — полная замена ресурса
- **PATCH** — частичное обновление
- **DELETE** — удаление ресурса

**Важно:**
- ✅ Используйте стандартные методы
- ✅ Метод определяет действие, URL — ресурс
- ✅ Не изобретайте свои методы

---

### 3. Stateless — каждый запрос независим

**Принцип:** Сервер не хранит состояние между запросами.

**Характеристики:**
- Каждый запрос содержит всю необходимую информацию
- Сервер не помнит предыдущие запросы
- Состояние хранится на клиенте (куки, токены)

**Пример:**
```
Запрос 1: GET /api/users (с токеном)
Запрос 2: GET /api/posts (с токеном)
→ Каждый запрос независим, сервер не помнит предыдущий
```

**Преимущества:**
- ✅ Масштабируемость
- ✅ Простота кеширования
- ✅ Отказоустойчивость

---

## 🔄 CRUD операции:

### Create → POST

**Создание нового ресурса:**

```http
POST /api/users HTTP/1.1
Content-Type: application/json

{
  "name": "John",
  "email": "john@example.com"
}
```

**Ответ:**
```http
HTTP/1.1 201 Created
Location: /api/users/123
```

---

### Read → GET

**Получение ресурса:**

```http
GET /api/users/1 HTTP/1.1
```

**Ответ:**
```http
HTTP/1.1 200 OK
Content-Type: application/json

{
  "id": 1,
  "name": "John",
  "email": "john@example.com"
}
```

---

### Update → PUT

**Полная замена ресурса:**

```http
PUT /api/users/1 HTTP/1.1
Content-Type: application/json

{
  "name": "John Updated",
  "email": "john.new@example.com"
}
```

**Ответ:**
```http
HTTP/1.1 200 OK
```

---

### Delete → DELETE

**Удаление ресурса:**

```http
DELETE /api/users/1 HTTP/1.1
```

**Ответ:**
```http
HTTP/1.1 204 No Content
```

---

## ✅ Хорошие практики:

### ✅ GET /api/users (множественное число)

**Правильно:**
```http
GET /api/users      → список пользователей
GET /api/posts      → список постов
GET /api/products   → список товаров
```

**Почему:**
- URL описывает коллекцию ресурсов
- Понятно, что возвращается список
- Соответствует REST принципам

---

### ✅ GET /api/users/1/posts (вложенные ресурсы)

**Правильно:**
```http
GET /api/users/1/posts        → посты пользователя 1
GET /api/users/1/posts/5       → пост 5 пользователя 1
GET /api/orders/123/items      → товары заказа 123
```

**Почему:**
- Иерархическая структура ресурсов
- Понятная связь между ресурсами
- Логичная организация API

---

### ✅ GET /api/users?status=active (фильтры)

**Правильно:**
```http
GET /api/users?status=active
GET /api/users?role=admin&page=2
GET /api/products?category=electronics&min_price=100
```

**Почему:**
- Используются query параметры
- Не изменяет структуру URL
- Стандартный подход к фильтрации

---

### ✅ GET /api/v1/users (версионирование)

**Правильно:**
```http
GET /api/v1/users
GET /api/v2/users
GET /api/v1/users/1
```

**Почему:**
- Версионирование в URL
- Обратная совместимость
- Понятная эволюция API

---

## ❌ Плохие практики:

### ❌ POST /api/createUser (глаголы в URL)

**Неправильно:**
```http
POST /api/createUser
POST /api/deleteUser
POST /api/updateUser
GET /api/getUsers
```

**Почему плохо:**
- HTTP метод уже указывает действие
- Нарушает REST принципы
- Избыточность информации

**Правильно:**
```http
POST /api/users      → создание
DELETE /api/users/1  → удаление
PUT /api/users/1     → обновление
GET /api/users       → получение
```

---

### ❌ GET /api/user (единственное число)

**Неправильно:**
```http
GET /api/user        → неясно, один или список?
GET /api/user/1      → несоответствие с коллекцией
```

**Почему плохо:**
- Неясно, что возвращается
- Несоответствие между коллекцией и ресурсом
- Нарушает консистентность

**Правильно:**
```http
GET /api/users       → список
GET /api/users/1     → конкретный пользователь
```

---

## 💡 Ключевые моменты

> **📌 Запомните:**

- **REST = ресурсы + HTTP методы** — ресурсы в URL, действия через HTTP методы
- **URL идентифицирует ресурс** — URL описывает что, метод описывает как
- **Stateless - нет состояния** — каждый запрос независим
- **Избегай глаголов в URL** — HTTP метод уже указывает действие

---

## 📊 Структура REST API:

### Иерархия ресурсов:

```
/api                          → корень API
/api/v1                       → версия API
/api/v1/users                 → коллекция пользователей
/api/v1/users/1               → конкретный пользователь
/api/v1/users/1/posts         → посты пользователя
/api/v1/users/1/posts/5       → конкретный пост
```

---

## 🎯 RESTful дизайн:

### Правила именования:

| Ресурс | Коллекция | Элемент |
|--------|-----------|---------|
| Пользователь | `/api/users` | `/api/users/1` |
| Пост | `/api/posts` | `/api/posts/5` |
| Заказ | `/api/orders` | `/api/orders/123` |

---

### Соответствие HTTP методов и операций:

| Операция | HTTP метод | Пример |
|----------|------------|--------|
| Создать | POST | `POST /api/users` |
| Прочитать | GET | `GET /api/users/1` |
| Обновить (полностью) | PUT | `PUT /api/users/1` |
| Обновить (частично) | PATCH | `PATCH /api/users/1` |
| Удалить | DELETE | `DELETE /api/users/1` |

---

## 📋 Дополнительные принципы:

### Единообразие (Uniform Interface)

**Принцип:** Стандартизированный интерфейс для всех ресурсов.

**Характеристики:**
- Единый формат URL
- Стандартные HTTP методы
- Единый формат ответов (JSON)
- Стандартные статус коды

---

### Кешируемость (Cacheable)

**Принцип:** Ответы должны быть кешируемыми.

**Характеристики:**
- GET запросы кешируются
- Использование Cache-Control заголовков
- ETag для проверки изменений
- Оптимизация производительности

---

### Клиент-сервер архитектура

**Принцип:** Разделение клиента и сервера.

**Характеристики:**
- Клиент не знает о хранении данных
- Сервер не знает о UI клиента
- Независимое развитие клиента и сервера

---

### Многоуровневая система (Layered System)

**Принцип:** Система может состоять из нескольких уровней.

**Характеристики:**
- Прокси, балансировщики, кеши
- Клиент не знает о промежуточных уровнях
- Улучшение производительности и безопасности

---

## 🧪 Тестирование REST API:

### Что проверять:

- ✅ Правильность URL структуры
- ✅ Соответствие HTTP методов операциям
- ✅ Stateless поведение (независимость запросов)
- ✅ Правильные статус коды
- ✅ Единообразие формата ответов
- ✅ Работа фильтров через query параметры
- ✅ Вложенные ресурсы работают корректно
- ✅ Версионирование API

---

## 📋 Best Practices:

### ✅ Рекомендации:

1. **Используйте множественное число** — для коллекций (`/api/users`)
2. **Избегайте глаголов в URL** — HTTP метод уже указывает действие
3. **Используйте вложенные ресурсы** — для связанных данных
4. **Версионируйте API** — `/api/v1/`, `/api/v2/`
5. **Используйте стандартные статус коды** — 200, 201, 404, 500
6. **Возвращайте JSON** — единый формат ответов
7. **Используйте фильтры через query** — `?status=active`
8. **Документируйте API** — Swagger/OpenAPI

---

## 🔄 Примеры RESTful API:

### Пример 1: Управление пользователями

```http
# Получить список пользователей
GET /api/v1/users

# Получить конкретного пользователя
GET /api/v1/users/1

# Создать пользователя
POST /api/v1/users
Body: {"name": "John", "email": "john@example.com"}

# Обновить пользователя
PUT /api/v1/users/1
Body: {"name": "John Updated", "email": "john.new@example.com"}

# Удалить пользователя
DELETE /api/v1/users/1
```

---

### Пример 2: Вложенные ресурсы

```http
# Получить посты пользователя
GET /api/v1/users/1/posts

# Получить конкретный пост пользователя
GET /api/v1/users/1/posts/5

# Создать пост для пользователя
POST /api/v1/users/1/posts
Body: {"title": "New Post", "content": "..."}
```

---

### Пример 3: Фильтрация и пагинация

```http
# Фильтрация
GET /api/v1/users?status=active&role=admin

# Пагинация
GET /api/v1/users?page=2&limit=20

# Сортировка
GET /api/v1/users?sort=name&order=asc

# Комбинация
GET /api/v1/users?status=active&page=1&limit=10&sort=created_at&order=desc
```

---

## ⚠️ Частые ошибки:

### ❌ Ошибка 1: Глаголы в URL

**Неправильно:**
```http
POST /api/createUser
GET /api/getUsers
DELETE /api/removeUser
```

**Правильно:**
```http
POST /api/users
GET /api/users
DELETE /api/users/1
```

---

### ❌ Ошибка 2: Действия через GET

**Неправильно:**
```http
GET /api/users/1/delete
GET /api/users/1/activate
```

**Правильно:**
```http
DELETE /api/users/1
PATCH /api/users/1
Body: {"status": "active"}
```

---

### ❌ Ошибка 3: Хранение состояния на сервере

**Неправильно:**
```http
# Запрос 1: Установить фильтр
POST /api/setFilter
Body: {"status": "active"}

# Запрос 2: Получить отфильтрованных пользователей
GET /api/users  # Сервер помнит фильтр
```

**Правильно:**
```http
# Один запрос с фильтром
GET /api/users?status=active
```

---

[← Вернуться к содержанию раздела](./README.md)
