# 📋 HEAD метод

> Получение заголовков без тела ответа.

---

## 🔍 HEAD — получить только заголовки

**Назначение:** Получение метаданных ресурса без тела ответа.

**Характеристика:** Работает как GET, но без тела ответа.

---

## 📝 Пример запроса:

```http
HEAD /api/users/1 HTTP/1.1
Host: api.example.com
```

**Ответ:**
```http
HTTP/1.1 200 OK
Content-Type: application/json
Content-Length: 156
Last-Modified: Mon, 15 Jan 2024 10:30:00 GMT
ETag: "abc123"
```

**Результат:** Возвращены только заголовки, тело ответа отсутствует.

---

## ⚙️ Особенности:

### ✅ Работает как GET, но без body

- HEAD запросы возвращают **те же заголовки**, что и GET
- **Тело ответа отсутствует** — экономия трафика
- Используется для **проверки существования** ресурса
- Полезен для **проверки изменений** (ETag, Last-Modified)

---

### 📋 Только заголовки

**Заголовки, которые обычно проверяются:**
- `Content-Type` — тип содержимого
- `Content-Length` — размер ресурса
- `Last-Modified` — дата последнего изменения
- `ETag` — тег версии ресурса
- `Cache-Control` — управление кешем

---

### 🔄 Идемпотентный

**Определение:** Повторный запрос дает тот же результат.

**Пример:**
```
HEAD /api/users/1 → Заголовки (200 OK)
HEAD /api/users/1 → Те же заголовки (200 OK)
```

**Преимущества:**
- Безопасно повторять запрос
- Можно использовать для проверки состояния

---

### ✅ Возвращает те же статусы, что и GET

**Статус коды:**
- `200 OK` — ресурс существует
- `404 Not Found` — ресурс не найден
- `304 Not Modified` — ресурс не изменился (если используется If-Modified-Since)

---

## 💡 Ключевые моменты

> **📌 Запомните:**

- **Работает как GET, но без body** — возвращает только заголовки
- **Только заголовки** — полезно для проверки метаданных
- **Идемпотентный** — повторный запрос дает тот же результат
- **Возвращает те же статусы, что и GET** — стандартные HTTP статусы

---

## 🎯 Когда использовать HEAD:

### ✅ Подходит для:

- Проверка существования ресурса
- Проверка размера файла (Content-Length)
- Проверка изменений (ETag, Last-Modified)
- Проверка типа содержимого (Content-Type)
- Оптимизация трафика (не нужно загружать тело)

### ❌ Не подходит для:

- Получение данных (используйте GET)
- Изменение ресурсов
- Когда нужны данные из body

---

## 📊 Примеры использования:

### Пример 1: Проверка существования файла

**Запрос:**
```http
HEAD /files/document.pdf HTTP/1.1
Host: api.example.com
```

**Ответ:**
```http
HTTP/1.1 200 OK
Content-Type: application/pdf
Content-Length: 1048576
Last-Modified: Mon, 15 Jan 2024 10:30:00 GMT
```

**Результат:** Файл существует, размер 1MB, тип PDF.

---

### Пример 2: Проверка изменений ресурса

**Запрос:**
```http
HEAD /api/users/1 HTTP/1.1
Host: api.example.com
If-None-Match: "abc123"
```

**Ответ (если не изменился):**
```http
HTTP/1.1 304 Not Modified
ETag: "abc123"
```

**Ответ (если изменился):**
```http
HTTP/1.1 200 OK
ETag: "def456"
Last-Modified: Mon, 15 Jan 2024 11:00:00 GMT
```

---

### Пример 3: Проверка размера ресурса

**Запрос:**
```http
HEAD /api/reports/large-report.json HTTP/1.1
Host: api.example.com
```

**Ответ:**
```http
HTTP/1.1 200 OK
Content-Type: application/json
Content-Length: 52428800
```

**Результат:** Размер файла 50MB — можно решить, стоит ли его загружать.

---

## 🔄 Сравнение HEAD и GET:

| Характеристика | HEAD | GET |
|----------------|------|-----|
| **Заголовки** | ✅ Возвращает | ✅ Возвращает |
| **Тело ответа** | ❌ Нет | ✅ Есть |
| **Трафик** | Меньше | Больше |
| **Назначение** | Метаданные | Данные + метаданные |
| **Идемпотентность** | ✅ Да | ✅ Да |

---

## 🧪 Тестирование HEAD запросов:

### Что проверять:

- ✅ Возвращаются правильные заголовки
- ✅ Тело ответа отсутствует
- ✅ Статус коды соответствуют GET
- ✅ Заголовки совпадают с GET запросом
- ✅ Работа с условными заголовками (If-Modified-Since, If-None-Match)
- ✅ Проверка существования ресурса (200 vs 404)

---

## 🔒 Безопасность:

### ⚠️ Важно помнить:

- **HEAD может раскрыть информацию** — заголовки могут содержать метаданные
- **Проверка прав доступа** — HEAD должен проверять те же права, что и GET
- **Логирование** — HEAD запросы могут логироваться

---

## 📋 Best Practices:

### ✅ Рекомендации:

1. **Используйте HEAD для проверки** — существования и метаданных ресурса
2. **Возвращайте те же заголовки, что и GET** — для консистентности
3. **Не возвращайте тело** — даже если оно есть
4. **Используйте условные заголовки** — для оптимизации (If-Modified-Since)
5. **Проверяйте те же права, что и GET** — для безопасности

---

[← Вернуться к содержанию раздела](./README.md)
