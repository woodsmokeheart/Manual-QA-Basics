# 📦 Структура HTTP запроса

> Из чего состоит HTTP запрос и ответ.

---

## 🔍 HTTP ЗАПРОС состоит из:

### 1. Стартовая строка (Request Line)

**Формат:** `МЕТОД ПУТЬ HTTP/ВЕРСИЯ`

**Пример:**
```
GET /api/users HTTP/1.1
```

**Компоненты:**
- **Метод** — действие (GET, POST, PUT, DELETE и др.)
- **Путь** — URL ресурса (`/api/users`)
- **Версия протокола** — обычно `HTTP/1.1` или `HTTP/2`

---

### 2. Заголовки (Headers)

**Назначение:** Метаданные запроса

**Примеры заголовков:**
- `Host` — домен сервера
- `Content-Type` — тип содержимого
- `Authorization` — данные аутентификации
- `User-Agent` — информация о клиенте
- `Accept` — какие типы данных принимает клиент

---

### 3. Пустая строка (разделитель)

**Назначение:** Разделяет заголовки и тело запроса

**Важно:** Обязательна для правильного парсинга запроса

---

### 4. Тело запроса (Body) — опционально

**Назначение:** Данные для отправки на сервер

**Когда используется:**
- POST, PUT, PATCH запросы
- Отправка JSON, XML, файлов

**Не используется:**
- GET, DELETE запросы (обычно)

---

## 📝 Пример HTTP запроса:

```http
POST /api/users HTTP/1.1
Host: api.example.com
Content-Type: application/json
Authorization: Bearer token123
User-Agent: Mozilla/5.0

{
  "name": "John",
  "email": "john@example.com"
}
```

**Разбор:**
- **Стартовая строка:** `POST /api/users HTTP/1.1`
- **Заголовки:** Host, Content-Type, Authorization, User-Agent
- **Пустая строка:** разделитель
- **Тело:** JSON данные

---

## 🔍 HTTP ОТВЕТ состоит из:

### 1. Статус-строка (Status Line)

**Формат:** `HTTP/ВЕРСИЯ КОД ОПИСАНИЕ`

**Пример:**
```
HTTP/1.1 200 OK
```

**Компоненты:**
- **Версия протокола** — `HTTP/1.1` или `HTTP/2`
- **Код статуса** — числовой код (200, 404, 500)
- **Описание** — текстовое описание (OK, Not Found, Internal Server Error)

---

### 2. Заголовки ответа (Response Headers)

**Назначение:** Метаданные ответа

**Примеры заголовков:**
- `Content-Type` — тип содержимого ответа
- `Set-Cookie` — установка cookies
- `Location` — редирект (для 3xx статусов)
- `Cache-Control` — управление кешированием

---

### 3. Пустая строка

**Назначение:** Разделяет заголовки и тело ответа

---

### 4. Тело ответа (Response Body)

**Назначение:** Данные от сервера

**Форматы:**
- JSON
- XML
- HTML
- Текст
- Бинарные данные

---

## 📝 Пример HTTP ответа:

```http
HTTP/1.1 201 Created
Content-Type: application/json
Location: /api/users/1
Set-Cookie: session_id=abc123; Path=/

{
  "id": 1,
  "name": "John",
  "email": "john@example.com"
}
```

**Разбор:**
- **Статус-строка:** `HTTP/1.1 201 Created`
- **Заголовки:** Content-Type, Location, Set-Cookie
- **Пустая строка:** разделитель
- **Тело:** JSON данные

---

## 💡 Ключевые моменты

> **📌 Запомните:**

- **Запрос: метод + путь + версия** — стартовая строка определяет действие
- **Заголовки содержат метаданные** — дополнительная информация о запросе/ответе
- **Пустая строка разделяет headers и body** — обязательна для правильного парсинга
- **Body опционален (зависит от метода)** — GET обычно без body, POST/PUT с body

---

## 📊 Сравнение запроса и ответа:

| Компонент | Запрос | Ответ |
|-----------|--------|-------|
| **Первая строка** | Стартовая строка (метод + путь) | Статус-строка (код + описание) |
| **Заголовки** | Метаданные запроса | Метаданные ответа |
| **Пустая строка** | Разделитель | Разделитель |
| **Тело** | Данные для сервера (опционально) | Данные от сервера |

---

## 🔧 Как просмотреть структуру:

### В браузере (Chrome DevTools):

1. Откройте DevTools (F12)
2. Перейдите на вкладку **Network**
3. Выполните запрос
4. Кликните на запрос
5. Посмотрите вкладки:
   - **Headers** — заголовки и стартовая/статус строка
   - **Payload** — тело запроса
   - **Response** — тело ответа

### В Postman:

1. Откройте запрос
2. Вкладка **Headers** — заголовки
3. Вкладка **Body** — тело запроса
4. После отправки — **Response** показывает ответ

---

## 🎯 Практические советы:

### Для тестирования:

- ✅ **Проверяйте заголовки** — они могут влиять на поведение API
- ✅ **Смотрите статус-коды** — они показывают результат запроса
- ✅ **Анализируйте тело ответа** — там может быть важная информация об ошибках
- ✅ **Используйте DevTools** — визуализация помогает понять структуру

---

[← Вернуться к содержанию раздела](./README.md)
