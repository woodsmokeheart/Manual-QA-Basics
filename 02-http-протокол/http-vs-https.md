# 🔒 HTTP vs HTTPS

> Разница между протоколами.

---

## 🔍 HTTP vs HTTPS

### HTTP (порт 80):

**Характеристики:**
- ❌ Данные открытым текстом
- ❌ Можно перехватить
- ❌ НЕ безопасен для паролей
- ✅ Быстрее (нет шифрования)

**Пример:**
```
http://example.com/api/users
```

**Когда используется:**
- Разработка и тестирование
- Внутренние сети (не рекомендуется)
- Старые системы без поддержки HTTPS

---

### HTTPS (порт 443):

**Характеристики:**
- ✅ Данные зашифрованы (TLS/SSL)
- ✅ Защита от перехвата
- ✅ Обязателен для production
- ⚠️ Медленнее (шифрование)

**Пример:**
```
https://example.com/api/users
```

**Когда используется:**
- Production окружения
- Передача чувствительных данных
- Современные веб-приложения
- API с аутентификацией

---

## 🔐 Что защищает HTTPS:

### 1. Конфиденциальность – данные зашифрованы

**Описание:** Данные передаются в зашифрованном виде.

**Пример:**
```
HTTP:  password=Secret123  → видно всем
HTTPS: password=Secret123  → зашифровано, не видно
```

**Защита:**
- Пароли не видны в открытом виде
- Токены защищены от перехвата
- Личные данные зашифрованы

---

### 2. Целостность – данные не изменены

**Описание:** Гарантия, что данные не были изменены при передаче.

**Защита:**
- Предотвращает модификацию данных
- Обнаруживает подмену пакетов
- Гарантирует, что данные дошли без изменений

**Пример:**
```
HTTP:  Данные могут быть изменены → небезопасно
HTTPS: Данные защищены от изменения → безопасно
```

---

### 3. Аутентификация – сервер подтвержден сертификатом

**Описание:** Сертификат подтверждает подлинность сервера.

**Защита:**
- Подтверждает, что вы общаетесь с правильным сервером
- Защищает от MITM (Man-in-the-Middle) атак
- Доверие к серверу через сертификат

**Пример:**
```
HTTPS: example.com → сертификат подтверждает, что это действительно example.com
HTTP:  example.com → нет подтверждения, может быть подделка
```

---

## ⚠️ Типичные ошибки сертификатов:

### ERR_CERT_DATE_INVALID

**Описание:** Сертификат просрочен.

**Причина:**
- Сертификат истек
- Не обновлен вовремя

**Решение:**
- Обновить сертификат
- Проверить дату окончания действия

**Пример:**
```
Сертификат действителен до: 15.01.2024
Текущая дата: 20.01.2024
→ ERR_CERT_DATE_INVALID
```

---

### ERR_CERT_AUTHORITY_INVALID

**Описание:** Недоверенный центр сертификации (CA).

**Причина:**
- Самоподписанный сертификат
- Сертификат от неизвестного CA
- Корневой сертификат не установлен

**Решение:**
- Использовать сертификат от доверенного CA
- Установить корневой сертификат
- Для тестирования: добавить исключение (небезопасно!)

**Пример:**
```
Самоподписанный сертификат → ERR_CERT_AUTHORITY_INVALID
Let's Encrypt сертификат → ✅ Доверенный
```

---

### ERR_CERT_COMMON_NAME_INVALID

**Описание:** Домен не совпадает с сертификатом.

**Причина:**
- Сертификат выдан для другого домена
- Неправильный домен в запросе
- Использование IP вместо домена

**Решение:**
- Использовать правильный домен
- Обновить сертификат для нужного домена
- Проверить соответствие домена и сертификата

**Пример:**
```
Сертификат для: api.example.com
Запрос к: api.test.com
→ ERR_CERT_COMMON_NAME_INVALID
```

---

## 💡 Ключевые моменты

> **📌 Запомните:**

- **HTTP - открытый текст (небезопасно)** — данные передаются в открытом виде
- **HTTPS - зашифрован (безопасно)** — данные защищены шифрованием
- **HTTPS обязателен для production** — всегда используйте HTTPS в продакшене
- **Сертификат подтверждает сервер** — гарантирует подлинность сервера

---

## 📊 Сравнительная таблица:

| Характеристика | HTTP | HTTPS |
|----------------|------|-------|
| **Порт** | 80 | 443 |
| **Шифрование** | ❌ Нет | ✅ TLS/SSL |
| **Безопасность** | ❌ Небезопасно | ✅ Безопасно |
| **Скорость** | ✅ Быстрее | ⚠️ Медленнее |
| **Сертификат** | ❌ Не требуется | ✅ Требуется |
| **Использование** | Dev/Test | Production |

---

## 🔐 Как работает HTTPS:

### Процесс установления соединения:

1. **Клиент запрашивает HTTPS соединение**
   ```
   Client → Server: Запрос на HTTPS соединение
   ```

2. **Сервер отправляет сертификат**
   ```
   Server → Client: SSL/TLS сертификат
   ```

3. **Клиент проверяет сертификат**
   ```
   Client: Проверка валидности сертификата
   ```

4. **Установление зашифрованного соединения**
   ```
   Client ↔ Server: Зашифрованное соединение установлено
   ```

5. **Безопасная передача данных**
   ```
   Client ↔ Server: Все данные зашифрованы
   ```

---

## 🎯 Когда использовать:

### ✅ Используйте HTTPS для:

- Production окружения
- Передача паролей и токенов
- Личные данные пользователей
- Платежные операции
- API с аутентификацией
- Любые чувствительные данные

### ⚠️ HTTP можно использовать для:

- Локальная разработка (localhost)
- Внутренние тестовые окружения
- Старые системы без поддержки HTTPS
- **НЕ рекомендуется для production!**

---

## 🔒 Безопасность:

### ⚠️ Риски HTTP:

- **Перехват данных** — злоумышленник может видеть все данные
- **Подмена данных** — данные могут быть изменены
- **MITM атаки** — злоумышленник может перехватывать и изменять трафик
- **Кража паролей** — пароли передаются в открытом виде

### ✅ Преимущества HTTPS:

- **Шифрование** — данные не видны злоумышленникам
- **Целостность** — данные не могут быть изменены
- **Аутентификация** — подтверждение подлинности сервера
- **Доверие** — пользователи видят защищенное соединение

---

## 🧪 Тестирование HTTPS:

### Что проверять:

- ✅ HTTPS работает корректно
- ✅ Сертификат валиден и не просрочен
- ✅ Домен соответствует сертификату
- ✅ Нет ошибок сертификата
- ✅ Редирект с HTTP на HTTPS работает
- ✅ Все ресурсы загружаются по HTTPS
- ✅ Нет смешанного контента (HTTP + HTTPS)

---

## 📋 Best Practices:

### ✅ Рекомендации:

1. **Всегда используйте HTTPS в production** — обязательное требование
2. **Настройте редирект HTTP → HTTPS** — автоматическое перенаправление
3. **Используйте валидные сертификаты** — от доверенных CA
4. **Обновляйте сертификаты вовремя** — до истечения срока действия
5. **Проверяйте сертификаты** — регулярно проверяйте валидность
6. **Используйте HSTS** — HTTP Strict Transport Security
7. **Избегайте смешанного контента** — все ресурсы должны быть HTTPS

---

## 🔄 Миграция с HTTP на HTTPS:

### Шаги миграции:

1. **Получение сертификата**
   - От доверенного CA (Let's Encrypt, DigiCert и др.)
   - Установка на сервер

2. **Настройка HTTPS**
   - Конфигурация веб-сервера
   - Настройка порта 443

3. **Редирект HTTP → HTTPS**
   - Автоматическое перенаправление
   - Обновление ссылок

4. **Проверка**
   - Тестирование всех endpoints
   - Проверка сертификата
   - Проверка редиректов

---

## 📊 Примеры ошибок в браузере:

### Chrome:

- 🔴 **Not Secure** — HTTP соединение
- ✅ **Secure** — HTTPS соединение
- ⚠️ **⚠ Not Secure** — проблемы с сертификатом

### Firefox:

- 🔴 **⚠** — HTTP соединение или проблемы с сертификатом
- ✅ **🔒** — HTTPS соединение

---

[← Вернуться к содержанию раздела](./README.md)
