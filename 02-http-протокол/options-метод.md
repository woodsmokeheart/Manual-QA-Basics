# 🔍 OPTIONS метод

> Получение информации о поддерживаемых методах и возможностях.

---

## 🔍 OPTIONS — получить информацию о ресурсе

**Назначение:** Получение информации о поддерживаемых HTTP методах и возможностях ресурса.

**Характеристика:** Используется для CORS и определения возможностей API.

---

## 📝 Пример запроса:

```http
OPTIONS /api/users HTTP/1.1
Host: api.example.com
Origin: https://example.com
Access-Control-Request-Method: POST
```

**Ответ:**
```http
HTTP/1.1 200 OK
Allow: GET, POST, PUT, DELETE, OPTIONS
Access-Control-Allow-Origin: https://example.com
Access-Control-Allow-Methods: GET, POST, PUT, DELETE
Access-Control-Allow-Headers: Content-Type, Authorization
Access-Control-Max-Age: 3600
```

**Результат:** Возвращена информация о поддерживаемых методах и CORS настройках.

---

## ⚙️ Особенности:

### ✅ Информация о поддерживаемых методах

- OPTIONS запросы возвращают список **поддерживаемых HTTP методов**
- Заголовок `Allow` содержит список методов
- Полезно для **документации API** и **автоматического обнаружения**

---

### 🌐 Используется для CORS

**CORS (Cross-Origin Resource Sharing):** Механизм для запросов между разными доменами.

**Preflight запрос:**
- Браузер автоматически отправляет OPTIONS перед некоторыми запросами
- Проверяет разрешения для кросс-доменных запросов
- Заголовки `Access-Control-*` определяют политику CORS

---

### 📋 Заголовки CORS:

| Заголовок | Описание |
|-----------|----------|
| `Access-Control-Allow-Origin` | Разрешенные домены |
| `Access-Control-Allow-Methods` | Разрешенные HTTP методы |
| `Access-Control-Allow-Headers` | Разрешенные заголовки |
| `Access-Control-Max-Age` | Время кеширования preflight запроса |

---

### 🔄 Идемпотентный

**Определение:** Повторный запрос дает тот же результат.

**Пример:**
```
OPTIONS /api/users → Allow: GET, POST, PUT, DELETE
OPTIONS /api/users → Allow: GET, POST, PUT, DELETE
```

**Преимущества:**
- Безопасно повторять запрос
- Можно кешировать ответ

---

### ✅ Возвращает 200 OK

**Статус код:** `200 OK` — стандартный ответ для OPTIONS.

**Альтернативные статусы:**
- `204 No Content` — если нет информации для возврата
- `405 Method Not Allowed` — если OPTIONS не поддерживается

---

## 💡 Ключевые моменты

> **📌 Запомните:**

- **Информация о поддерживаемых методах** — OPTIONS показывает доступные HTTP методы
- **Используется для CORS** — для настройки кросс-доменных запросов
- **Заголовки CORS** — определяют политику безопасности
- **Идемпотентный** — повторный запрос дает тот же результат

---

## 🎯 Когда использовать OPTIONS:

### ✅ Подходит для:

- Определение поддерживаемых методов ресурса
- Настройка CORS для кросс-доменных запросов
- Preflight запросы браузера
- Автоматическое обнаружение возможностей API
- Документация API через заголовки

### ❌ Не подходит для:

- Получение данных (используйте GET)
- Изменение ресурсов
- Когда CORS не используется

---

## 📊 Примеры использования:

### Пример 1: Проверка поддерживаемых методов

**Запрос:**
```http
OPTIONS /api/users HTTP/1.1
Host: api.example.com
```

**Ответ:**
```http
HTTP/1.1 200 OK
Allow: GET, POST, PUT, DELETE, OPTIONS
```

**Результат:** Ресурс поддерживает GET, POST, PUT, DELETE методы.

---

### Пример 2: CORS Preflight запрос

**Запрос (от браузера):**
```http
OPTIONS /api/users HTTP/1.1
Host: api.example.com
Origin: https://example.com
Access-Control-Request-Method: POST
Access-Control-Request-Headers: Content-Type, Authorization
```

**Ответ:**
```http
HTTP/1.1 200 OK
Access-Control-Allow-Origin: https://example.com
Access-Control-Allow-Methods: GET, POST, PUT, DELETE
Access-Control-Allow-Headers: Content-Type, Authorization
Access-Control-Max-Age: 3600
```

**Результат:** Браузер получил разрешение на POST запрос с указанными заголовками.

---

### Пример 3: Проверка возможностей конкретного ресурса

**Запрос:**
```http
OPTIONS /api/users/1 HTTP/1.1
Host: api.example.com
```

**Ответ:**
```http
HTTP/1.1 200 OK
Allow: GET, PUT, DELETE, OPTIONS
```

**Результат:** Конкретный пользователь поддерживает GET, PUT, DELETE (но не POST, так как ресурс уже создан).

---

## 🌐 CORS в деталях:

### Простой запрос:

**Не требует preflight:**
- GET, HEAD, POST
- Простые заголовки (Content-Type: text/plain)
- Без кастомных заголовков

### Preflight запрос:

**Требует OPTIONS:**
- PUT, DELETE, PATCH
- Кастомные заголовки (Authorization, X-Custom-Header)
- Content-Type: application/json

**Процесс:**
1. Браузер отправляет OPTIONS
2. Сервер отвечает с CORS заголовками
3. Если разрешено → браузер отправляет основной запрос
4. Если запрещено → браузер блокирует запрос

---

## 🔄 Сравнение OPTIONS с другими методами:

| Характеристика | OPTIONS | GET | HEAD |
|----------------|---------|-----|------|
| **Назначение** | Информация о методах | Получение данных | Получение заголовков |
| **Тело ответа** | Обычно пустое | Есть | Нет |
| **CORS** | Используется для preflight | Обычный запрос | Обычный запрос |
| **Заголовок Allow** | ✅ Возвращает | ❌ Нет | ❌ Нет |

---

## 🧪 Тестирование OPTIONS запросов:

### Что проверять:

- ✅ Возвращается заголовок Allow с правильными методами
- ✅ CORS заголовки настроены правильно
- ✅ Preflight запросы обрабатываются корректно
- ✅ Разные Origin обрабатываются правильно
- ✅ Access-Control-Max-Age работает для кеширования
- ✅ OPTIONS работает для разных ресурсов

---

## 🔒 Безопасность CORS:

### ⚠️ Важно помнить:

- **Access-Control-Allow-Origin** — не используйте `*` для защищенных ресурсов
- **Access-Control-Allow-Credentials** — требует явного указания Origin
- **Проверка Origin** — валидируйте домены запросов
- **Ограничение методов** — разрешайте только необходимые методы

### ✅ Рекомендации:

- Указывайте конкретные домены вместо `*`
- Ограничивайте разрешенные методы
- Используйте Access-Control-Max-Age для кеширования
- Проверяйте Origin на сервере

---

## 📋 Best Practices:

### ✅ Рекомендации:

1. **Возвращайте заголовок Allow** — для всех ресурсов
2. **Настройте CORS правильно** — для кросс-доменных запросов
3. **Используйте конкретные домены** — вместо `*` для безопасности
4. **Кешируйте preflight** — используйте Access-Control-Max-Age
5. **Документируйте CORS политику** — объясняйте настройки

---

## 📊 Примеры CORS конфигурации:

### Разрешить все домены (небезопасно):

```http
Access-Control-Allow-Origin: *
Access-Control-Allow-Methods: GET, POST, PUT, DELETE
```

### Разрешить конкретный домен:

```http
Access-Control-Allow-Origin: https://example.com
Access-Control-Allow-Methods: GET, POST
Access-Control-Allow-Headers: Content-Type, Authorization
Access-Control-Allow-Credentials: true
```

### Разрешить несколько доменов (через проверку):

Сервер проверяет Origin и возвращает соответствующий заголовок.

---

[← Вернуться к содержанию раздела](./README.md)
