# 🗑️ DELETE метод

> Удаление ресурсов.

---

## 🔍 DELETE — удалить ресурс

**Назначение:** Удаление существующего ресурса с сервера.

**Характеристика:** Удаляет ресурс по указанному пути.

---

## 📝 Пример запроса:

```http
DELETE /api/users/1 HTTP/1.1
Host: api.example.com
Authorization: Bearer token123
```

**Ответ:**
```http
HTTP/1.1 204 No Content
```

**Результат:** Ресурс успешно удален, тело ответа пустое.

---

## 🔄 Повторный DELETE (Идемпотентность):

### Первый запрос:

```http
DELETE /api/users/1 HTTP/1.1
```

**Ответ:**
```http
HTTP/1.1 204 No Content
```

**Результат:** Ресурс удален.

---

### Повторный запрос:

```http
DELETE /api/users/1 HTTP/1.1
```

**Ответ:**
```http
HTTP/1.1 404 Not Found
```

**Результат:** Ресурс уже не существует, но состояние сервера не изменилось.

**Идемпотентность:** Повторный DELETE не изменяет состояние сервера (ресурс уже удален).

---

## 🔄 Soft Delete vs Hard Delete:

### Soft Delete (Мягкое удаление):

**Определение:** Ресурс помечается как удаленный, но физически остается в базе данных.

**Запрос:**
```http
DELETE /api/users/1 HTTP/1.1
```

**Операция в БД:**
```sql
UPDATE users SET deleted_at = NOW() WHERE id = 1
```

**Характеристики:**
- Ресурс помечается как удаленный (`deleted_at`)
- Данные остаются в базе
- Можно восстановить
- Используется для аудита и истории

---

### Hard Delete (Физическое удаление):

**Определение:** Ресурс полностью удаляется из базы данных.

**Запрос:**
```http
DELETE /api/users/1?hard=true HTTP/1.1
```

**Операция в БД:**
```sql
DELETE FROM users WHERE id = 1
```

**Характеристики:**
- Ресурс полностью удален
- Данные удалены из базы
- Восстановление невозможно
- Используется для полной очистки

---

## ⚙️ Особенности:

### ✅ Удаляет ресурс

- DELETE запросы **удаляют** ресурс с сервера
- Ресурс должен **существовать** (или вернется 404)
- Операция **необратима** (если не используется soft delete)

---

### 📋 Обычно без body

**Принцип:** DELETE запросы обычно не содержат тело запроса.

**Стандартный запрос:**
```http
DELETE /api/users/1 HTTP/1.1
Host: api.example.com
Authorization: Bearer token123
```

**Без body:** Вся информация передается через URL.

**Исключения:**
- Некоторые API могут требовать подтверждение в body
- Каскадное удаление может требовать параметры

---

### 🔄 Идемпотентный

**Определение:** Повторный запрос не изменяет состояние сервера.

**Пример:**
```
DELETE /api/users/1 → 204 No Content (удален)
DELETE /api/users/1 → 404 Not Found (уже удален, состояние не изменилось)
DELETE /api/users/1 → 404 Not Found (все еще удален)
```

**Преимущества:**
- Безопасно повторять запрос
- Предсказуемое поведение
- Можно использовать для синхронизации

---

### ✅ Возвращает 204 или 200

**Статус коды:**

| Код | Описание | Когда используется |
|-----|----------|-------------------|
| **204 No Content** | Успешное удаление | Стандартный ответ, тело пустое |
| **200 OK** | Успешное удаление | Если возвращается информация об удаленном ресурсе |
| **404 Not Found** | Ресурс не найден | Ресурс уже удален или не существует |
| **403 Forbidden** | Нет прав | Пользователь не имеет прав на удаление |
| **409 Conflict** | Конфликт | Ресурс используется и не может быть удален |

---

## 💡 Ключевые моменты

> **📌 Запомните:**

- **Удаляет ресурс** — DELETE удаляет ресурс с сервера
- **Обычно без body** — вся информация передается через URL
- **Идемпотентный** — повторный запрос не изменяет состояние сервера
- **Возвращает 204 или 200** — стандартные статус коды успешного удаления

---

## 🎯 Когда использовать DELETE:

### ✅ Подходит для:

- Удаление ресурсов по ID
- Удаление пользователей, заказов, статей
- Очистка данных
- Удаление связей между ресурсами

### ❌ Не подходит для:

- Получение данных (используйте GET)
- Создание ресурсов (используйте POST)
- Обновление ресурсов (используйте PUT/PATCH)
- Массовое удаление (может требовать специальный endpoint)

---

## 📊 Примеры использования:

### Пример 1: Удаление пользователя

**Запрос:**
```http
DELETE /api/users/123 HTTP/1.1
Host: api.example.com
Authorization: Bearer token123
```

**Ответ (Soft Delete):**
```http
HTTP/1.1 204 No Content
```

**Результат:** Пользователь помечен как удаленный (`deleted_at` установлен).

---

### Пример 2: Удаление заказа

**Запрос:**
```http
DELETE /api/orders/456 HTTP/1.1
Host: api.example.com
Authorization: Bearer token123
```

**Ответ:**
```http
HTTP/1.1 200 OK
Content-Type: application/json

{
  "id": 456,
  "status": "deleted",
  "deleted_at": "2024-01-15T10:30:00Z"
}
```

**Результат:** Заказ удален, возвращена информация об удалении.

---

### Пример 3: Физическое удаление

**Запрос:**
```http
DELETE /api/users/123?hard=true HTTP/1.1
Host: api.example.com
Authorization: Bearer admin_token
```

**Ответ:**
```http
HTTP/1.1 204 No Content
```

**Результат:** Пользователь полностью удален из базы данных.

---

## 🔄 Сравнение Soft и Hard Delete:

| Характеристика | Soft Delete | Hard Delete |
|----------------|-------------|-------------|
| **Данные в БД** | Остаются | Удаляются |
| **Восстановление** | ✅ Возможно | ❌ Невозможно |
| **Аудит** | ✅ Сохраняется история | ❌ История теряется |
| **Производительность** | Быстрее (UPDATE) | Медленнее (DELETE) |
| **Использование** | Рекомендуется | Для полной очистки |

---

## 🧪 Тестирование DELETE запросов:

### Что проверять:

- ✅ Успешное удаление ресурса (204/200)
- ✅ Идемпотентность (повторный запрос → 404)
- ✅ Обработка несуществующего ресурса (404)
- ✅ Проверка прав доступа (403)
- ✅ Проверка зависимостей (409 Conflict)
- ✅ Soft delete vs Hard delete
- ✅ Восстановление после soft delete (если поддерживается)

---

## ⚠️ Частые ошибки:

### ❌ Ошибка 1: Использование GET для удаления

**Неправильно:**
```http
GET /api/users/1/delete HTTP/1.1
```

**Проблема:** GET не должен изменять состояние сервера.

**Правильно:** Использовать DELETE метод.

---

### ❌ Ошибка 2: Игнорирование зависимостей

**Проблема:** Удаление ресурса, который используется другими ресурсами.

**Пример:**
```
DELETE /api/users/1
→ Пользователь удален, но его заказы остались без владельца
```

**Решение:** Проверять зависимости перед удалением или использовать каскадное удаление.

---

## 🔒 Безопасность:

### ⚠️ Важно помнить:

- **Авторизация** — проверяйте права на удаление
- **Подтверждение** — для критичных операций может потребоваться подтверждение
- **Аудит** — логируйте все операции удаления
- **Мягкое удаление** — используйте soft delete для возможности восстановления
- **Защита от случайного удаления** — требуйте подтверждение для важных ресурсов

---

## 📋 Best Practices:

### ✅ Рекомендации:

1. **Используйте Soft Delete** — для возможности восстановления
2. **Возвращайте 204 No Content** — стандартный ответ для DELETE
3. **Проверяйте зависимости** — перед удалением проверяйте связанные ресурсы
4. **Логируйте удаления** — для аудита и отладки
5. **Используйте идемпотентность** — повторный DELETE не должен вызывать ошибку

---

## 🔄 Каскадное удаление:

### Определение:

**Каскадное удаление** — автоматическое удаление связанных ресурсов.

**Пример:**
```
DELETE /api/users/1
→ Удаляет пользователя
→ Автоматически удаляет его заказы
→ Автоматически удаляет его комментарии
```

**Важно:** Документируйте поведение каскадного удаления.

---

## 📊 Статус коды DELETE:

### Успешные ответы:

- **204 No Content** — ресурс успешно удален, тело пустое
- **200 OK** — ресурс удален, возвращается информация об удалении

### Ошибки:

- **404 Not Found** — ресурс не существует или уже удален
- **403 Forbidden** — нет прав на удаление
- **409 Conflict** — ресурс используется и не может быть удален
- **400 Bad Request** — невалидный запрос

---

[← Вернуться к содержанию раздела](./README.md)
