# 📊 HTTP статус коды

> Основные коды ответов и их значение.

---

## 🔍 Классификация статус кодов:

HTTP статус коды делятся на 5 категорий по первой цифре:

| Категория | Диапазон | Описание |
|-----------|----------|----------|
| **1xx** | 100-199 | Информационные |
| **2xx** | 200-299 | Успех |
| **3xx** | 300-399 | Перенаправление |
| **4xx** | 400-499 | Ошибка клиента |
| **5xx** | 500-599 | Ошибка сервера |

---

## ✅ 2xx - Успех (Success)

### 200 OK

**Описание:** Успешный запрос.

**Когда используется:**
- GET запросы — данные успешно получены
- PUT/PATCH запросы — ресурс успешно обновлен
- POST запросы — операция выполнена успешно (если не создан новый ресурс)

**Пример:**
```http
GET /api/users/1 HTTP/1.1
→ HTTP/1.1 200 OK
Content-Type: application/json

{"id": 1, "name": "John"}
```

**Что проверять:**
- ✅ Данные корректны
- ✅ Формат ответа соответствует Content-Type
- ✅ Все запрошенные поля присутствуют

---

### 201 Created

**Описание:** Ресурс успешно создан.

**Когда используется:**
- POST запросы — новый ресурс создан
- PUT запросы — ресурс создан (если не существовал)

**Пример:**
```http
POST /api/users HTTP/1.1
Content-Type: application/json

{"name": "John", "email": "john@example.com"}
→ HTTP/1.1 201 Created
Location: /api/users/123
Content-Type: application/json

{"id": 123, "name": "John", "email": "john@example.com"}
```

**Особенности:**
- ✅ Должен возвращать заголовок `Location` с URL нового ресурса
- ✅ Тело ответа обычно содержит созданный ресурс
- ✅ Используется для операций создания

**Что проверять:**
- ✅ Статус код 201
- ✅ Заголовок Location указывает на новый ресурс
- ✅ Созданный ресурс в теле ответа
- ✅ Ресурс действительно создан в системе

---

### 204 No Content

**Описание:** Успех без тела ответа.

**Когда используется:**
- DELETE запросы — ресурс успешно удален
- PUT/PATCH запросы — обновление успешно, но тело не требуется
- Операции, которые не требуют возврата данных

**Пример:**
```http
DELETE /api/users/1 HTTP/1.1
→ HTTP/1.1 204 No Content
```

**Особенности:**
- ✅ Тело ответа отсутствует
- ✅ Заголовки могут содержать полезную информацию
- ✅ Успешная операция без данных для возврата

**Что проверять:**
- ✅ Статус код 204
- ✅ Тело ответа пустое
- ✅ Операция выполнена успешно (удален/обновлен)

---

## 🔄 3xx - Перенаправление (Redirection)

### 301 Moved Permanently

**Описание:** Ресурс перемещен навсегда.

**Когда используется:**
- Постоянное изменение URL ресурса
- Редирект на новый домен
- Реструктуризация API

**Пример:**
```http
GET /old-api/users HTTP/1.1
→ HTTP/1.1 301 Moved Permanently
Location: /api/users
```

**Особенности:**
- ✅ Браузеры кешируют редирект
- ✅ Поисковые системы обновляют индексы
- ✅ Клиенты должны обновить URL

**Что проверять:**
- ✅ Заголовок Location указывает на новый URL
- ✅ Редирект работает корректно
- ✅ Новый URL доступен

---

### 302 Found (Temporary Redirect)

**Описание:** Временное перенаправление.

**Когда используется:**
- Временное изменение URL
- Редирект на другой сервер
- Временная недоступность ресурса

**Пример:**
```http
GET /api/users HTTP/1.1
→ HTTP/1.1 302 Found
Location: https://backup.example.com/api/users
```

**Особенности:**
- ✅ Не кешируется браузерами
- ✅ Клиенты должны продолжать использовать старый URL
- ✅ Может быть изменен в будущем

**Что проверять:**
- ✅ Заголовок Location присутствует
- ✅ Редирект работает
- ✅ После редиректа запрос выполняется успешно

---

### 304 Not Modified

**Описание:** Кеш актуален, ресурс не изменился.

**Когда используется:**
- Клиент использует условные заголовки (If-Modified-Since, If-None-Match)
- Ресурс не изменился с момента последнего запроса
- Оптимизация трафика

**Пример:**
```http
GET /api/users/1 HTTP/1.1
If-None-Match: "abc123"
→ HTTP/1.1 304 Not Modified
ETag: "abc123"
```

**Особенности:**
- ✅ Тело ответа отсутствует
- ✅ Клиент использует кешированную версию
- ✅ Экономит трафик и время

**Что проверять:**
- ✅ Статус код 304
- ✅ Тело ответа пустое
- ✅ ETag или Last-Modified совпадают с запросом
- ✅ Клиент использует кеш

---

## ❌ 4xx - Ошибка клиента (Client Error)

### 400 Bad Request

**Описание:** Неверный запрос.

**Когда используется:**
- Неправильный синтаксис запроса
- Невалидные данные в запросе
- Отсутствуют обязательные параметры
- Неправильный формат данных

**Пример:**
```http
POST /api/users HTTP/1.1
Content-Type: application/json

{"name": "John"}  // Отсутствует обязательное поле email
→ HTTP/1.1 400 Bad Request
Content-Type: application/json

{
  "error": "Validation failed",
  "details": {
    "email": "Field is required"
  }
}
```

**Особенности:**
- ✅ Проблема в запросе клиента
- ✅ Клиент должен исправить запрос
- ✅ Обычно содержит описание ошибки

**Что проверять:**
- ✅ Описание ошибки понятно
- ✅ Указаны конкретные поля с ошибками
- ✅ Клиент может исправить запрос

---

### 401 Unauthorized

**Описание:** Нужна аутентификация.

**Когда используется:**
- Отсутствует токен аутентификации
- Токен невалидный или истек
- Требуется авторизация для доступа

**Пример:**
```http
GET /api/users/1 HTTP/1.1
→ HTTP/1.1 401 Unauthorized
WWW-Authenticate: Bearer realm="api"
Content-Type: application/json

{
  "error": "Unauthorized",
  "message": "Authentication required"
}
```

**Особенности:**
- ✅ Должен возвращать заголовок `WWW-Authenticate`
- ✅ Клиент должен предоставить учетные данные
- ✅ Не означает отсутствие прав (это 403)

**Что проверять:**
- ✅ Заголовок WWW-Authenticate присутствует
- ✅ Сообщение об ошибке понятно
- ✅ После добавления токена запрос работает

---

### 403 Forbidden

**Описание:** Доступ запрещен.

**Когда используется:**
- Клиент аутентифицирован, но нет прав доступа
- Ресурс существует, но доступ запрещен
- Недостаточно прав для выполнения операции

**Пример:**
```http
DELETE /api/admin/users/1 HTTP/1.1
Authorization: Bearer user_token
→ HTTP/1.1 403 Forbidden
Content-Type: application/json

{
  "error": "Forbidden",
  "message": "Insufficient permissions"
}
```

**Особенности:**
- ✅ Клиент аутентифицирован (в отличие от 401)
- ✅ Проблема в правах доступа
- ✅ Даже с правильными учетными данными доступ запрещен

**Что проверять:**
- ✅ Разница между 401 и 403 понятна
- ✅ Сообщение объясняет причину запрета
- ✅ Правильные права дают доступ

---

### 404 Not Found

**Описание:** Ресурс не найден.

**Когда используется:**
- Ресурс не существует
- Неправильный URL
- Ресурс был удален

**Пример:**
```http
GET /api/users/999 HTTP/1.1
→ HTTP/1.1 404 Not Found
Content-Type: application/json

{
  "error": "Not Found",
  "message": "User with id 999 not found"
}
```

**Особенности:**
- ✅ Самый распространенный статус код ошибки
- ✅ Может использоваться для скрытия существования ресурса (безопасность)
- ✅ Клиент должен проверить URL

**Что проверять:**
- ✅ Сообщение об ошибке информативно
- ✅ Проверка несуществующих ID
- ✅ Проверка неправильных путей

---

### 422 Unprocessable Entity

**Описание:** Валидация не прошла.

**Когда используется:**
- Данные синтаксически правильны, но семантически невалидны
- Бизнес-логика не позволяет обработать запрос
- Нарушены правила валидации

**Пример:**
```http
POST /api/users HTTP/1.1
Content-Type: application/json

{
  "email": "invalid-email",  // Невалидный формат email
  "age": -5                   // Отрицательный возраст
}
→ HTTP/1.1 422 Unprocessable Entity
Content-Type: application/json

{
  "error": "Validation failed",
  "details": {
    "email": "Invalid email format",
    "age": "Age must be positive"
  }
}
```

**Особенности:**
- ✅ Запрос синтаксически корректен (в отличие от 400)
- ✅ Проблема в валидации данных
- ✅ Обычно содержит детали ошибок валидации

**Что проверять:**
- ✅ Детали ошибок валидации понятны
- ✅ Указаны все невалидные поля
- ✅ Клиент может исправить данные

---

### 429 Too Many Requests

**Описание:** Rate limit превышен.

**Когда используется:**
- Превышен лимит запросов
- Защита от злоупотребления API
- Ограничение нагрузки на сервер

**Пример:**
```http
GET /api/users HTTP/1.1
→ HTTP/1.1 429 Too Many Requests
Retry-After: 60
Content-Type: application/json

{
  "error": "Too Many Requests",
  "message": "Rate limit exceeded",
  "retry_after": 60
}
```

**Особенности:**
- ✅ Должен возвращать заголовок `Retry-After`
- ✅ Указывает время до следующего разрешенного запроса
- ✅ Защита от злоупотребления

**Что проверять:**
- ✅ Заголовок Retry-After присутствует
- ✅ После указанного времени запрос работает
- ✅ Лимиты настроены правильно

---

## 🔴 5xx - Ошибка сервера (Server Error)

### 500 Internal Server Error

**Описание:** Ошибка на сервере.

**Когда используется:**
- Неожиданная ошибка на сервере
- Ошибка в коде приложения
- Проблемы с базой данных
- Любая внутренняя ошибка сервера

**Пример:**
```http
GET /api/users/1 HTTP/1.1
→ HTTP/1.1 500 Internal Server Error
Content-Type: application/json

{
  "error": "Internal Server Error",
  "message": "An unexpected error occurred"
}
```

**Особенности:**
- ✅ Проблема на стороне сервера
- ✅ Клиент не может исправить запрос
- ✅ Требуется исправление на сервере

**Что проверять:**
- ✅ Логи сервера содержат детали ошибки
- ✅ Ошибка воспроизводится или случайна
- ✅ После исправления запрос работает

---

### 502 Bad Gateway

**Описание:** Проблема с прокси или шлюзом.

**Когда используется:**
- Прокси-сервер получил невалидный ответ от upstream сервера
- Проблема с балансировщиком нагрузки
- Недоступность upstream сервера

**Пример:**
```http
GET /api/users HTTP/1.1
→ HTTP/1.1 502 Bad Gateway
Content-Type: text/html

<html>
  <body>502 Bad Gateway</body>
</html>
```

**Особенности:**
- ✅ Проблема в инфраструктуре, а не в приложении
- ✅ Может быть временной
- ✅ Требует проверки прокси/шлюза

**Что проверять:**
- ✅ Доступность upstream серверов
- ✅ Конфигурация прокси/балансировщика
- ✅ Восстановление после перезапуска

---

### 503 Service Unavailable

**Описание:** Сервис недоступен.

**Когда используется:**
- Временная недоступность сервиса
- Техническое обслуживание
- Перегрузка сервера
- Плановые работы

**Пример:**
```http
GET /api/users HTTP/1.1
→ HTTP/1.1 503 Service Unavailable
Retry-After: 300
Content-Type: application/json

{
  "error": "Service Unavailable",
  "message": "Service is temporarily unavailable",
  "retry_after": 300
}
```

**Особенности:**
- ✅ Временная недоступность
- ✅ Может возвращать `Retry-After`
- ✅ Сервис должен восстановиться

**Что проверять:**
- ✅ Заголовок Retry-After (если есть)
- ✅ Восстановление сервиса
- ✅ Причина недоступности

---

## 💡 Ключевые моменты

> **📌 Запомните:**

- **2xx - все хорошо** — успешные запросы, операция выполнена
- **4xx - клиент ошибся** — проблема в запросе клиента, нужно исправить запрос
- **5xx - сервер сломался** — проблема на сервере, клиент не может исправить
- **401 vs 403: нет токена vs нет прав** — 401 = не аутентифицирован, 403 = нет прав доступа

---

## 📊 Сводная таблица статус кодов:

| Код | Категория | Описание | Когда использовать |
|-----|-----------|----------|-------------------|
| **200** | 2xx | OK | Успешный запрос |
| **201** | 2xx | Created | Ресурс создан |
| **204** | 2xx | No Content | Успех без тела |
| **301** | 3xx | Moved Permanently | Постоянный редирект |
| **302** | 3xx | Found | Временный редирект |
| **304** | 3xx | Not Modified | Кеш актуален |
| **400** | 4xx | Bad Request | Неверный запрос |
| **401** | 4xx | Unauthorized | Нужна аутентификация |
| **403** | 4xx | Forbidden | Доступ запрещен |
| **404** | 4xx | Not Found | Ресурс не найден |
| **422** | 4xx | Unprocessable Entity | Валидация не прошла |
| **429** | 4xx | Too Many Requests | Rate limit |
| **500** | 5xx | Internal Server Error | Ошибка сервера |
| **502** | 5xx | Bad Gateway | Проблема с прокси |
| **503** | 5xx | Service Unavailable | Сервис недоступен |

---

## 🔍 Разница между похожими кодами:

### 400 vs 422:

| Код | Описание | Когда использовать |
|-----|----------|-------------------|
| **400** | Bad Request | Синтаксическая ошибка в запросе |
| **422** | Unprocessable Entity | Синтаксис правильный, но валидация не прошла |

**Пример:**
- 400: `{"name": "John"` — невалидный JSON (синтаксис)
- 422: `{"email": "invalid"}` — валидный JSON, но невалидный email (валидация)

---

### 401 vs 403:

| Код | Описание | Когда использовать |
|-----|----------|-------------------|
| **401** | Unauthorized | Нет токена или токен невалидный |
| **403** | Forbidden | Токен валидный, но нет прав |

**Пример:**
- 401: Запрос без токена → нужна аутентификация
- 403: Запрос с токеном пользователя к админ-эндпоинту → нет прав

---

## 🧪 Тестирование статус кодов:

### Что проверять:

- ✅ Правильный статус код для каждого сценария
- ✅ Сообщения об ошибках информативны
- ✅ Заголовки соответствуют статусу (Location для 201, Retry-After для 429/503)
- ✅ Тело ответа соответствует статусу (пустое для 204, детали для 4xx)
- ✅ Поведение соответствует спецификации HTTP

---

## 📋 Best Practices:

### ✅ Рекомендации:

1. **Используйте правильные статус коды** — следуйте HTTP спецификации
2. **Возвращайте информативные сообщения** — особенно для 4xx ошибок
3. **Добавляйте детали валидации** — для 422 ошибок
4. **Используйте заголовки** — Location для 201, Retry-After для rate limit
5. **Логируйте 5xx ошибки** — для отладки проблем на сервере

---

[← Вернуться к содержанию раздела](./README.md)
